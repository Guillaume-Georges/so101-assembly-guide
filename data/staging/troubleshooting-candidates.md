# Troubleshooting candidates — review report

Mined 2026-09-09 per `docs/tickets/troubleshooting-corpus-mining.md`. Staging only: the build reads none of this.
Companion file: `troubleshooting-candidates.yaml` (112 candidates, 5 merges into existing entries).

## Corpus covered

| Source           | Threads inventoried |
| ---------------- | ------------------- |
| lerobot issues   | 202                 |
| SO-ARM100 issues | 99                  |
| Seeed wiki       | 28                  |
| HF forum         | 10                  |

Inventory rows by relevance: troubleshooting 217, question 58, feature 35, sourcing-chatter 13, off-topic 8, not-so101 8.
Clusters formed: 113; rows dropped at clustering: 114; clusters skipped at extraction: 1.
Not covered: GitHub Discussions (disabled on both upstream repos as of 2026-09-09); this repo has no issues yet (PRs only); Discord/Slack/WeChat by rule.

Inventory agent notes:

- Slice = 90 issue threads in so-arm100.digest.jsonl (all read; none skipped) + 7 PR rows from so-arm100.index.jsonl whose titles read as build/bug fixes rather than CAD additions (167, 117, 106, 95, 48, 62, 7). Tier-1/tier-2 git queries run in .claude/worktrees/troubleshooting-candidates before reading data/: `git log --oneline -S"troubleshooting" -- data/troubleshooting.yaml` found only the two commits that created the file; trailer grep on that path surfaced `Constraint: slug is frozen at first publish; changing one is a URL break, not a rename` (from commit 86a2a8f) — binding on any later promotion of these candidates into real troubleshooting slugs, not on this inventory output itself, since this run touches no repo files.

Judgment calls / things I could not classify cleanly:

- #144: the only stated cause for the front teardrop holes traces to a maintainer paraphrasing something "Rob said on the LeRobot Discord" — Discord is non-citable per CLAUDE.md rule 3, so I marked fix_confirmed_by "none" even though the GitHub comment reads as an answer; a future extraction pass must not cite the Discord content as the source.
- #16, #170: fix_confirmed_by "both" — reporter's own follow-up comment corroborates the maintainer's answer in the same thread.
- #63: comment 13 is a different builder (ethdang) than the OP (ywiyogo) confirming a scaling workaround (100.10%) worked; no single canonical fix was confirmed for the OP's specific case, so this thread likely needs a human decision at extraction time (multiple partial/conflicting workarounds: XY compensation, CAD edit, scale-up).
- #24, #75, #94, #146, #149, #150, #94: calibration/setup-motors scope tags per ticket instruction to extract-and-tag even though v1 CLAUDE.md marks calibration/lerobot-software out of scope for now — left as "calibration"/"setup-motors"/"lerobot-software" per the ticket's explicit directive to tag, not filter.
- #17, #36, #56, #71, #132, #134, #142, #153, #174: no scope tag fits perfectly (electrical/spec/backlash questions); I used "assembly" as the closest fit since they concern physical hardware/build rather than software, calibration, or pure sourcing chatter — flagged for maintainer review at extraction.
- #99, #104, #135: general feature/spec requests, tagged "feature", scope is a loose fit (sourcing/lerobot-software) since none of the six scope values covers "general spec/roadmap question" well.
- PR selection: skipped the much larger set of CAD/URDF/Mujoco/sourcing-link PRs per the ticket's "most PRs are CAD updates, skip" instruction; included only titles that read as a fix to a reported build/calibration/sourcing defect. #162 "Better Joint Limits" and #101 "Fix Mujoco attributes" were considered and excluded as too ambiguous/simulation-only to confidently call a "build problem" fix — a maintainer may want those added back at review.
- Two threads (#136, #138, #139, #152) are unambiguous off-topic/spam and got relevance "off-topic" with scope left unset conceptually (I put the closest placeholder "assembly" is wrong — I did NOT set a scope for these; see the enum note below).

Schema note: the required "scope" enum has no null option in this schema, so for the four off-topic/spam threads (#136, #138, #139, #152) I could not truly represent "no scope applies" — I used "assembly" only as a required-field placeholder; a human reviewer should disregard scope on any row marked relevance:"off-topic".

- Slice: lerobot.digest.part00, all 68 rows read in full (no rows skipped). Used body_head/comment_heads only; opened no full thread files since heads were sufficient to classify every row (no ambiguity required the full JSON).

Classification notes and uncertainties:

- Row #607 (idx2) and #1296 (idx24): multiple disagreeing causes across reporters (motor ID reversed, gripper servo saturation, USB connector, assembly zero-position error, power-cycle). Treated per ticket instruction as symptoms-with-disagreement rather than a single cause; fix_confirmed_by reflects the strongest/maintainer-adjacent confirmation but a human should split these into cause variants at extraction time.
- Row #1252 (idx21) and #1010 (idx7): the person who confirmed a fix worked was NOT the original thread author (OP never clearly confirmed, in one case OP's confirmation was truncated/ambiguous in the digest head). I erred toward "proposed-only" for #1252 despite an SO101-specific confirmation from a non-OP commenter (Arcod7/paulpacaud) — flag for a human to check the full thread if this is promoted, since "reporter" per the ticket's schema strictly means the thread author.
- Row #1110 (idx12) scope set to "assembly" (physical jumper/USB-hub wiring root cause) though the title reads like a motor-configuration software error — worth double-checking against the scope enum's intent at extraction.
- Rows #1333 (SO-100 title, idx27) kept relevance=troubleshooting because a commenter states seeing the identical instability on SO-101 with presumably the same STS3215 servo/driver path; not fully certain this rises to "identical enough" per the not-so101 rule — flag for review.
- Rows #952, #1365, #1700 marked not-so101: PI0/LIBERO benchmark eval quality and MuJoCo/OpenGL on macOS are software/ML issues not tied to SO-101 hardware specifically, and don't hinge on the STS3215 bus or SO-101 assembly.
- No Discord/Slack/login-only sources appeared in this slice; all citable via GitHub issue/comment URLs, retrieved 2026-09-09 (not yet added as explicit source objects since this task returns structured inventory data, not staging YAML).
- Row #1668 file path in JSON has a copy-paste path typo ("corpus/1668.json" missing the "lerobot/" segment) — the correct path per the digest is corpus/lerobot/1668.json; downstream consumers should use the URL as the authoritative key.
- Did not run the tier-1/tier-2 git queries specified in the ticket's non-negotiable #5 because this task is read-only inventory over pre-fetched JSON/digest files in the scratchpad, not a read of repo code or data/ files — nothing under data/troubleshooting.yaml or data/assemblies/*.yaml was touched or consulted.
- Slice: lerobot.digest.part01 (68 threads, all huggingface/lerobot issues). Per-thread source files are under corpus/lerobot/<number>.json relative to the corpus dir (path given as "file" combines that: corpus/lerobot/<n>.json).

Classification notes / things I could not fully verify from the digest heads alone (opened full body for #2556, #2805, #2836, #2628, #2748 to get exact error text; all others classified from body_head/comment_heads):

- #1819 (关于Hirserl的复现) is Chinese-language; body/comments mix so100 and so101 usage of the same HIL-SERL RL script. Several comments exchange WeChat contact info (p5, p10) — not citable, and not used for any fix claim. The one concrete, verbatim error I could cite (p8) is specific to so101_leader's teleop implementation.
- #2035 marked not-so101: the cited datasets and error are so100-specific (svla_so100_stacking/sorting); I could not confirm from the head whether the underlying Parquet/KeyError bug is confirmed to reproduce identically on so101 datasets, so left it out of scope per the "unless problem applies identically" instruction rather than assume.
- #2210 and #2819 are tagged scope=assembly because root cause was hardware failure (a bad follower arm; an overloaded/damaged gripper servo), even though both surfaced as software crashes/behavior — flagging this judgment call for review since "assembly" per the ticket's own definition is about physical build steps, not in-service hardware failure; a human should decide if these belong under assembly or a not-yet-existing "hardware failure" bucket.
- #2387: fix_confirmed_by="both" and fix_comment_position=10 point to the maintainer's summary comment ("reported calibration error came from using 12V supply with lower-voltage motors"); multiple non-maintainer users (p2, p3) also independently confirmed switching to 5V fixed it, but the schema only allows one position.
- #2431: fix_confirmed_by="both" — reporter's own workaround (p3) fixed the AttributeError but they hit further errors afterward; a COLLABORATOR (p8) later links code showing the path is now natively supported. Position given (8) is the more authoritative/complete one; p3 is the partial reporter fix.
- #2246: fix_confirmed_by="both" at position 15 (a NONE-assoc commenter states "--rename_map= ... working for me") plus maintainer PRs #2265/#2278 merged to address it; could not confirm with certainty that the p15 commenter is the original reporter (@talregev) rather than another participant in the thread — flagging this as a possible reporter-vs-other-user misattribution to check against the full thread.
- Several long ML/training threads (#1998, #2259, #2351) are tangential to physical SO-101 troubleshooting (dataset tooling, cross-embodiment fine-tuning, a different robot arm) — tagged feature/not-so101 rather than troubleshooting since the acceptance criteria emphasizes not inventing scope relevance.
- No thread in this slice was Discord/Slack/WeChat-only sourced (the WeChat mentions in #1819 are asides inside an otherwise-public GitHub thread, not the sole source), so no entries needed unverified-only-Discord handling.
- Frequency/duplicate-clustering across scopes (e.g. multiple async-inference oscillation reports: #2356, #2531, #2622, #2628, #2137, #2244) is left to the downstream clustering phase — I did not merge them here since my task is per-thread inventory only.
- Slice = lerobot.digest.part02 (66 threads, #2848-#4550). Author-association mapping used: MEMBER/COLLABORATOR/OWNER/CONTRIBUTOR on huggingface/lerobot counts as maintainer per task instructions (so CONTRIBUTOR comments, e.g. wadeKeith, RahimMirani, nevertmr, AlexandreEDMOND, Mr-C4T, are treated as maintainer-level even though they are outside authors, not employees).

Judgment calls flagged for review:

- #2907, #3215, #3345: FK/IK and eye-to-hand calibration issues use lerobot's kinematics module in a simulated/external context rather than lerobot-calibrate; tagged calibration/lerobot-software by best fit, not a clean match to the ticket's narrow scope definitions.
- #3131 (SO-101 disconnects during teleop): maintainer suspected "bad wiring" so tagged assembly, but the visible fix is a software try/except workaround (lerobot-software) — root cause never resolved either way; a human should pick the scope on review.
- #3211 (Missing motor IDs / duplicate ID collision): tagged setup-motors; maintainer's confirmation (comment 5) references the OP's fix but the confirming comment itself is not from the OP, so fix_confirmed_by:maintainer is inferred, not directly quoted from the OP.
- #4282 (LeKiwi sync_read no retry): marked not-so101 — it explicitly says the same defect class was "the same problem #4207 fixed for the SO follower and leader" i.e. this specific thread is about LeKiwi code, but the underlying STS3215/Feetech sync_read retry-budget issue is shared with SO-101; worth a second look if the corpus needs that already-fixed SO-101 case (#4207 isn't in this slice).
- #3098 (libero/smolvla low-VRAM eval): no SO-101 or SO-100 mention anywhere in body_head/comment_heads; marked not-so101 on that basis alone, could be wrong if the full thread names the robot.
- Threads with 0 comments and no confirmable outcome (#3441, #3585, #3640-#3645 range, #3758, #3861 sibling, #3942, #4442, #4483, #4496, #4550, #3536... ) got fix_confirmed_by:none by default since nothing in the head confirms a fix; several are still "open" state so this is consistent with GitHub's own state.
- i18n/translation-coordination threads (#3058, #3290) and roadmap/call-for-contribution threads (#3076, #3134, #3143, #3146, #3191, #3368/#4368, #3794 is a feature not a coordination thread — kept as feature) were not opened in full; classification rests on body_head + comment_heads only, per the "open full thread only when head insufficient" rule — none of these needed the full thread since their nature was clear from the head.
- Did not fetch full thread files for any row; every classification above rests on body_head/comment_heads, which were sufficient to decide relevance and fix status in all 66 cases. If a reviewer wants comment-position-level verification for a promoted candidate (exact quoted sentence supporting cause/fix), the full thread JSON at the given file path should be opened before writing the final troubleshooting entry — this inventory pass does not do that.
- Tier-1/tier-2 git queries: ran `git log --oneline -S"troubleshooting-candidates"` and a trailer grep on docs/tickets/troubleshooting-corpus-mining.md from the worktree; both came back empty (the ticket file is new, no history to quote).
- Wiki-forum slice. No GitHub threads assigned. Sources: (a) Waveshare wiki page oldid=109741 (216 lines, read in full — only two Note: callouts exist, both included); (b) Seeed so100m-new.md (3027 lines, scanned by grep for warning/note/caution/error/troubleshoot/fail/issue markers then read in full around every hit, including the complete FAQ section lines 2735-2973); (c) hf-forum-topics.tsv (20 rows, all read) cross-checked against hf-forum-so-101.json / hf-forum-so101.json topic lists — 10 topics were SO-101-relevant (fetched full thread JSON via curl -A 'Mozilla/5.0' from discuss.huggingface.co/t/<slug>/<id>.json); the other 10 tsv rows (Gemma safetensors, Deepseek, COVID TPU, LAION dataset script, generic Transformers/logits threads) are generic HF/ML topics with no SO-101 mention and were excluded, not rowed.

Git tier-1/tier-2 (run from .claude/worktrees/troubleshooting-candidates per instructions): `git log --oneline -S"troubleshooting.yaml" -- data/troubleshooting.yaml` → only 86a2a8f (schema-introducing commit). `git log --format=full data/troubleshooting.yaml | grep -E '^Constraint:|^Directive:|^Rejected:'` → no output, no trailers exist on that file. `git log --oneline -S"waveshare"/-S"seeedstudio" -- SOURCES.md` → both point to b6441a1 (the commit that added the SOURCES.md rows for these wikis), no design-decision trailers found there either — nothing to quote verbatim.

This repo (Guillaume-Georges/so101-assembly-guide) has no issues, only PRs, as of 2026-09-09, per the task brief — confirmed no issue-sourced rows are included from it.

Dedup decisions: several body callouts in the Seeed wiki duplicate what the page's own FAQ section (lines 2735-2973) restates more precisely with a verbatim quoted error — e.g. the ffmpeg "No valid stream found" tip (line ~437-454/520-538), the chmod 666 port-permission tip (line ~892-895), the "delete calibration files" recalibration note (line 836), the "Magnitude 30841 exceeds 2047" note (line 906), the rerun `AttributeError: 'scalar'` note (line 1426-1436), and the pynput keyboard-shortcut note (line 1572-1576) all restate the FAQ entry. I rowed each of these ONCE, using the FAQ version (it carries the cleanest verbatim error string) and noted the earlier occurrence in the symptom field rather than creating a duplicate row — this is "merge as alias/symptom," not omission.

Two items were deliberately NOT rowed as troubleshooting: the GR00T/flash-attn PyTorch-version conflict (line ~2108) and the conda "Proxy issues" note (line ~2422) are generic ML-tooling/environment problems unrelated to the SO-101 arm itself (GR00T VLA training environment, general proxy config) — out of this guide's subject even under the broadest lerobot-software reading. Flagging rather than silently dropping: if Guillaume wants GR00T-adjacent SO-101 threads in scope, `groot-on-so101-failing` (row below) is the one genuinely SO-101-scoped GR00T problem and is included.

One row (`realsense-macos-error`, seeed FAQ area) has a source text that is visibly garbled — the raw seeed-so100m-new.md render interleaves `macOS`/`sudo` inline-code spans out of natural reading order ("...you could get this error: , this can be solved by running the same command with permissions. Note that using RealSense cameras in is unstable.macOSError finding RealSense cameras: failed to set power statesudomacOS."). I copied the error_string verbatim as it appears in the source per the citation rule, but flag that the true error is almost certainly "Error finding RealSense cameras: failed to set power state" fixed by re-running the same command with `sudo` on macOS — an extraction agent should re-derive this from the live wiki page (or its HTML) rather than trust the markdown-converted text.

Two HF forum threads got a nuanced fix_confirmed_by: `is-it-possible-to-get-usb-camera-working-with-so101-arm-through-wsl-ubuntu` (178337) — I set `reporter` because the OP (InukaSilva, post #9) says the MJPG+opencv-4.12 approach "seemed to work sort of" and a visually-correct image was saved, but this is a soft/partial confirmation, not a clean "it works now" — flagged for a human to downgrade to `proposed-only` if that hedge isn't enough. `lerobot-camera-backend-issues` (173200) — I set `proposed-only` because the pin-opencv-to-4.12.0.88 fix was confirmed working by a _different_ commenter (`lmbsh`) reproducing the same symptom, not by the thread's own OP (`CursedRock17`), and neither party is a maintainer (HF forum has no MEMBER/COLLABORATOR association scheme the way GitHub does — no forum poster here is a lerobot/SO-ARM100 maintainer, all are `admin:false, staff:false` regular users, so `fix_confirmed_by: maintainer` is never applicable to any HF-forum row in this slice).

`fix_comment_position` for wiki-derived rows (waveshare, seeed) is left null — these are prose sections within a single wiki page/revision, not a numbered comment thread, so "comment position" doesn't apply; the section anchor is carried in `file` instead. For forum rows it is the 1-based post_number of the post stating the fix (proposed or confirmed); for threads with no stated fix it is null.

`comment_count` for wiki rows is set to 0 (a wiki page has no comment thread); `created` for wiki rows has no per-item timestamp available (MediaWiki oldid revisions and the Docusaurus page have no visible per-section date) so it is recorded as the citation's `retrieved` date with that caveat spelled out inline, not fabricated as a real creation date. `state` for wiki rows is "published" (no open/closed concept applies).

20 possible rows sit in the "assembly/setup-motors/calibration" boundary because the Seeed wiki interleaves servo/port/calibration guidance across three headed sections — I tagged each to the section it physically sits under, not by guessing intent, and called out the couple of genuinely ambiguous ones (`port-connect-error`, `port-mapping-ttyACM`) inline.

Nothing here modifies data/troubleshooting.yaml, data/schema, or any build input — this is inventory only, returned as data per the task's non-negotiable #4.

Git trailers quoted by the clustering agent: Tier-1 on data/troubleshooting.yaml (run from /Users/macbook/Developer/so101-assembly-guide/.claude/worktrees/troubleshooting-candidates):
`git log --oneline -- data/troubleshooting.yaml` -> e19f06e "data: three assembly troubleshooting entries, ten kit vendors"; 86a2a8f "data(schema): step slug, troubleshooting.yaml, vendors.yaml per ADR-0002".
`git log --oneline -S"setup-motors-no-response"` / `-S"servo5-cable-torn"` / `-S"wrong-leader-gear-ratio"` -> e19f06e only, for all three ids. The file has never been edited since the three entries landed, so nothing in it is stale relative to a later change.

Tier-2 trailer grep, data/troubleshooting.yaml -- ONE trailer found, quoted verbatim:
Constraint: slug is frozen at first publish; changing one is a URL break, not a rename
(commit 86a2a8f, "data(schema): step slug, troubleshooting.yaml, vendors.yaml per ADR-0002")
Same commit's ## Decisions adds, verbatim: "Slug uniqueness is global across steps and troubleshooting entries, not per assembly, so a slug can never collide when pages are cross-linked."

Tier-2 trailer grep, data/assemblies (for the related_steps ids) -- trailers found, quoted verbatim:
Constraint: ids are stable and never reused (e973c5b)
Directive: add a field to the schema before the viewer reads it, never the reverse (e973c5b)
Directive: when Phase 2 confirms a part's role, clear unverified on its steps in the same commit as the geometry mapping (7603b4a)
Constraint: our own wording says motor; only verbatim quotes say servo (1020f73)
Directive: a leader step that overrides a field a plain sentence points at must re-read the sentence; the validator checks the numbers, the reviewer checks the words (1020f73)
Constraint: title_short must name the same parts and action as title; it is a rewording, never a new fact (0db37ec)
Constraint: provenance is never rendered inside the instruction panel (0db37ec)
Constraint: motor-tab-screw covers 24 positions per arm, 22 into servo tabs and 2 holder-to-base; D-001 stays open on the size (b029791)
Constraint: cable paths live in data (cables.yaml); renderers read them, never derive them (f1fbde3)
Constraint: derived cable entries are overwritten on regeneration; hand-authored ones (derived: false) win (f1fbde3)
Constraint: a page dataset row without a source fails validation like any other row (c6f3f02)

How these bind this output (two of them do, directly):

1. "slug is frozen at first publish" + "ids are stable and never reused" mean no cluster key I propose may equal setup-motors-no-response, servo5-cable-torn or wrong-leader-gear-ratio. None does -- verified. The four clusters carrying overlaps_existing_id are therefore MERGE proposals (new aliases/symptoms on the existing id), never new entries, and each says so in its note.
2. "our own wording says motor; only verbatim quotes say servo" conflicts with this job's non-negotiable 3 (titles are verbatim error strings). Verbatim upstream titles here say "servo" ("Cannot insert my servo motor to the base...", "Servo No.5 wire can tear during install", "Gripper servo load is very high"). The trailer's own carve-out covers this -- verbatim quotes keep "servo" -- so titles stay verbatim and any symptom/cause/fix sentence WE write must say "motor". Flagging it because an extraction agent unaware of the trailer will normalise the titles and break non-negotiable 3.
3. "a page dataset row without a source fails validation like any other row" is why every cluster here carries its thread URLs: no candidate can reach data/ without them.

## Counts by scope and confidence

| Scope            | confirmed | reported | open | total |
| ---------------- | --------- | -------- | ---- | ----- |
| lerobot-software | 36        | 22       | 20   | 78    |
| assembly         | 3         | 7        | 5    | 15    |
| calibration      | 5         | 5        | 3    | 13    |
| setup-motors     | 4         | 0        | 0    | 4     |
| firmware         | 0         | 1        | 0    | 1     |
| sourcing         | 0         | 1        | 0    | 1     |

Unverified (no source confirms the fix): 65 of 112.

## 20 most frequent symptoms

| #   | Symptom                                                                                                                                                               | Threads | Cluster                                     |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------- |
| 1   | Training, evaluation or inference fails because the camera/image feature key names in the robot config do not match the names in the dataset or the policy checkpoint | 11      | camera-feature-key-mismatch                 |
| 2   | A policy trains without error but on the real arm produces almost no useful motion, never closes the gripper, or fails every grasp                                    | 8       | policy-trained-but-robot-barely-moves       |
| 3   | Unclear which power supply to use: BOM/kit ships 5V but the Waveshare driver board is rated 9-12.6V and motors are 7.4V or 12V                                        | 7       | power-supply-5v-or-12v                      |
| 4   | lerobot-setup-motors or lerobot-calibrate reports a named sts3215 motor as not found / no motors detected, with COMM_RX_TIMEOUT on the bus                            | 7       | motor-not-found-sts3215                     |
| 5   | Under async inference the arm produces no actions at all, or executes only the first action chunk then stalls                                                         | 5       | async-inference-produces-no-actions         |
| 6   | Each camera streams fine alone but the second fails to connect, times out, or returns no data when both are configured                                                | 5       | second-camera-fails-when-both-configured    |
| 7   | A tutorial file or examples/ path referenced from the docs or from another page no longer exists after a docs reorganisation                                          | 5       | tutorial-docs-moved-404                     |
| 8   | An optional camera mount (wrist webcam, overhead, RealSense D435) does not fit or aligns the sensor wrong for the camera actually available                           | 4       | camera-mount-does-not-fit-camera            |
| 9   | The serial port cannot be opened even though the device node exists and the port was set correctly                                                                    | 4       | could-not-connect-on-port                   |
| 10  | Reading positions fails, or teleoperation drops after a few seconds, with a no-status-packet sync-read error                                                          | 4       | sync-read-present-position-no-status-packet |
| 11  | Calibration appears to complete but no calibration file is written, or the offsets do not reach the motors, so teleop keeps asking to recalibrate                     | 4       | calibration-not-persisted                   |
| 12  | Calibration crashes writing a homing offset that overflows the signed 12-bit register                                                                                 | 4       | magnitude-exceeds-2047                      |
| 13  | Training, visualisation or merging raises an index error on dataset frames, episodes or timestamps                                                                    | 4       | dataset-index-out-of-bounds                 |
| 14  | End-effector-space teleop or replay moves the arm much faster than the leader, oscillates, jumps, or has misaligned axes                                              | 4       | end-effector-teleop-erratic-motion          |
| 15  | Following the HIL-SERL docs for SO-101 hits missing methods, missing config attributes and unimplemented features                                                     | 4       | hil-serl-so101-unsupported                  |
| 16  | The shipped URDF lacks joint limits, has inverted joint axes, deprecated hardwareInterface tags and no decomposed collision meshes                                    | 4       | urdf-joint-limits-and-axes-wrong            |
| 17  | Printed part opening is too tight to accept the motor or the horn; assembly needs excessive force                                                                     | 3       | printed-part-too-tight-for-motor            |
| 18  | 3DPRINT.md lists STL folder paths and per-part names that no longer exist after a repo reorganisation                                                                 | 3       | which-stls-to-print-follower                |
| 19  | The wrist_roll motor stops responding, is invisible during calibration, or rotates uncontrollably                                                                     | 3       | wrist-roll-motor-not-responding             |
| 20  | A joint whose travel crosses the 0/4095 encoder seam records a corrupted range, so calibration silently produces bad limits                                           | 3       | encoder-wrap-corrupts-recorded-range        |

## Merges into existing entries

- **setup-motors-no-response** (+7 threads): aliases ["RuntimeError: Motor 'gripper' (model 'sts3215') was not found. Make sure it is connected.","Motor 'gripper' was not found, Make sure it is connected","RuntimeError: Motor 'shoulder_lift' (model 'sts3215') was not found. Make sure it is connected.","Error occurred during motor configuration: No motors detected. Please ensure you have one motor connected.","RuntimeError: FeetechMotorsBus motor check failed on port '/dev/ttyACM0': Missing motor IDs","Missing motor IDs","Cannot set up servo sts3215","COMM_RX_TIMEOUT (-6)","lerobot-setup-motors fails to detect any STS3215 servo motors"]; symptoms ["the board powers up (red LED on) and the port opens, but every motor returns COMM_RX_TIMEOUT (-6) at every baudrate and every ID 1-253","motors 6,5,4,3 are set, then one motor blinks red fast and returns \"[RxPacketError] Input voltage error!\" - caused by a 12 V supply on 7.4 V motors (reporter- and maintainer-confirmed on lerobot#2387)","lerobot-calibrate then reports 'Missing motor IDs' for exactly the motors that failed to be set","two motors share one ID, so one expected ID is missing and the whole bus is intermittent - maintainer-confirmed on lerobot#3211","the controller board reaches the computer through a USB hub and no motor answers until it is plugged in directly","a mains adapter overshooting its rating (16 V measured instead of 12 V) puts the servos into protection mode so they never answer","the Feetech/Waveshare jumpers are not on the USB channel (already covered by this entry's cause)"]. MERGE, not a new entry, and verified as such: the existing entry has no verbatim error string and this cluster supplies the one builders paste. Strings verified present: "RuntimeError: Motor 'gripper' (model 'sts3215') was not found. Make sure it is connected." in the #3394 title and body, the #1244 body and #2387 comment 5; the wiki's variant is "Motor 'gripper' was not found, Make sure it is connected"; all nine aliases are verbatim somewhere in the cited threads. WHAT THE EXISTING cause/fix PROSE DOES NOT REACH - the merge schema has no cause/fix fields, so these land as symptoms and a reviewer must extend the prose by hand: (1) DUPLICATE/MISCONFIGURED MOTOR IDs. "Had this exact error. Turned out to be a duplicate motor ID, not a dead motor or power issue. My base motor was set to ID 6 (same as the wrist), so ID 1 was empty and the two were colliding on the bus... To check, broadcast-ping the bus across all baudrates with FeetechMotorsBus.scan_port." - https://github.com/huggingface/lerobot/issues/3211#issuecomment-4582371577 (comment 3), thanked by another builder (comment 4: "spent hours debugging, until I encountered your comment !!") and confirmed by a maintainer: "This was traced to duplicate/misconfigured motor IDs; user confirmed isolating motors and rerunning setup fixed the issue." - https://github.com/huggingface/lerobot/issues/3211#issuecomment-5145497133 (comment 5, COLLABORATOR). (2) 12 V SUPPLY ON 7.4 V MOTORS, which produces '[RxPacketError] Input voltage error!' and then this same not-found message: reporter-confirmed at https://github.com/huggingface/lerobot/issues/2387#issuecomment-3536861545 (comment 1) and comment 2, maintainer-confirmed at comment 10. That cause has its own candidate entry in this batch (power-supply-5v-or-12v-input-voltage-error) - cross-link rather than duplicate. (3) A MAINS ADAPTER OVER-DELIVERING: "Measured the multimeter reading ony to realise my shitty adapter is overshooting voltage by 30% (it was showing 16V instead of 12V). This was leading to my servos going into protection mode and not turning on. Changed my adapter and everything was smooth again." - https://github.com/huggingface/lerobot/issues/3394#issuecomment-4591226117 (comment 2, a builder with the same symptom, not the thread author). Already covered by the existing cause/fix: jumpers on the B (USB) channel (#1110 comments 2-5, reporter-confirmed: "connecting jumpers did solve the problem :)") and unseated cables. FIRMWARE LEAD, recorded and not promoted (it is a different scope and nobody confirmed it): #1244 comment 1 says "Make sure feetech servos have updated version of firmware. you can download it from feetech site", and comment 6 answers that this is circular - "Since the motor cannot be recognized in the first place, firmware updates using FD.exe are not possible." Unsupported claim NOT promoted: "In my case. It was follower's board. not one with leader's." - lerobot#1244 comment 4 - which implies the two arms' boards are not interchangeable; data/parts.yaml carries a single motor-control-board id with qty 2, so this is logged in discrepancies instead. #3394 itself remains open and unexplained (all six motors mute at every baudrate); its only maintainer reply suggests testing with the Feetech debug tool. frequency 7 = 6 GitHub threads + 1 wiki page. Existing id and slug are frozen and untouched.
- **servo5-cable-torn** (+1 threads): aliases ["Wrist Roll motor not responding","wrist roll motor went partially stiff and it is not responding","motor 5 not responding in FT SCServo Debugger"]; symptoms ["After a working setup, calibration and teleoperation, the follower's wrist roll motor goes partially stiff and stops responding, also in the FT SCServo Debugger"]. MERGE ADDED BY THIS PASS; the extractor wrote "CROSS-LINK, deliberately not a merge" and left merges empty, which leaves the ticket's Acceptance line ("Duplicates of the existing 3 entries are merged as new aliases or symptoms on those ids, reported separately, not re-created") unsatisfied. lerobot#2693's searchable failure is the same as the existing entry's symptom "Motor 5 not answering after the wrist is assembled" on servo5-cable-torn, whose related_steps (F-050, L-050) and related_parts (wrist-roll-pitch-so101) the extractor's candidate was duplicating. Quote: "I connected to the lerobot so101 bot ->setup motors->callibrated->tested teleoperation ,everything wewnt fine .But after few hours when recallibration is done in some other system the wrist roll motor of the follower arm went partially stiff and it is not responding . [Used FT SCServo Debugger]" (https://github.com/huggingface/lerobot/issues/2693, issue body; zero comments, no maintainer reply). ADD ALIASES AND THE SYMPTOM ONLY - NO CAUSE CHANGE: I grepped all three wrist threads for cable, torn and pinch, and #2693 and #2924 contain none of them, so this thread does NOT support the existing entry's torn-cable cause; the merge is there so a builder pasting "Wrist Roll motor not responding" lands on the page that tells them to check the motor-5 cable, which is one thing to check, not the established cause. The timing also differs from the existing entry (failure after hours of working use, not at assembly) - a reviewer may prefer to add a hedge sentence to the existing symptom list rather than take the alias verbatim.
- **wrong-leader-gear-ratio** (+2 threads): aliases ["Disaster: Built following the instructions here. Only then started following Huggingface, which says I need to modify all leader servos.","Is it strictly necessary for me to modify all leader servos?","SO-100 components(without 3D Parts) can be used for SO-101?","how can I modify the gear ratio base on the so100 leader arm's servo?","Can I equip it with New SO-101 3D printed parts?"]; symptoms ["The arm was built from the SO-ARM100 instructions and videos first, and only afterwards did the LeRobot page say all leader servos need modifying","Leader arm is harder to move because of friction, so it cannot be teleoperated with one hand or for long","An SO-100 motor kit bought without printed parts is being fitted under SO-101 printed parts, and only the leader motors are geared differently","Twelve identical motors on hand, with no replacement gears for the leader joints"]. VERIFIED as a merge, not a new entry: the existing id and slug are frozen by the trailer on commit 86a2a8f, quoted verbatim from the tier-2 grep — "Constraint: slug is frozen at first publish; changing one is a URL break, not a rename". Two new entry points for the same problem, both quotes re-read in the files. Maintainer nuance the existing `fix` sentence does not yet carry — Cadene (COLLABORATOR), SO-ARM100#31 comment 1, answering "Is it strictly necessary for me to modify all leader servos?": "No it is not, but the leader arm will be harder to move due to friction. You wont be able to teleoperate it with one hand and for a long time. We are working on an updated version of the motors to not have to remove the gears." (https://github.com/TheRobotStudio/SO-ARM100/issues/31#issuecomment-2615120271, comment 1) — i.e. a wrongly geared leader joint is a usability penalty, not a hard stop, which a reviewer may want beside "swap the servo, then re-run motor setup". Second entry point — pkooij (COLLABORATOR), SO-ARM100#71 comment 1: "Yes you can! From the motors perspective only the leader arm motors for SO-101 are differently geared" (comment 1), with the remedy at comment 3 by matbeedotcom (NONE): "you'd need the replacement gears- or purchase the servo kit from alibaba" (comment 3). Correction to the extraction's note: the line "the leader arm is always 7.4v for the SO101" appears at #71 comment 4 only as a blockquote by mazytomo (NONE) with no in-thread origin, so it is corroborating context, not a maintainer statement; it still matches every leader row's note in data/servos.yaml, and nothing in either thread contradicts 1:191 (joints 1, 3), 1:345 (joint 2) or 1:147 (joints 4-6). Open in the public record: #71 comment 6 (dannyyy) asks what happens if the same gearing is used for all six leader motors and whether a servo register or calibration change is needed — unanswered, so no fix sentence may claim it works. The alias taken from #31's title normalises the double space after "here." to one; no other wording changed.
- **servo5-cable-torn** (+1 threads): aliases ["Be careful when inserting the No. 5 servo, otherwise it is easy to tear the wire","servo No.5 wire can tear during install","motor 5 wire torn in the wrist bracket","can't get the motor 5 cable in after the servo is fitted"]; symptoms ["Inserting servo 5 before the wires are routed tears the wire","With servo 5 already in the wrist bracket F8 there is too little space left to wire it"]. MERGE ONLY, no new entry, no id/slug change: the tier-2 trailer grep in this pass reproduced commit 86a2a8f's trailer verbatim, "Constraint: slug is frozen at first publish; changing one is a URL break, not a rename", so servo5-cable-torn keeps its id and its slug wrist-roll-servo-cable-torn. Support, quoted verbatim from corpus/wiki/waveshare-assembly.txt line 151, the note following step 14 of section "Follower Arm Assembly": "Note: Be careful when inserting the No. 5 servo, otherwise it is easy to tear the wire. If the servo is installed here before wiring, it will be difficult to install due to the limited space." The fix already on the entry is supported by step 14 itself (line 149): "Adjust the position of the 5th servo cable, then insert the servo into the wrist bracket F8. The side of the servo with the gear should be inserted first, with the gear facing up." SYMPTOM INVERSION CORRECTED IN VERIFICATION — the one substantive defect in this merge: the extraction read the wiki's second sentence as "servo 5 is difficult to fit into the wrist bracket F8 at all ... once the servo is wired", i.e. the SERVO being hard to fit. The wiki says the opposite — install the servo before wiring and it is the WIRING that becomes difficult in the remaining space — which is exactly what the existing entry's own cause already says ("there is little room to wire it afterwards"). Symptom 2 is rewritten to the wiring reading, and the extraction's alias "servo 5 won't fit in the wrist bracket" is DROPPED because it was built on the inversion and would pull the wrong searches onto this entry. WHAT IS ACTUALLY NEW: (a) the existing entry has no `aliases` key at all, so these four are the first ones — and its title is a paraphrase ("Wrist roll (motor 5) cable damaged during insertion") rather than anything a builder would type, which makes aliases load-bearing for search (aliases 1 is verbatim wiki text; 2-4 are editorial search phrasings); (b) one reframed symptom, the wiring difficulty, which the existing symptom list (cable pinched/torn, motor 5 not answering) does not carry. NO NEW SOURCE: the ref above is byte-identical to the one already on the entry (same oldid=109741), only the retrieved date differs (2026-09-07 there, 2026-09-09 here) — bump that date rather than add a second row, since two rows with one ref would claim two sources where there is one. The entry's related_steps F-050 and L-050 are confirmed by this read: wiki line 171, section "Leader Arm Assembly", verbatim: "The assembly steps 1~15 for the Leader leader arm are the same as those for the Follower follower arm and will not be repeated; they differ starting from step 16." — so the step-14 warning applies to the leader too, and data/assemblies/follower.yaml F-050 already carries the warning "Route the cables before inserting motor 5: \"otherwise it is easy to tear the wire\" (Waveshare)."
- **setup-motors-no-response** (+1 threads): aliases ["the power supply cable might disconnect as you manipulate the board"]; symptoms ["The power supply cable disconnects as the controller board is manipulated between the per-motor steps of motor-ID setup"]. MERGE, no new id. VERIFIED against corpus/wiki/seeed-so100m-new.md line 736, and the context claim checks out: it is a `:::tip` immediately after the per-motor instruction "Connect the controller board to the 'wrist_roll' motor only and press enter." and the `:::caution` "Repeat the operation for each motor as instructed." Supporting text, verbatim: "Check your cabling at each step before pressing Enter. For instance, the power supply cable might disconnect as you manipulate the board." (https://wiki.seeedstudio.com/lerobot_so100m_new/, retrieved 2026-09-09). TWO VERIFIER FIXES APPLIED: (1) the proposed symptom ended "…so the next motor does not answer when Enter is pressed" — the wiki does not say that; the consequence is inferred from the context, so the symptom is now only what the source states; (2) the alias "motor stops answering after I moved the board" was invented phrasing appearing in no source (and no builder appears in this record at all), so it is dropped and replaced by a verbatim fragment of the wiki sentence. The existing entry's cause already names an unseated power supply — "A cable is not seated: power supply, USB to the board, or the 3-pin cable board-to-motor" — so this adds only the WHEN (between per-motor steps, because the board is being handled) plus the wiki as a second source alongside the existing upstream so101.mdx ref; nothing in the existing cause or fix needs changing. The same wiki paragraph also restates the existing fix's one-motor-at-a-time rule ("make sure it is the only motor connected to the board and that the motor itself isn't connected to any other one"), which is corroboration, not a new fact. Wiki URL cited exactly as SOURCES.md row 24 gives it, no fragment — `#cabling-check-each-step` in the inventory is an inventory label, not a page anchor (confirmed: no such anchor in the file). Note that the existing entry's single source has no `retrieved` date (it is an upstream snapshot path); this new row has one, as required.

## Discrepancies with data/

- power-supply-5v-or-12v-input-voltage-error vs `data/parts.yaml: power-supply-5v and motor-control-board; data/discrepancies.md D-002`: Three SO-ARM100 threads state that the Waveshare Bus Servo Adapter (A) (v1.1) is printed DC 9-12.6 V input and that the board documentation says the input voltage should match the servo voltage, which contradicts the 5 V supply our BOM carries for the 7.4 V servos: "The Waveshare board label says 9-12.6 V input only. The kit includes one 5 V adapter, which is below that range and should not be used for these boards" (#151 body); "has a listed input of 9-12.6V but the listed power supply is 5V" (#142 body); "The Waveshare documentation specifies an input voltage of 9-12.6 V. It also states that the input voltage should match the voltage of the connected servos." (#174 body). All three quotes re-verified in the issue bodies. D-002 records the 5 V/12 V disagreement between README, SVRC and Seeed but not the board's own printed rating, and motor-control-board's notes carry no voltage rating at all. Nobody in the corpus resolves it; lerobot#2387 (maintainer-confirmed) shows 12 V on 7.4 V motors failing in practice, while SO-ARM100#151 comment 1 reports the vendor shipping a second 5 V 6 A supply after its own testing - i.e. the two public data points point opposite ways from the printed rating. (https://github.com/TheRobotStudio/SO-ARM100/issues/151)
- setup-motors-no-response vs `data/parts.yaml: motor-control-board (single id, qty 2, one per arm)`: A builder reports that the follower and leader control boards are not interchangeable - "In my case. It was follower's board. not one with leader's." followed by a blockquote "Make it sure it's right one's. / Follower - Follower Servo / Leader - Leader Servo" - which our BOM's single motor-control-board id with qty 2 does not express. Verified at comment 4 of the thread. Single unverified community claim (author NONE, no maintainer reply, thread closed as stale by the bot); recorded for a reviewer, not acted on. (https://github.com/huggingface/lerobot/issues/1244#issuecomment-3359241601)
- magnitude-exceeds-2047 vs `data/servos.yaml: follower-wrist-roll and leader-wrist-roll (no range or continuous-rotation property); data/parts.yaml: wrist-roll-pitch-so101 and the Waveshare/Seeed printed-part variants`: LeRobot's calibration treats wrist_roll as a full-turn joint with no physical limits (range hardcoded 0-4095 and excluded from range recording - lerobot#3193 body and comment 1), but two builders with Seeed-printed parts report hard stops on that joint: "my SO-101 actually _does_ have physical stops on the wrist roll joint! And the teleop, too ... I got my printed parts from seeed studio" (comment 4) and "I just got a SO-101 Pro Kit from Seeed with the hard stops on the wrist roll joint" (comment 5). data/servos.yaml records no range or continuous-rotation property for wrist_roll, so nothing in data/ is wrong today - but the Seeed printed-part variant differs physically from what upstream software assumes, and the moment joint ranges or a wrist_roll 'continuous' flag are added, this variant must be noted (CLAUDE.md: prefer the upstream TheRobotStudio spec, noting the variant). (https://github.com/huggingface/lerobot/issues/3193)
- wrist-roll-missing-from-calibration vs `data/parts.yaml power-supply-5v notes ("Leader is always 7.4V") and servo-sts3215-12v-345 notes ("Never for the leader")`: The reporter of lerobot#2924 states "The Voltage that I used for my model is 5V for follower_arm and 12v for leader_arm" - a 12V leader, which our data says never applies (12V is an optional follower-only servo variant). The thread is one builder's setup, not a spec claim, so our data is not wrong; but a 12V supply on a leader arm is a plausible contributing factor to that thread's motor symptoms and nobody in the thread picks it up. Re-pointed from the rejected wrist-roll-motor-not-responding entry to the entry that now carries #2924. (https://github.com/huggingface/lerobot/issues/2924)
- printed-part-too-tight-for-motor vs `data/printing.yaml print-material (value "PLA+", sourced to upstream/SO-ARM100/README.md#step-1-choose-a-printer)`: The reporter of SO-ARM100#63 (2025-04-19) quotes the same README section as recommending "Material: PLA" with a 0.4 mm nozzle at 0.2 mm layer height. Nozzle and layer values match our data; the material does not (PLA vs PLA+), which suggests the upstream README wording changed between that thread and our 2026 snapshot. Worth a line in data/discrepancies.md if the guide ever cites the older wording. (https://github.com/TheRobotStudio/SO-ARM100/issues/63)
- printed-part-too-tight-for-motor vs `nothing in data/ to contradict today - parts.yaml carries no servo-pocket dimensions for base-so101`: SO-ARM100#63 comment 4 measures the base model's servo opening at 24.70 x 32.90 mm against an STS3215 datasheet figure of 24.72 x 35 mm, i.e. claims the printed model is about 2 mm short in height, while comment 5 (the print file's author, a repo CONTRIBUTOR) reprints the same file and both Waveshare and Feetech servos fit. Recorded because if the pipeline ever publishes a servo-pocket dimension from the STEP it must be checked against both claims. (https://github.com/TheRobotStudio/SO-ARM100/issues/63#issuecomment-2817001869)
- opencv-camera-backend-selection-fails vs `no data file - SOURCES.md and the corpus inventory`: Two of this entry's three cited threads are Hugging Face Discourse topics (discuss.huggingface.co), a source class SOURCES.md does not yet list. They are public, login-free pages and the ticket's Scope names the Hugging Face forum as in-scope, but if these entries are promoted, SOURCES.md needs a row for the forum alongside the Waveshare and Seeed wikis. (https://discuss.huggingface.co/t/lerobot-camera-backend-issues/173200)
- leader-handle-attaches-to-holder-not-motor5 vs `data/assemblies/leader.yaml L-061 (title "Attach the handle to the leader holder", check "Handle held by 1 M2x6, driven into the tab under the holder.", source[0] note 'Leader: "Attach the handle to the leader holder using 1 M2x6mm screw."')`: NARROWED BY CHECK, not a data defect. The rendered LeRobot docs page at v0.6.0 said "Attach the handle to motor 5 using 1 M2x6mm screw", but our pinned snapshot upstream/lerobot/docs/source/so101.mdx:341 (pinned at lerobot 3f2c29e, commit a19a720) already reads "- Attach the handle to the leader holder using 1 M2x6mm screw.", so L-061's source note quotes the snapshot accurately and data/ needs no change. What is worth a row in data/discrepancies.md is that a published upstream doc version disagreed with our step and was corrected (reported at v0.6.1), because builders following an older docs version will search the wrong string — which is exactly what this candidate's title is for. (https://github.com/TheRobotStudio/SO-ARM100/issues/169)
- servo-rotated-before-assembly-breaks-range vs `data/parts.yaml wrist-roll-follower-so101 (name "Follower wrist roll (gripper body)") and wrist-roll-so101 (name "Leader wrist roll (leader holder)")`: SO-ARM100#170 comment 1 links STL/SO101/Individual/Wrist_Roll_Follower_SO101.stl and calls it "the leader wrist", the opposite of our assignment, in the same EDIT note where it corrects itself about the SO-100. The author's association is NONE and our mapping follows the upstream file names, so no data change is proposed — but this is precisely the confusion a builder carries into a search, so the reviewer may want the part pages to name both STL filenames explicitly. (https://github.com/TheRobotStudio/SO-ARM100/issues/170#issuecomment-5304042515)
- servo5-cable-torn vs `data/troubleshooting.yaml servo5-cable-torn, symptoms: "Motor 5 not answering after the wrist is assembled" (EXISTING PUBLISHED entry, not a candidate in this batch)`: The Seeed wiki calibration section (corpus/wiki/seeed-so100m-new.md line 878) states: "Due to the update of the lerobot repository, it is normal that the terminal does not receive a signal from servo 5 when performing master-slave arm calibration. You can continue with the operation." A builder with an intact cable who sees no motor-5 signal during calibration would be told by our published entry that the cable is pinched or torn. The existing symptom needs narrowing to the assembly-time case, or the wiki statement needs recording beside it. (https://wiki.seeedstudio.com/lerobot_so100m_new/)
- daisy-chain-power-brownout vs `data/parts.yaml — power-supply-5v (line 525, "Power supply, 5V (for 7.4V servos)", qty: 2) and servo-sts3215-12v-345 (line 413, "optional follower variant", qty: 0)`: The thread treats a 12 V / 5 A PSU with 12 V ST3215 servos as "the current guidance" for the arm, but data/parts.yaml carries only a 5 V supply (for the 7.4 V servos) and no 12 V supply part at all, even though it does carry the 12 V servo as an optional variant with qty 0. If the 12 V build is a supported path, the BOM is missing its power supply; if it is not, this candidate is about a configuration the guide does not describe and the reviewer should say so on the page. Nothing in data/ is factually contradicted — it is a gap, not a conflict. VERIFIER: both line numbers re-checked in the worktree with grep -n and both are exact. (https://github.com/TheRobotStudio/SO-ARM100/issues/132)
- end-effector-teleop-stopiteration vs `data/parts.yaml:413 servo-sts3215-12v-345 — qty: 0, "optional follower variant"`: The #1433 reporter describes "a newly built so101 follower with the 12V motors" as an ordinary build, and SO-ARM100#132 assumes the same. Two independent builders in this batch are on the 12 V variant that data/parts.yaml records with qty 0 as optional — worth checking whether "optional variant" understates how common it is before the vendor/kit pages are written. VERIFIER: the two reporters are indeed different people (jtabor on lerobot#1433, rabhishek100 on SO-ARM100#132), so "two independent builders" is accurate; the quoted phrase "a newly built so101 follower with the 12V motors" is verbatim in #1433's issue body. (https://github.com/huggingface/lerobot/issues/1433)
- print-infill-13-or-30-percent vs `data/printing.yaml:10-13 (id print-infill, "setting: Infill density", "value: 15%", source upstream/SO-ARM100/README.md#step-1-choose-a-printer — re-verified in this pass); data/compare.yaml:60-71 (id cmp-print, "so100: PLA, 13% infill." at line 62, "so101: PLA+, 15% infill; same nozzle/layer options." at line 63 — the extraction's 62-71 corrected to the block at 60-71)`: Issue 58 (2025-04-11) reports that the .3mf profile shipped for the Bambu X series is saved at 30% infill, and a COLLABORATOR answered that the low README figure is correct "although 15% and even 20% is okey". Our data records only the README figures (15% for SO-101, 13% for SO-100) and nowhere records the 30% slicer profile, so a builder who opens the shipped .3mf sees a value twice our published one with no explanation. Issue 24 comment 2 shows the same confusion in the field (a builder printed at 24% and planned to reprint at 13%). Two follow-ups: add a discrepancies.md row for README-vs-.3mf infill, and consider recording the maintainer's 13-20% band, since data/printing.yaml publishes a single point value that the upstream maintainer treats as a range. (https://github.com/TheRobotStudio/SO-ARM100/issues/58#issuecomment-2820319903)
- wire-groove-optional-on-motor-brackets vs `data/compare.yaml:25-35 (id cmp-wiring: "so100: Brackets without wire grooves." at line 27, the so101 "improved wiring" line at 28, already citing the same Waveshare item 3) and data/glossary.yaml:197-207 (id wire-slot, kind: fact, same wiki source) — both anchors re-verified in this pass, correcting the extraction's 27-28 and 198-207`: Not a contradiction but a duplication the reviewer should settle before this candidate is promoted: the fact this entry would publish (SO-ARM101 brackets have wire grooves, SO-ARM100's do not, and the groove is cosmetic) is already recorded in two places the site renders, and no builder is on record as blocked by it. Either promote it as a /faq or /compare row, or drop the candidate and add the "it can be unused" clause to the existing glossary fact; publishing it a third time as a troubleshooting page would be three renderings of one sourced fact. (https://www.waveshare.com/w/index.php?title=SO-ARM100/101_Kit_Aassembly&oldid=109741)
- no-signal-from-servo5-during-calibration vs `data/troubleshooting.yaml — published entry servo5-cable-torn (slug wrist-roll-servo-cable-torn), symptom "Motor 5 not answering after the wrist is assembled"`: Direct conflict with a published entry. The Seeed wiki states that receiving no signal from servo 5 during master-slave calibration is normal and the builder should continue; our published servo5-cable-torn entry treats motor 5 not answering as a real fault (3-pin cable pinched or torn at the wrist, sourced from Waveshare follower step 14). Both cannot be the first thing a builder reads. The wiki never names the lerobot change responsible, so the benign claim cannot be checked against an upstream commit or issue, and publishing 'this is normal' risks masking a torn cable. Resolution required before either page ships: decide whether to publish the benign case at all and, if so, how the page distinguishes the two and cross-links servo5-cable-torn. (https://wiki.seeedstudio.com/lerobot_so100m_new/)

## Needs a human (sources conflict or Discord-only)

- **camera-feature-key-mismatch-mean-is-infinity**: Sources give two different causes for the same verbatim title string: a reporter and the Seeed Studio wiki say mismatched camera keys (fix: match keys / --rename_map), while a MEMBER on #3645 says the string came from in-weights normalization layers removed in v0.4.0 and 'can no longer occur on current LeRobot'. Decide whether this is one entry with both causes or two, and whether the title should instead be one of the newer feature-mismatch strings. Note also that three cited threads (#3181, #3425, #4245) are open upstream bugs where the prescribed --rename_map is silently ignored, so the fix does not hold on those versions; the only Discord-sourced lead (#1620 comment 1) is not citable.
- **policy-trained-but-robot-barely-moves**: Eight threads, no confirmed cause and no fix that holds: the dataset-quality guidance (from a lerobot CONTRIBUTOR, so a maintainer under this run's rule) is contradicted inside the same thread by reporters at 77 and 220 episodes, and the one reporter-confirmed remedy (40 -> 80 episodes) is a single data point. This may belong in an FAQ rather than as a troubleshooting page. The HF-forum thread's only reply is a community helper who routes the reporter to Discord - not citable as authority.
- **power-supply-5v-or-12v-input-voltage-error**: Cause and fix are confirmed, but the question five of the seven rows actually ask is unanswered in the public record: may the Waveshare Bus Servo Adapter (A), printed DC 9-12.6 V, be fed the 5 V supply that the 7.4 V motors need, or does it need a buck converter between supply and board? I retagged the scope to setup-motors (the titled error comes from lerobot-setup-motors and the related_steps are F-002/L-002) - confirm that call, and decide whether the kit-level mismatch (#151) belongs here or in a sourcing entry.
- **setup-motors-no-response**: Merge target: the existing cause/fix prose covers unseated cables and Waveshare jumpers only. It does not reach two causes this cluster confirms (duplicate/misconfigured motor IDs, maintainer-confirmed on #3211; a 12 V supply on 7.4 V motors, maintainer-confirmed on #2387) or the over-delivering mains adapter, and the merge schema has no cause/fix fields - so those sentences must be written into the existing entry by hand. #3394, where all six motors stay mute at every baudrate, remains unexplained and may need its own entry; the #1244 firmware lead is unconfirmed and circular.
- **async-inference-produces-no-actions**: After narrowing to the two threads that are genuinely open, this entry is 2 threads saying 'known broken' with a maintainer stating the async stack needs a refactor. Decide whether that is worth publishing at all. The two resolved sub-cases I removed should probably become their own entries if async is published: #2120 (regression, fixed by PR #2132, MEMBER-confirmed) and #1500 ('Observation #0 has been filtered out', explained by a CONTRIBUTOR as the must_go/timestep filter and reporter-confirmed).
- **second-camera-fails-when-both-configured**: The USB-bandwidth cause is wiki-stated and reporter-confirmed, but it explains only one of the five rows: #4550's reporter rules bandwidth out with dmesg evidence and a certified USB3 cable and is unresolved with zero comments, #1743's only maintainer reply says the attached error is unrelated to the cameras, and #1353 carries no error text. I moved the title onto the confirming thread (#1179's TimeoutError) to stop the entry promising #1743's builder a fix a maintainer disowned - confirm that, and decide whether the RealSense-plus-OpenCV case needs a second, open entry.
- **tutorial-docs-moved-404**: Fix is maintainer-confirmed five times, but the entry rots on every upstream docs reshuffle: decide whether the guide should mirror upstream doc paths at all or only link the docs root, and whether '404 Not Found' is an acceptable title - it is rule-compliant (verbatim issue title and body text) but nobody searches it; the real search terms are the old filenames, which are in the aliases.
- **camera-mount-does-not-fit-camera**: No applicable fix exists in public: the maintainer's promised fix on #55 is not confirmed landed (issue still open) and the one builder who corrected the RealSense mount cannot share the design for employment reasons. I dropped PR #62 because only its title was retrieved - if a reviewer wants it as evidence that upstream does fix mounts, it must be fetched and read first. Decide whether to publish with the workaround-only framing, and whether optional camera mounts are in scope at all given they are absent from data/parts.yaml.
- **could-not-connect-on-port**: Three distinct causes under one string: missing port permissions (Seeed Studio vendor wiki ONLY - no confirmation from anyone on either upstream repo, so treat the chmod advice as vendor guidance, not a confirmed fix), an edited port in configs.py ignored by a non-editable install (maintainer-endorsed), and a WSL2 case that is unresolved; SO-ARM100#150's termios.error (22) has zero comments. Decide whether WSL2 gets its own entry - pairing it with the permissions fix would mislead.
- **sync-read-present-position-no-status-packet**: I retagged scope to lerobot-software to match the entry's own reason for staying separate from setup-motors-no-response (a teleop/record dropout after setup succeeded); confirm that, and confirm the non-merge itself. The cause also does not cover every thread: #89's reporter never replied, so the walk-the-LEDs advice is unconfirmed, and on #3131 the reporter replaced cables, board and PSU and a COLLABORATOR conceded they were 'as lost as you are' - so the page must not promise that re-seating a cable fixes it.
- **calibration-not-persisted**: No confirmed cause or fix: #1342 got eight comments and no maintainer reply, #1328's reporter closed without stating a method (the cluster note wrongly listed it as reporter-confirmed), #1668 is non-reproducible. The only wiki remedy (delete stale calibration JSON) answers a different question - recalibrating after a repair or part swap. Decide whether to publish the symptom with no fix, and whether calibration is meant to write to the motor registers at all; also note the reporter's hardware is a 6.4V leader and 12V follower, which is why no part id is referenced.
- **magnitude-exceeds-2047**: The fix is confirmed, but #3193's second problem is not: leader and follower wrist_roll zero points not corresponding, which a MEMBER says PR #3373 fixed and which depends on the joint's position when calibration starts, while a later reporter says neither helped and only the Seeed RoboController middle-position tool did. That likely deserves its own entry. Also decide whether the entry should list the power-cycle remedy, which worked for three builders and explicitly did not for a fourth, and see the new wrist_roll hard-stop discrepancy against data/servos.yaml.
- **dataset-index-out-of-bounds**: Four different verbatim strings grouped on one symptom: the reinstall-from-source fix is reporter- and maintainer-confirmed for #2854 and #2680 but one builder on #2680 still reports the failure after the fix landed, #1895's unremapped --dataset.episodes indices have no fix at all, and on #1505 the Seeed Studio wiki (datasets==2.19) and a community comment (datasets==3.6.0) disagree. Decide whether this splits into a train/merge entry and a dataset-viz entry.
- **printed-part-too-tight-for-motor**: Sources conflict on the cause: a reporter's measurement says the model opening is about 2 mm short, the author of the print file (a repo CONTRIBUTOR) reprinted it and both servo brands fit, and other builders blame printer and filament dimensional error. No maintainer has accepted a model defect, so the cause sentence states the disagreement rather than picking a side. A human must decide whether the guide says "tune your printer" or "the model is tight" - this is the one assembly-scope page in the batch a builder will actually hit.
- **wrist-roll-missing-from-calibration**: This and wrist-roll-unstable-during-teleop are what remains of the rejected three-thread wrist-roll entry: two single-thread pages, no cause, no fix, confidence open. A human should decide whether either is worth publishing, and whether the alias set on the merge into servo5-cable-torn is too strong given that nothing in lerobot#2693 supports that entry's torn-cable cause.
- **hil-serl-so101-leader-unsupported**: The record is contradictory about what is supported: a contributor's analysis of current main says the leader path cannot work and the hilserl docs' control_mode is never read, while a maintainer closed the neighbouring urdf_path thread as "supported". Two threads also push the answer off-platform (a WeChat number in #1819 comment 5, a WeChat handle in #2431 comment 6), so whatever was settled there is not citable. A human should decide what the page claims is supported today, and whether a v1 site should carry an HIL-SERL page at all.
- **record-video-muxing-dts**: Narrowing leaves the --resume=true time_base error (lerobot#2049) with only an in-issue patch and a contributor saying the thread should not have been closed. A human should decide whether that becomes its own entry at confidence open, or stays an alias on this page.
- **which-stls-to-print-follower**: Same unfetched-PR problem as the URDF entry, but on the assembly pages v1 actually ships: this entry's fix status rests on PR #167 and duplicate-part-in-bambu-print-file's on PR #48, both known only as number, title and closed state, and closed is not merged. Before either page goes live a human (or a follow-up fetch) must confirm whether 3DPRINT.md and Print_Leader_Bambu_P1P.3mf are fixed upstream today; if they are, both pages become historical notes for builders using older files and should say so in the fix sentence.
- **urdf-joint-limits-and-axes-wrong**: Fix status rests on two PRs whose threads were not fetched (number, title and closed state only), and closed is not merged. It is also the weakest v1 relevance in the batch (simulation only, nothing touching the physical build), so a human should decide whether to keep it at all before anyone spends time verifying merge state.
- **rerun-has-no-attribute-scalar**: The only source is a vendor wiki, not a thread: nobody in the corpus reports this error on an SO-101 build and no maintainer confirms the downgrade. A human should decide whether a vendor-wiki-only fix meets the bar for a published page, and whether pinning rerun-sdk==0.23 is still correct advice at the date of publication.
- **calibration-range-outside-expected-bounds**: Four competing causes in one thread, none excluded by the others: reversed motor IDs in the robot config (#607 comment 1), a motor physically positioned wrong (#607 comment 2, the reporter's own confirmed fix), gripper start_pos == end_pos from not reaching both limits (#607 comments 4, 6 and 11), a wider mechanical grip range than the software limits (#60 comments 1 and 3, the reporter's own confirmed fix), plus a USB-A 2.0 cable (#607 comment 10). Verification downgraded the entry to reported/unverified because the cause it adopts and the two fixes reporters confirmed do not match each other. A human must decide whether this is one entry or three (start/end range, motor orientation, software limits), and whether the pre-0.5 control_robot.py error strings are still worth publishing verbatim.
- **drive-mode-reversed-after-calibration**: The only proposed fix (edit drive_mode in both .cache/calibration json files so they match, #1130 comment 2) is explicitly blocked on the version #3942 reports: "Since lerobot checks the JSON signature, I cannot manually edit the configuration files". #1130 was auto-closed as stale and #3942 has zero comments, so the record contains a fix a current builder cannot apply and no maintainer statement either way. Verification also found the two threads do not share a mechanism (#3942 has drive_mode 0 on both arms), so a human should decide whether they are one entry at all.
- **async-and-local-inference-differ**: Neither thread has a maintainer reply, both are by the same reporter, and the code under discussion is his own robot_client_ee.py rather than shipped LeRobot. The only confirmation is his own "add a sleep after send_action" workaround, whose mechanism he says he does not understand, and he states he recorded further information in Discord — not citable under CLAUDE.md rule 3, so the decisive evidence may exist only behind a login. A human should decide whether a custom-script thread belongs in a kit builder's troubleshooting corpus at all.
- **dagger-snap-back-on-resume**: Verification downgraded this to reported/unverified because the defect the title names (RTC stale observation on resume, lerobot#3747) is still open with its fix only in the unmerged PR #4398, while the cluster's other thread (#3723, pause handover) has a landed, reporter-validated fix in #3506. One entry cannot honestly carry both confidences. A human should decide whether to split it into a confirmed pause-handover entry and a reported resume entry — the split is the reason the confidence field reads reported rather than confirmed.
- **dataset-version-conversion-fails**: Verification dropped lerobot#2805 from this entry (different converter, different error, zero comments, a LeIsaac/so100 dataset with no SO-101 involvement), which took frequency from 2 to 1. The thread is a real unanswered report — "UnboundLocalError: local variable 'robot_config' referenced before assignment" then "Robot type 'so100' is not available." on python -m lerobot.datasets.v2.convert_dataset_v1_to_v2 — so a human should decide whether it becomes its own entry with confidence open and the fix sentence "No confirmed fix in the public record as of 2026-09-09.", or is dropped as out of scope. URL: https://github.com/huggingface/lerobot/issues/2805
- **datasets-4-0-feature-type-list**: The sources disagree on the version to pin: datasets 3.6.0 and 3.2.0 (contributor, #1538 comment 17), 3.6.0 plus re-recording (#1538 comment 16), 3.0 (#1538 comment 7), 4.0.0 working fine (#1538 comments 13 and 14), and datasets==2.19 on the Seeed wiki. Publishing one number would make the others wrong; a human should decide whether the entry names a constraint (record and train on the same pinned version below 4.0) or a specific version.
- **find-port-and-leader-follower-port-mixup**: Two decisions a verifier cannot make. (1) Scope: the enum defines setup-motors as setting IDs/baud, which lerobot-find-port does not do; it is tagged setup-motors because the wiki files it as the prerequisite of step F-002/L-002 and because it sits next to the existing setup-motors-no-response entry, but a strict reading of the enum makes it lerobot-software. (2) This is the batch's only documentation-only candidate — no GitHub thread, no reporter, frequency 1 — so confidence confirmed rests on a vendor wiki stating both the failure and the remedy rather than on a reporter or maintainer. A human should confirm that a vendor doc counts as confirmation, and whether the leader/follower port swap (the page's second, arguably more valuable half) should be its own entry.
- **editable-install-config-ignored + editable-install-plugins-not-registered**: The cluster's two threads give OPPOSITE advice, so I split it into two entries rather than write conditional symptoms. #1129 (lerobot 0.1.0 docs, 2025-05, COLLABORATOR-confirmed): a non-editable install makes local config edits invisible, so use `pip install -e`. #2460 (lerobot 0.4.2, 2025-11, COLLABORATOR-confirmed): under PEP 660 an editable install hides third-party `lerobot_*` plugins from `register_third_party_devices()`, fixed in PR #2687. Decide whether both pages ship (and cross-link with the version each applies to), or only the first. The unsourced "install non-editable" workaround has already been cut from the plugins entry's fix field. VERIFIER: both confirmations re-checked and both hold; the two entries are genuinely about different lifecycles (running the app vs developing a plugin), so shipping both with the version band stated is defensible.
- **libtorchcodec-load-failed + training-memory-crash**: CROSS-CLUSTER, the highest-value review decision in this batch: these two candidates have the same root cause — torchcodec built against a different PyTorch/ffmpeg than the one installed. #2209 comment 4 calls it "an ABI instability caused by version incompatibility between TorchCodec and PyTorch"; #3976 was fixed by `pip install torchcodec==0.7`; #964 comment 5 says "version 0.2.1 is for the torch version 2.6 not 2.5". They differ only in the observable: the library fails to load (RuntimeError at import) versus it aborts inside a DataLoader worker (std::bad_alloc). Two pages with two error strings is defensible for search; one page with both is defensible for truth. Decide, and if they stay separate, cross-link them so a builder who lands on one sees the other's fix. VERIFIER: agreed and sharpened — note that #941 comment 10 shows one builder hitting BOTH in sequence (the ffmpeg>=7 fix let her record, then training died in libtorchcodec), which is evidence for cross-linking rather than merging.
- **jetson-arm64-wrong-actions-or-no-gpu**: One cluster, two unrelated failures. The maintainer-by-association (CONTRIBUTOR) confirmation covers ONLY the CPU-fallback half (#2363, "Verified on a real Orin NX: torch.cuda.is_available() -> True"), and even that is the fix author's own verification, not the reporter's. #3636's wrong-actions symptom has no established cause: comment 1's controlled experiments rule out the decode backend and leave fp16/bf16 numerics under JetPack torch 2.5.0 untested, with experiments still in progress as of comment 2. Either split #3636 into its own `open` entry or drop it from this one; as written, one page claims a fix for a symptom nobody has explained. VERIFIER: I did not split, but I rewrote the fix sentence so it no longer claims the #3636 half — the page is now honest even if it ships unsplit. If you do split, the #3636 half is `open`, cause "Not established in the public record.", evidence = #3636 issue body + comment 1.
- **training-memory-crash**: #2556's "RuntimeError: CUDA error: device-side assert triggered" is carried as an alias per the cluster note, but it is a different error string with zero comments, no traceback and no environment block, and a device-side assert is usually an out-of-range GPU index rather than a DataLoader abort. Decide whether it stays an alias (good for search, risks telling a builder to change torchcodec for an unrelated bug) or becomes its own `open` entry. VERIFIER: confirmed #2556 has zero comments and no traceback; if it stays an alias, the page must not present the torchcodec fix as applying to it.
- **record-keyboard-shortcuts-not-working**: Two different failures bundled: the Seeed wiki's unresponsive-keys case (hedged fix, "you may need", nobody confirmed) and #4483's Foxglove `TypeError: 'RawImage' object is not an instance of 'CompressedImage'` crash on left-arrow, which the reporter root-caused in `record_loop` and self-confirmed a patch for. The better-evidenced half is the one that does not match the title, and its patch is therefore parked in review_notes rather than in `fix`. Recommend splitting; if not, the page must keep the two causes visibly separate. VERIFIER: agreed, and this is the split I would do first after the jetson one — #4483 stands alone as a clean entry (title "TypeError: 'RawImage' object is not an instance of 'CompressedImage'", scope lerobot-software, the reporter's own patch and verification), and it is currently the only reporter-confirmed fix in this batch that no page states in its fix field.
- **rerecorded-episode-contains-old-frames**: #1372 (re-record terminates the run instead of re-recording, fixed by commit 69901b9 via PR #1371) is a distinct bug from #4109 (discarded frames encoded into the re-recorded episode, fixed by PR #3683). Both are reporter-confirmed and both are CONTRIBUTOR-authored; they share only the left-arrow re-record trigger. Splitting gives two clean confirmed entries — my recommendation — rather than one page whose single `cause`/`fix` pair can only describe #4109. VERIFIER: endorsed; both confirmations verified verbatim, so the split costs nothing in evidence and yields two `confirmed` entries.
- **policy-choice-class-not-found**: #1785's "Unrecognized model in lerobot/smolvla_base. Should have a `model_type` key in its config.json" is probably a transformers-side config lookup in a reward-classifier run, not the draccus choice-class registry of #1350; it had no human reply and was auto-closed as stale. Decide whether it stays an alias. Separately, #1350's only working-command report is a me-too (not the reporter) and the maintainer comment announces the `lerobot-train` entry point without tying it to this failure — if you want `confirmed` here, someone must reproduce it. VERIFIER: confirmed both points, and note a second me-too on #1350 (comment 1, caichuang0415) — two builders affected, still nobody confirming the workaround.
- **so101-end-effector-class-missing + end-effector-teleop-stopiteration**: Both come from the same gap — LeRobot has no SO-101 end-effector robot class — and the workaround for each is the other's subject (use the SO-100 EE class), while #3441 reports that class gone from 0.5.2 and #1433 comment 10 reports it removed in PR #1452. A builder who searches `StopIteration` and a builder who searches `SO101FollowerEndEffector` have the same problem. Decide whether these are one entry with two error strings or two cross-linked entries. VERIFIER: both threads re-read; the shared gap is real and both entries are `open`, so whichever way you go, neither page may carry the removed-class workaround as a fix.
- **daisy-chain-power-brownout**: The only `assembly`-scope candidate in the batch, i.e. the only one already inside v1 scope — and the weakest sourced: a single open thread with zero comments, whose impact list is headed "Impact Observed / Likely" so it is unknown what was measured versus predicted, and whose servo current figures have no datasheet link. Publishing it would put an unreviewed electrical claim against step F-071 on an in-scope page. Either hold it until someone replies upstream (or a measurement exists), or publish strictly as "a builder has raised this, unconfirmed" with the Observed/Likely ambiguity visible. VERIFIER: endorsed, with one addition — at promotion the plain-register rule from commit 41fad95 ("Constraint: a plain sentence is a rewording; every number in it must exist in a field or source its `from` names") blocks the 2.7 A / 2-3 A per pin / AWG 26-24 figures outright, since nothing in data/ or in any cited source carries them.
- **follower-arm-lag**: I raised frequency from 1 to 2 to obey the batch rule (frequency = distinct threads cited), but the two threads are one builder cross-posting: lerobot#1766 (2025-08-21) and SO-ARM100#125 (2025-08-22) are andrew123-q with near-identical text and the identical follow-up "I use Nvidia Jetson 4012 for evaluation." In a report sorted by frequency this entry will sit alongside genuinely independent 2-thread entries. Decide whether the staging file needs a second field (distinct reporters) or whether this entry should be hand-demoted to 1 before the report is read.
- **faulty-follower-arm-core-dump**: Sources conflict on the cause and the verification made the conflict sharper. The cited traceback is a policy input-normalization assertion (`mean` is infinity at policies/normalize.py:170); the reporter's closing comment attributes the whole failure to the follower arm hardware after a swap; and the one apparently independent confirmation is not independent — the account AlexHe99 posts from the same host and tree as the author alexhegit (alex@SER9, /home/alex/lerobot), so the record contains one builder, two policies (SmolVLA and ACT), no maintainer, and no mechanism. Publishing "replace the follower arm" as the fix for this error string would be advice the record does not support, hence reported/unverified and scope lerobot-software against the cluster's assembly. Guillaume should decide whether this is published at all, and if so whether it is framed as "this error is not necessarily your arm".
- **loose-base-shoulder-joints-vibration**: The cluster brief and the thread disagree on scope. The brief re-tags it to assembly because "both the failure and the fix are mechanical (fastening)"; the thread says tightening every screw did not help, that the horn play is normal, and that the confirmed fix was two Feetech register changes (minimum start force 16N -> 5N, P_coefficient 16 -> 8) on the base and shoulder motors — so it is tagged setup-motors, which moves it out of the v1 assembly-only scope. Added in verification: the reporter is on 7.4V motors and the OWNER's first line notes unexplained problems with the 12V version, so the register values are not established for 12V builds. A human must choose: publish as an assembly-page reassurance ("this play is normal, do not chase it with screws") with the register values as the fix, or hold it for the post-v1 motor-setup phase.
- **feetech-goal-velocity-required**: No maintainer has touched the thread in eleven months, two independent reporters are affected, and the only reporter with a workaround was running a vendor-built arm ("S0-101 built by vassar robotics (navrim model)"). Whether Goal_Velocity/Goal_Acceleration must be written for Feetech STS3215 buses generally, or only on that variant, decides whether this is an SO-101 entry at all or a vendor-variant note. Scope was corrected to lerobot-software in verification, which also places it outside v1's assembly-only scope.
- **youtube-tutorial-differs-from-stl**: Added in verification. The entry's fix is a YouTube playlist the reporter found on 19 January 2025 and called "the assembly of the latest model" — a date that predates the SO-101, so the playlist most likely shows an SO-ARM100 and publishing it on an SO-101 guide could reproduce the reporter's own complaint for our readers. The public record cannot settle which arm the playlist shows; someone must open it. If it is SO-ARM100-era, the entry should either be dropped or rewritten as "the upstream videos may show an older arm — use the step pages", with no outbound link.
- **no-signal-from-servo5-during-calibration**: NEW FROM VERIFICATION, and the sharpest call in the batch: confidence was downgraded confirmed -> reported with unverified: true. The rules binding this run define a maintainer as an author_association of MEMBER/COLLABORATOR/OWNER/CONTRIBUTOR on huggingface/lerobot or TheRobotStudio/SO-ARM100; Seeed Studio is the kit vendor, neither that nor the reporter, so 'maintainer-written' in the cluster note meant vendor-published and `confirmed` was unsupportable. On top of that, this entry tells the builder that silence from servo 5 is normal and to carry on, while our PUBLISHED entry servo5-cable-torn tells them motor 5 not answering is a torn 3-pin cable at the wrist. Both cannot be the first thing a builder reads, and the benign claim rests on a single vendor sentence that never names the lerobot change responsible, so it cannot be checked upstream; publishing 'this is normal' risks masking a real fault. Decide whether to publish it at all and, if so, how the page distinguishes the two cases and cross-links servo5-cable-torn.
- **incorrect-status-packet**: Three decisions. (1) Sources inside the one thread contradict each other on the firmware cause: the body says "they are in version 3.9 when version 3.10 is required", comment 13 says "I think version of 3.9 is not the problem. The real problem is that all 6 motors should have the same version such as 3.9 or 3.10" (…#issuecomment-2898025687) and comment 15 repeats "3.9, 3.10 will both also work just make it all same". The cause says 'mismatched versions across the bus' because the maintainer-side evidence supports that reading (comment 9, CarolinePascal COLLABORATOR: "@aliberts confirmed that a single servo with a different firmware could be the cause"; comment 11, aliberts: lerobot now checks that all servos on the bus share a firmware version). Decide whether the page also states a minimum version, which no maintainer confirms. (2) Four distinct causes share one error string (voltage, firmware mismatch, port permissions, stale USB) — one page with four branches, or several pages? (3) The verifier flipped scope setup-motors -> calibration because every cited report hits the error inside lerobot-calibrate, and that flip has a routing consequence: it moves this entry into the named post-v1 phase, where setup-motors sat closer to v1. Confirm the flip or overrule it (`firmware` is the third defensible tag for the firmware-mismatch branch).
- **calibration-works-on-old-version-not-new**: The thread's headline claim and its resolution do not match, and three unrelated causes share the thread. The reporter confirmed that replacing a motor whose LED blinked fixed THEIR arm (comment 5), but nobody explains why all six IDs read as missing, nor why the same unchanged hardware calibrated on the 20250605 checkout and not on the latest version; the issue closed stale/not_planned with no maintainer reply. Decide whether the page is titled by the error string and scoped to 'a dead motor on the bus' (the reading kept here, confidence confirmed) or demoted to 'reported' because the version regression is unexplained. Comment 6's burnt-out-motor-from-overload is a separate cause plus a feature request and should not be merged in; comment 2 is a Koch arm. ALSO an action at promotion: rename the id to the slug (motor-check-failed-missing-motor-ids) before the slug freezes — the current id, kept only for cross-batch uniqueness, no longer describes the entry.
- **bi-so-follower-cameras-not-supported**: Live maintainer/reporter disagreement, unresolved in the record. The maintainer closed the issue with "This is the correct command for latest releases" (comment 1, COLLABORATOR, and the example is lerobot-teleoperate rather than the reported lerobot-record); the reporter answered that the per-arm form prefixes every camera key and breaks policies trained before PR #2780, asked to reopen citing bi_openarm_follower and reachy2 as siblings with a top-level cameras field (comments 2 and 3), and got no reply. So 'the fix' depends on whose position you take: use the per-arm form, or apply the reporter's unmerged fork patch. Additionally the body's root-cause analysis is AI-drafted by the reporter (self-declared), not maintainer-verified.
- **so100-stl-files-removed**: Licence question, which CLAUDE.md rule 3 says to ask rather than decide: the only stated route to the SO-100 source files is a Makerworld model page hosted by Seeed, whose licence was not verified, so that URL is kept out of every ref and cited only inside review_notes. Decide whether the mirror may be linked at all, and whether an SO-100-only sourcing note belongs on /compare rather than in troubleshooting — the same wiki section still links the SO-101 STLs on TheRobotStudio's repo, so no upstream deletion is actually demonstrated, and data/compare.yaml already calls SO-100 deprecated from the pinned upstream SO100.md. Note too that no builder appears anywhere in this record: the single symptom is authored from vendor documentation, not observed.
- **eval-feature-autodetect-regression**: Two unresolved items. (1) Sources conflict on the cause: the reporter verified by revert that PR #1771 is the regression (comment 5), while a maintainer states the failure is a permanent action-space mismatch — smolvla_base is 6-dim (SO-101) vs Libero's 7-dim Franka — that "will always trigger an assertion error" (comment 12), and closed the issue on that basis. Both are kept as symptoms and the fix is scoped to the reporter's confirmed `--rename_map` case; a human must decide which cause the page leads with, and whether a Libero/sim entry belongs in the guide at all. (2) NEW, found in verification: comment 2 names https://github.com/huggingface/lerobot/issues/2247 as "other people have the same issue as well". That thread was not in this batch and has not been read, so frequency 1 may understate the cluster and #2247 may be a merge target or a second entry.
- **eval-output-dir-file-exists**: Source-class question, raised in verification. The only source is the Seeed Studio wiki — a kit vendor's own published page — which states the remedy as documentation. Nobody in the GitHub sense confirmed it: the wiki author is neither a reporter nor a MEMBER/COLLABORATOR/OWNER/CONTRIBUTOR on huggingface/lerobot or TheRobotStudio/SO-ARM100, which is the strict test in the verification brief. Kept at confidence `confirmed` / unverified:false because a documentation page has no reporter/maintainer axis to fail and data/troubleshooting.yaml already cites this same wiki URL at unverified:false (wrong-leader-gear-ratio). Guillaume should set the policy once: does vendor documentation count as confirming for `confidence`? If not, this entry and evdev-build-fails-fresh-ubuntu both move to reported / unverified:true together.
- **evdev-build-fails-fresh-ubuntu**: Same source-class question as eval-output-dir-file-exists, and it must be answered the same way for both: the only source is the vendor-authored Seeed wiki, verified verbatim at line 543, so the fix is published but not confirmed by a reporter or an upstream-repo maintainer. Kept at confirmed / unverified:false. Second, smaller call: the entry's title is the wiki's own phrasing rather than an error string, because no compiler traceback exists anywhere in the public record for this — if Guillaume wants every title to be a pasteable error string, this entry has no citable one and would need a builder's real traceback before publishing.
- **gripper-present-load-very-high**: The single source (the reporter, self-answering at comment 1 — confirmed to be the same login as the thread author) establishes only that Present_Load is signed with the sign in the high byte. It never states that decoding the sign accounts for ~1042 vs ~14, and never rules out a real direction-dependent mechanical load — so whether this entry is a register-decoding note or a mechanical symptom is unresolved in the public record. Also a 2024-12 SO-ARM100 (SO-100-era) thread: applicability to the SO-101 STS3215 servos is not stated anywhere in it. Scope was set to `firmware` (Feetech servo register semantics) against the cluster's `lerobot-software`; firmware is the best of the six enum values but neither fits cleanly, since the builder's remedy lives in their own control code.
- **groot-torch-version-conflict**: The only public reply opens by sending the reporter to the LeRobot Discord for "the most reliable answer" — not citable under CLAUDE.md rule 3 — and the rest of it is explicitly hedged ("I am not claiming the exact commands above will work"). Verified in this pass that the replier is an ordinary forum user: staff=false, admin=false, moderator=false, trust_level=3, not HF staff and not a lerobot maintainer. No public confirmed pin of PyTorch + torchcodec + flash-attn exists, and the reporter never returned. A human should decide whether to publish a symptom-only page or wait for a citable resolution; GR00T is post-v1 territory either way.
- **keyboard-teleop-sync-write-crash**: Still open with no maintainer diagnosis — verified in this pass by reading the one uncited COLLABORATOR comment (comment 3), which is only "@prrw I understood situation. thank you." A collaborator flagged issue #3057 as a similar report and the reporter argued it is a different case (end-effector control vs direct joint control). #3057 was not in this batch and has not been read, so a human or a follow-up mining pass must decide whether it merges here and whether frequency rises above 1.
- **lerobot-eval-camera-resolution-forced**: The thread's own framing shifts between post 1 and post 3 — the reporter first blames `lerobot-eval` for forcing 1280×800, then says eval had already broken on missing dependencies and the resolution problem actually appeared in `record`/`train`. No maintainer ever replied, so which command produces the symptom is unresolved, and both candidate causes (missing eval extras vs the OpenCV FFMPEG/V4L2 backend) come from a Hugging Face forum member at trust level 3 with staff=false — verified not a lerobot maintainer. Decide whether to publish it at all, and if so whether as one entry or two. The same responder points the reporter at the LeRobot Discord for a definitive answer, which is not citable.
- **robot-client-unrecognized-robot-type**: Sources conflict on what fixed it: a maintainer suspected PR #2425, the reporter then showed the error still present on the latest `main`, and three weeks later wrote only "Fixed now, thanks!" with no commit, version or action named. Verification downgraded this to confidence `open` for that reason — the record proves the bug went away and proves nothing about how. A human should decide whether to publish an entry whose fix is open but whose symptom is known-resolved upstream, or to hold it until a working lerobot version can be named (checking whether PR #2425 and its follow-up landed, and in which release, would settle it — that check is outside the pre-fetched corpus).
- **orbbec-camera-timeout**: Two faults share one entry and the identifiers disagree with each other: the `id` says "timeout" while `title` and `slug` are the serial-number-mismatch string (`no-orbbec-camera-found-for`). Both are frozen at first publish — the repo's own trailers are "Constraint: ids are stable and never reused" and "Constraint: slug is frozen at first publish; changing one is a URL break, not a rename" — so this is the last free moment to either split the entry (serial mismatch / frame timeout, each with its own fix) or rename the id to match the title. A second question for the same entry: the Orbbec depth camera is not in data/parts.yaml (the BOM has no camera at all), so a page about it has nothing to link to in this guide.
- **record-resume-num-episodes-semantics**: Two reasons a human should decide before this is promoted. (1) The sources conflict inside one page: the Seeed wiki's recording-parameters table says `--dataset.num_episodes` is "Total episodes to record" (line 1558) while the Critical Note says that on resume it is the number of additional episodes (line 1546); nothing on the page reconciles them and no upstream lerobot source was checked in this run to decide which is right. (2) No builder is recorded anywhere in the corpus as having hit this — it is a documentation rule with no reported fault and no searchable error string, so it may belong in step copy or an FAQ rather than in troubleshooting.
- **unrecognized-argument-policy-path**: Two separate decisions. (a) Cause is genuinely unresolved: the reporter's `=`-vs-space finding is only confirmed for `train.py --policy.path` on commit 2b71789e, while the usage block pasted for his original commit 42bf1e8b lists no `--control.policy.path` option at all (only `--control.policy.checkpoint_path` and `--policy`), and comment 3 shows him patching `pretrained_path` into `lerobot/configs/policies.py` to get a path through — so the titled error may be a missing flag in that version, which `=` would not fix. Comment 5 is an explicit non-confirmation of the collaborator's working command. Decide whether to publish one entry about `--flag=value` syntax (retitled accordingly) or scope it to the deprecated `control_robot.py` flag set. (b) This is an SO-100 thread: `--robot.type so100`, `act_so100_test` throughout; "so101" appears only inside the argparse choices list and in the dataset name `lerobot/svla_so101_pickplace`. CLAUDE.md puts brands other than SO-101 out of scope for v1.
- **wrist-camera-glitchy-in-record**: Two builders report opposite outcomes from the same remedy: the reporter says lowering to 5 Hz worked, the second builder says 5 Hz makes the arm jittery and settles on 20 Hz with glitches still present. Both are kept as symptoms, but a human should decide which remedy the page leads with (the durable one is replacing the camera). Also the reporter had patched `datasets/video_utils.py` from torchcodec to pyav before observing the glitches — an uninvestigated confounder on the exact code path in question.
- **vla-jepa-hardcoded-cond-tap**: Not a source conflict: the extracting agent recommended dropping this entry and I did not reject it, because the ticket's drop ground is irrelevance and an open/unverified entry is what the ticket prescribes when nothing is confirmed. Verified at review: all three comments are the reporter's own, no maintainer ever replied, the issue is closed with no PR reference, and every number is one unreplicated single-run measurement. The content (frozen-backbone conditioning taps, ridge-probe degree errors) is far outside an SO-101 build guide's audience, so a human drop decision is cheaper than publishing it.
- **smolvla-lora-alpha-defaults-to-8**: Zero comments and no maintainer reply, yet the issue is closed in the 2026-09-09 snapshot with nothing explaining the closure, so whether `lora_alpha` was exposed upstream is unknown from the public record. The reporter verified the mechanism but his workaround made the arm overshoot, so there is no clean fix to publish. Needs a check of current lerobot (or a comment on the issue) before promotion.
- **windows-temp-permission-denied**: Legitimately open: no cause, no fix, and the only activity after the reporter's own follow-up is stale-bot closure. Decide whether the site publishes symptom-only entries at all — if it does, this one is correctly graded; if it does not, drop it rather than downgrade it. The title also deviates from verbatim on purpose (run-specific temp-file token and the reporter's Windows username both removed), which is a privacy call a human should endorse.

## Verification verdicts

| Verdict | Entries |
| ------- | ------- |
| fix     | 73      |
| keep    | 37      |
| reject  | 1       |

Rejected at verification:

- wrist-roll-motor-not-responding: Three threads with three unrelated failures (stiff and unresponsive after hours of use in #2693, missing from calibration in #2924, uncontrolled rotation during teleop in #1333), no cause and no fix in any, and a headline symptom that duplicates the existing entry servo5-cable-torn down to the same related_steps and related_parts. Replaced by: a merge of #2693 into servo5-cable-torn (aliases and symptom only, no cause claim) and two honest single-thread entries, wrist-roll-missing-from-calibration and wrist-roll-unstable-during-teleop, both confidence open.

Fixed at verification:

- camera-feature-key-mismatch-mean-is-infinity: All quotes, positions and permalinks check out and every alias is verbatim; #3645 is an SO-101 thread so the title belongs here. Fixed: the wiki source notes now say 'Seeed Studio vendor wiki' instead of 'maintainer-written wiki' (the kit vendor is not a maintainer of either upstream repo), and review_notes records that 'confirmed' rests on lerobot#2262 comment 3 (MEMBER) and the reporter confirmations, not on the wiki.
- policy-trained-but-robot-barely-moves: Kept as reported/unverified - correct, because the dataset-quality advice is contradicted inside its own thread. Fixed: the alias 'the robot stays static or moves unpredictably after training' was not verbatim and is replaced by the #2213 body's wording; the fix sentence's invented figure '5-10 episodes per object position' is removed (no source states a per-position range).
- power-supply-5v-or-12v-input-voltage-error: Cause and fix are reporter- and maintainer-confirmed on lerobot#2387 and the title string is verbatim in SO-ARM100#174. Fixed: scope assembly -> setup-motors (the titled error is raised by lerobot-setup-motors and the entry's own related_steps are F-002/L-002); the review note's frequency arithmetic was wrong (7 distinct = 6 GitHub threads + 1 wiki page cited twice, not '5 GitHub + 1 wiki'); the non-verbatim alias is replaced by SO-ARM100#17's own question.
- async-inference-produces-no-actions: The entry asserted 'No confirmed fix in the public record' while citing #2120 (fixed by PR #2132 per a MEMBER) and #1500 (explained by a CONTRIBUTOR and reporter-confirmed) - a false statement on the page. Fixed: sources narrowed to the two threads that are actually open (#2356, #2980), the aliases and symptoms sourced only from #1500/#2120/#2628 dropped, #2628 dropped as a duplicate of #2622 (which belongs to the camera-key entry), frequency 5 -> 2, and both resolved threads kept as cross-references in review_notes.
- second-camera-fails-when-both-configured: The bandwidth cause is reporter-confirmed on #1179 and wiki-stated, but the title was taken from #1743 - the one cited thread where a maintainer says the error is unrelated to the cameras. Fixed: title is now #1179's verbatim 'TimeoutError: Timed out waiting for async_read() to start.' (rule 3 prefers verbatim error text and this aligns the title with the confirming thread), the old title demoted to alias, and the wiki attribution corrected.
- camera-mount-does-not-fit-camera: Fixed three things: dropped PR #62 as a source (the corpus holds its title only, so it supports nothing and citing an unread page breaks non-negotiable 1) with frequency 4 -> 3; rewrote the cause, which had attributed a 'drawn for one specific camera' claim to #55 that #55 does not make; and replaced the non-verbatim alias with #143's 'This makes the RGB image misaligned'. Stays unverified/reported - the only corrected mount cannot be shared.
- could-not-connect-on-port: The editable-install cause and fix are maintainer-endorsed (CarolinePascal, 'You're completely right !') and the title is verbatim in the wiki FAQ heading, so confirmed stands. Fixed: review_notes now states that the 'sudo chmod 666' half of the fix is Seeed Studio vendor-wiki-only with zero confirmation from anyone on either upstream repo, and the wiki attribution is corrected.
- sync-read-present-position-no-status-packet: Title verbatim in the wiki FAQ code block and the num_retry=3 clause confirmed by two reporters, so confirmed stands. Fixed: scope setup-motors -> lerobot-software, because the entry's own reason for not merging into setup-motors-no-response is that this is a teleop/record dropout after setup succeeded and none of the sources is a setup-motors run; and review_notes now records that #89's reporter never replied, so the walk-the-LEDs clause is an unconfirmed maintainer diagnostic.
- calibration-not-persisted: Correctly open/unverified; #1328 is indeed not reporter-confirmed. Fixed: related_parts dropped - #1342's reporter runs 6.4V leader and 12V follower servos, so servo-sts3215-c001-345 was not supported by any source; the raw '#1342' removed from the cause sentence; and the paraphrased aliases replaced by the verbatim 'Teleoperation is not working because it asks again and again to calibrate'.
- magnitude-exceeds-2047: The Offset-button fix is maintainer-endorsed (comment 19) and twice builder-confirmed, so confirmed stands. Fixed: review_notes wrongly called comment 18 'the reporter' - williamito is a third builder, the reporter is FaboNo; the quote is flagged as lightly normalised (markdown bold stripped); and the alias 'set_half_turn_homings fails' is replaced by the bare symbol, which is what the threads actually contain.
- dataset-index-out-of-bounds: The reinstall-from-source fix is reporter- and maintainer-confirmed on #2854 and #2680, and all quotes verified. Fixed: the cause sentence enumerated four raw issue numbers a page reader cannot resolve - rewritten as one sentence with the attribution moved to review_notes; the datasets==2.19 / datasets==3.6.0 conflict stays out of the fix and the wiki is not cited as a source.
- setup-motors-no-response: Merge is right, not a new entry: the existing entry has no verbatim error string and this cluster supplies it. Every alias and every quoted comment verified at its position and permalink; frequency 7 = 6 GitHub threads + 1 wiki page. Fixed: wiki attribution corrected, and the #1244 firmware lead (comment 1 suggests a firmware update, comment 6 shows it is impossible when the motor is not recognised) added to review_notes - it was read at extraction and not recorded.
- end-effector-teleop-erratic-motion: Sources and quotes check out, but the title came from #3890, the one cited thread whose failures nothing explains; retitled to the verbatim phrase in #2413 that carries the cause, with the #3890 title kept as an alias. Fix sentence also joined two independent mitigations, so it is narrowed to the Placo regularisation and the #3215 forward-kinematics remedy moves to a symptom plus a review note. One misquote corrected (comment 22 reads "I managed to tune th e IK solver").
- hil-serl-so101-unsupported: Renamed to hil-serl-so101-leader-unsupported and narrowed: the cause and fix each covered two unrelated bugs (the get_teleop_events TypeError and the missing urdf_path), breaking the one-sentence rule and pairing an unfixed bug with a maintainer-closed one. The urdf_path crash becomes its own entry, #2431 is no longer cited here, frequency 4 -> 3. Title string, cause quote and the Chinese-language gamepad workaround all verified verbatim.
- printed-part-too-tight-for-motor: Strongest entry in the batch; every quote verified. Two corrections: the gauge-zero-also-fails symptom was asserted without a citation (comment 10 by Lucisu is now a source, as is comment 2 for the 0.5 cm stuck depth), and the review notes now say which of the two near-twin part ids was chosen (base-motor-holder-so101, the part in F-012) and why. All eight ids resolve.
- camera-preview-not-displayed: The entry was two things at once: its cause called the Seeed wiki "maintainer-written" (it is a vendor wiki, not a lerobot maintainer) and its unverified: true contradicted the reporter confirmation quoted in its own notes. Narrowed to the rerun-shows-no-camera failure (#2287 and #3218, frequency 3 -> 2); the rerun-sdk downgrade becomes its own entry. Kept at confidence reported because the maintainer's request was a bisect ("try testing again but before this commit", struck through) so what the reporter actually ran is ambiguous, and the cause is an explicit guess.
- opencv-camera-backend-selection-fails: confidence: confirmed survives, re-anchored to #1368's issue body (reporter's own one-line CAP_MSMF fix) and the DSHOW first-hand reports. Two overclaims removed: the fix said the WSL V4L2+MJPG branch was "confirmed by the builder who tried it" when forum post 9 reads "this seemed to work sort of?" with Corrupt JPEG data following, and the cause stated the OpenCV 4.13-vs-4.12 backend change as fact when the poster who found it wrote "I would not call it a confirmed OpenCV regression from this alone". Both are now attributed, and the corrupt-image state is a symptom.
- realsense-camera-read-failed: Content verified - single-thread self-reported fix, zero comments, frequency correctly lowered to 2 because #1988 is a different error. Title changed: "read failed (status=False)" also occurs verbatim in the OpenCV cluster's logs and would collide in site search, so the title now names the camera class and the full line with serial is the first alias. The garbled wiki sentence is kept unreconstructed with an explicit instruction to re-read the live page.
- record-video-muxing-dts-timebase: Renamed record-video-muxing-dts and narrowed. The entry claimed confidence: confirmed over three error strings of which only the titled DTS crash is confirmed (contributor advice, then "Now it seems to work, thanks!" from the reporter); the --resume=true time_base error has only an in-issue patch with a contributor objecting to the closure, and the float-fps crash was closed by a PR whose state was never fetched. Cause and fix now cover the DTS crash only; the other strings stay as aliases and symptoms with a note recommending a separate open entry for resume=true.
- leader-handle-attaches-to-holder-not-motor5: Every quote re-checked against corpus/so-arm100/169.json and corpus/lerobot/4007.json and correct, including the extractor's own correction that #169 comment 1 is dmosher42 (NONE), not a maintainer, so unverified: true / confidence reported stand. Cause reworded to pin the wrong wording to v0.6.0 and the fix to stop asserting v0.6.1 as fact (a NONE commenter reports it). Discrepancy row 1 narrowed on new evidence: the pinned snapshot upstream/lerobot/docs/source/so101.mdx:341 already reads "leader holder", so our L-061 note quotes it accurately and nothing in data/ is wrong.
- find-port-and-leader-follower-port-mixup: frequency 2 was wrong — both cluster rows are the same Seeed wiki page (two sections), so the distinct cited-source count is 1. Two of three symptoms ('never names the one belonging to the arm', 'the port is never printed') are inferences the page never states and are removed. The title was a constructed sentence no builder would type; retitled around the verbatim fragment "the interface will not be detected" with the old wording kept as an alias. note fields trimmed to the section name. All four wiki quotes verified at lines 652, 661, 677, 853; the inventory's anchors confirmed not to exist on the page.
- calibration-range-outside-expected-bounds: Cause/fix mismatch: the adopted cause (start_pos == end_pos) rests on comments 4 and 11, both author_association NONE, while the fix sentence carried two different reporters' remedies (reorient the motor; widen the feetech limits) that do not follow from that cause. fix now carries only the remedy matching the cause, which no reporter or maintainer confirmed, so confidence drops confirmed -> reported and unverified becomes true; both reporters' remedies are documented in review_notes. Added the missing source for the 'hold each pose longer' clause (#607 comment 6). The suspect alias with low_factor=0.15283203 was checked and IS verbatim in SO-ARM100#60's issue body, so it stays.
- drive-mode-reversed-after-calibration: The cause asserted that both threads show different drive_mode values between arms, but lerobot#3942's own calibration dump shows drive_mode 0 for the gripper on BOTH arms and the joint still runs inverted — so the assertion is false for half the cluster. Cause rewritten to attribute each thread's state to that thread, and the fix sentence tightened to one remedy plus the documented blocker. unverified true / confidence reported verified as correct (the only COLLABORATOR comment asks a question; the thread was closed by a stale bot).
- servo-rotated-before-assembly-breaks-range: The cause cited the RETRACTED half of #170 comment 1 ('the part photographed was an SO-100 wrist'); the comment's own EDIT supersedes it with 'you took a photo of the follower wrist, not the leader'. The fix carried two remedies for two different defects and now leads with the #170 answer the reporter explicitly accepted, with the #75 recalibrate-with-current-LeRobot remedy (pkooij, COLLABORATOR, hedged 'should be fixed') moved to review_notes. Our own prose changed from 'servos' to 'motors' per the binding Constraint trailer from commit 1020f73. Also flagged that id/slug name the #75 half while the title names the #170 half.
- async-and-local-inference-differ: The 'joint_action grows larger and larger' symptom had no cited source; it is lerobot#2531 comment 1, now added and reworded to match the comment. Everything else verified: both threads open, both by milong26, no maintainer comment anywhere, the one Discord link named in review_notes as an uncitable lead and absent from every source row.
- async-server-gpu-client-cpu-device-mismatch: A symptom claimed the client ran on a Raspberry Pi; grep -ci raspberry returns 0 on both cited threads (that hardware belongs to lerobot#3861 in the diffusion cluster), so it is replaced with the hosts the threads actually name — Windows 11 laptop, Ubuntu CPU host, Macbook. Title verified verbatim in #2137's issue body; 'fixed by #2792' verified as zuoxingdong (COLLABORATOR); confidence confirmed stands.
- dagger-snap-back-on-resume: The fields contradicted each other: the title names the resume stale-observation defect (lerobot#3747, still open, fix only in the unmerged PR #4398) while cause, fix and confidence: confirmed described the pause handover landed in #3506. Cause and fix now describe only the titled defect; confidence reported and unverified true; the landed, reporter-validated pause fix is preserved in review_notes and the split recommendation added to needs_human.
- dataset-version-conversion-fails: lerobot#2805 is a different defect (v1-to-v2 converter, UnboundLocalError on robot_config, LeIsaac/so100 dataset, zero comments, no SO-101) so counting it inflated frequency to 2 and forced a two-remedy fix sentence. Dropped from source, aliases and symptoms; frequency 1; fix trimmed to PR #2199. Its URL and error strings are kept in review_notes as a lead and in needs_human as a candidate open-confidence entry, never invented away. Confirmed that #2190's issue body is genuinely empty, so citing comment 1 for the traceback is right.
- editable-install-config-ignored: All quotes verbatim in corpus/lerobot/1129.json and the COLLABORATOR fix confirmation holds; only alias 3 was a stitched phrase from the body, trimmed to the verbatim SerialException string.
- end-effector-teleop-stopiteration: Cause, symptoms and the invalidated workaround all verified; the '.pos keys / empty action' clause rested on comment 1, which was quoted but uncited, so its permalink was added to source.
- ffmpeg-no-valid-stream: Wiki and #941 quotes verbatim; added #941 comment 10 (independent builder reporting the ffmpeg>=7 fix worked) so `confirmed` does not rest on the vendor wiki alone, and recorded the maintainer's OpenCV/imshow caveat against ffmpeg 7 in review_notes.
- follower-arm-lag: Symptoms and the absence of any cause or fix verified; frequency corrected 1 -> 2 to match the batch rule (distinct threads cited), with the one-builder cross-post kept loud in review_notes and added to needs_human.
- jetson-arm64-wrong-actions-or-no-gpu: Evidence verified, but the fix sentence read as covering the whole entry while #3636's wrong-actions symptom has no established cause (comment 1 rules the decode backend out); fix now names the half it covers and states that no fix exists for #3636.
- policy-choice-class-not-found: Title and quotes verified, but `cause` carried an inference about the invocations that no comment states; trimmed to "Not established in the public record." and moved the observation to review_notes.
- daisy-chain-power-brownout: Single zero-comment thread verified verbatim including the "Impact Observed / Likely" heading; only change is the house register in our own prose (servo -> motor per commit 1020f73's Constraint trailer). Ids F-071, servo-cable-3pin, motor-control-board all resolve; power-supply-5v rightly omitted.
- faulty-follower-arm-core-dump: Sources all verified verbatim (title string, traceback, both comments). Two defects fixed: the review_notes called comment 1 "a second voice / a second user" when the account (AlexHe99 vs author alexhegit) posts from the same host and tree as the body (alex@SER9, /home/alex/lerobot), so independence cannot be assumed and the apparent corroboration was inflated — replaced with observables only; and the fix field read as two sentences (quote then em-dash clause), now one. scope lerobot-software and confidence reported/unverified upheld against the cluster brief.
- floating-cantilever-print-warning: Both comments and the body verified verbatim; the extraction's correction of the cluster brief (reporter's deselected object, not the collaborator's reorientation suggestion) is right. Cause softened: the reporter says the slicer "was showing that it was not sliced properly", not that the object was left out of the slice. Confidence confirmed upheld (reporter states the warnings are gone).
- gripper-servo-very-stiff: Cause is maintainer-confirmed (pkooij, MEMBER, comment 1, quoted verbatim) and both ConnectionError alias strings string-match the body exactly. But criterion 3 fails for the FIX: no comment says replacing the servo cleared the errors — the reporter's "happy to make progress more with a new one" is intent, and inferring success from later silence is the move non-negotiable 1 forbids. Downgraded confirmed -> reported and unverified false -> true, fix sentence reworded to say exactly what the record supports. Scope kept assembly to stay coherent with the physical title.
- loose-base-shoulder-joints-vibration: Fix and cause verified verbatim in reporter comment 3, owner closed with "Great!", so confirmed stands. Fixed an invented attribution: the extraction's symptom said "the owner and reporter agree that much of it is normal" when the owner never says it in the thread (his only substantive comment is a question) — now attributed to the reporter. Also carried the 7.4V qualifier into the symptoms, because the owner's unquoted first line ("Some people have reported problems with the 12v version") bears on whether 5N / P=8 generalises. scope setup-motors upheld against the brief's assembly.
- print-infill-13-or-30-percent: Body and the COLLABORATOR answer verified verbatim; title is the body's first line. Cause softened: the reporter never says the .3mf lives in the same repository, so that claim is removed and the 30% observation is attributed to him. Data anchors re-verified in this pass and corrected: data/printing.yaml:10-13 exact; data/compare.yaml cmp-print starts at 60 with the 13%/15% lines at 62-63 (extraction said 62-71).
- wire-groove-optional-on-motor-brackets: Both wiki lines verified verbatim at lines 127 and 191 with the section headings confirmed (114 and 185) and the oldid permalink printed at line 213; the three step/part mappings re-resolved against data/assemblies/follower.yaml (F-012 base-motor-holder-so101, F-014 motor-holder-so101-base, F-041 motor-holder-so101-wrist with the exact orientation_note quoted). Cause trimmed from two quoted sentences to one per criterion 8, with the SO-ARM100 sentence moved into review_notes. Duplication anchors corrected: compare.yaml cmp-wiring 25-35 (so100 line at 27), glossary.yaml wire-slot 197-207.
- youtube-tutorial-differs-from-stl: Title and both quotes verified verbatim; OP self-resolution supports confirmed. Two fixes: the caveat about the linked playlist was too soft — the thread is January 2025, which predates the SO-101, so "the latest model" almost certainly means SO-ARM100 and the link could reproduce the reporter's own complaint for our readers (now stated plainly and added to needs_human); and the extraction's own correction of the cluster brief had drifted line numbers, fixed to data/assemblies/leader.yaml:230 and :235 (both in L-061) and data/parts.yaml:246. Verbatim alias promoted to first position.
- feetech-goal-velocity-required: Body, environment line and comment 1 verified verbatim; issue is still open with no maintainer participation, so reported/unverified is right. scope corrected setup-motors -> lerobot-software: the enum scopes setup-motors to IDs/baud via lerobot-setup-motors or the Feetech software, while Goal_Velocity/Goal_Acceleration and the reporter's own framing ("I expected the example scripts for teleop to work") are the lerobot-software branch.
- servo5-cable-torn (merge): Wiki support verified verbatim at lines 149 and 151, and the leader applicability confirmed at line 171; the existing entry really has no aliases key and really carries retrieved 2026-09-07 on the identical ref, so the no-new-source note is right. One substantive defect fixed: the proposed new symptom inverted the wiki — "If the servo is installed here before wiring, it will be difficult to install due to the limited space" means the WIRING becomes difficult after the servo is in (which is what the existing entry's cause already states), not that the servo will not fit. Symptom rewritten; the alias "servo 5 won't fit in the wrist bracket" dropped as built on the inversion.
- goal-position-outside-calibrated-band: Every claim traced to the issue body of lerobot#3585 (zero comments; open/unverified correctly set). One quote repair: symptom 2 read "~75 degrees of physical rotation" inside double quotes; the body reads "~75° of physical rotation". Scope setup-motors -> lerobot-software (inventory's tag) upheld as correct.
- incorrect-status-packet: Two unsupported claims removed from the fix sentence. (1) The Kotakku FT_SCServo_Debug_Qt build was offered as a firmware-update route; comment 6 offers it only as a debug tool ("Currently investigating a solution for firmware updates !") and comment 9 says the only easy upgrade route is Feetech's utility on Windows — the extracted fix would have misdirected a builder. (2) "the 7.4V/5V supply" imported "5V" from our own BOM; the source says only "powering the motors at 7.4V". Also: chmod wording aligned to comments 18/20 rather than the wiki's /dev/ttyACM* glob, and scope setup-motors -> calibration (every cited report hits the error inside lerobot-calibrate; the enum's setup-motors clause covers the ID/baud activity, which nobody here was doing). Routing consequence flagged in needs_human.
- shoulder-lift-motor-overload: All quotes verified against SO-ARM100#66 (body plus the single NONE-association comment); reported/unverified correctly set and the cause/fix correctly attributed to that one non-maintainer reply. Title was a constructed full sentence too long to type; shortened to "Motor #2 (shoulder_lift) Overload", verbatim from the thread title, with the longer constructed form kept as an alias.
- calibrate-robot-type-vs-teleop-type: Cause and fix verified in both threads, and frequency 2 is right against the inventory's 1. But "which both reporters confirmed works" overstated thread 1, where the reporter stated the correction and closed the issue without saying it worked; the fix now names exactly who confirmed what. Issue 1694's thread URL added as a source row so the second thread is citable on its own, not only via two comment permalinks.
- calibration-works-on-old-version-not-new: Title, aliases, symptoms, cause evidence and the reporter's own fix confirmation (comment 5) all verified, so confirmed/unverified false stands. The cause's closing clause "so no motor answered the handshake" is stated by nobody and no comment explains why all six IDs were missing — inferred mechanism removed, the unexplained version regression stated instead. Flagged for an id rename to the slug at promotion: the id no longer describes the entry.
- eye-to-hand-calibration-large-error: Quotes verified; open/unverified correctly set and the commenter's self-promoted project correctly excluded from refs. Scope calibration -> lerobot-software: hand-eye is OpenCV camera-extrinsics work, and the enum reserves calibration for lerobot-calibrate/range/homing while putting cameras under lerobot-software. Also noted that the title is a constructed search phrase rather than verbatim (permitted here, since no error string exists). Drop recommendation carried over.
- no-signal-from-servo5-during-calibration: The wiki sentence and its context are verified verbatim, but confidence `confirmed` was unsupportable: under this run's definition a maintainer is an author_association on huggingface/lerobot or TheRobotStudio/SO-ARM100, and Seeed is the kit vendor, neither maintainer nor reporter. Downgraded to reported with unverified: true, and the fix sentence now says who asserts it and what is missing. The conflict with the published servo5-cable-torn entry is also filed as a discrepancy.
- so100-stl-files-removed: Wiki caution verified verbatim at line 323 and the licence hygiene (Makerworld URL kept out of every ref) is correct. Symptom reworded because it narrated a builder who does not exist anywhere in this record — the source is vendor documentation — and review_notes now say so plainly. Added the data/ check a reviewer would otherwise redo: nothing in data/ offers SO-100 source files, and compare.yaml's "deprecated" does not contradict "to be deleted", so no discrepancies row.
- act-state-delta-timesteps-inconsistent: Both comments verified verbatim, including the maintainer reproduction on 0.5.1, and open/unverified is right since nothing is merged. The cause joined the two quoted facts with "so ACT's forward concatenates a 3-dimensional state with a 2-dimensional one and raises in torch.cat" — a mechanism nobody states; the bridge is removed and the two facts now stand on their own.
- corrupt-jpeg-data: All citations verify: title 'Corrupt JPEG data: premature end of data segment' verbatim in the body, '222 extraneous bytes before marker 0xd0' and '47 extraneous bytes' verbatim, comment 1 = linshenghou (CONTRIBUTOR) 'u can check ... it may can help u' verbatim, comments 2-3 are github-actions[bot] and the issue closed not_planned. ONE DEFECT: the `fix` field stated non-confirmation ('Not confirmed — a contributor suggests...') instead of carrying the proposed fix; non-confirmation is what `confidence: reported` and `unverified: true` already encode. Rewrote `fix` to state the proposal and keep the caveat. confidence/unverified unchanged.
- eval-feature-autodetect-regression: Every quote verbatim at the claimed positions: comment 4 (imstevenpmwork, COLLABORATOR) revert request, comment 5 (talregev, reporter) 'I take latest lerobot and revert the #1771 PR, and it work in this setup without the workaround.', comment 10 rename_map, comment 12 action-space mismatch, comment 15 (reporter) 'I try `--rename_map=` and it working for me.' Title verbatim in the body traceback. Symptom 'worked one to two weeks earlier' traced to comment 2 ('this error didn't appear a week/2 weeks ago, so it regression'); 'a later user reports the same problem' traced to comment 17 (zhangpf21). confidence 'confirmed' survives because the REPORTER confirmed the fix at comment 15, independent of the cause dispute. ADVERSARIAL FIND: comment 2 also names https://github.com/huggingface/lerobot/issues/2247 as 'other people have the same issue as well' — that thread was not in this batch and has not been read, so frequency 1 may understate the cluster. Added to review_notes and needs_human.
- eval-policy-on-real-robot-fails: Citations verify: the title string 'ValueError: Choose either a policy or a teleoperator to control the robot' is verbatim in the issue body (so the extractor's deliberate title override of the cluster name is correct per rule 3), and comment 1 'Solved in https://github.com/huggingface/lerobot/pull/1284' is imstevenpmwork (COLLABORATOR) at the cited permalink, so 'confirmed' holds. review_notes' characterisation of comments 2-10 as unrelated SmolVLA/episode-length chatter is accurate. ONE DEFECT: the `cause` asserted 'because its policy/teleoperator check evaluated False==False' as established fact, while the reporter only wrote 'Upon verification, it seemed to be False==False here.' Reattributed to the reporter and added the line range; still one sentence.
- evdev-build-fails-fresh-ubuntu: Re-grepped the wiki: the tip is verbatim at corpus/wiki/seeed-so100m-new.md:543, the commands at :548-549 and 'After installation, rerun the LeRobot installation command.' at :554, all inside 'Install LeRobot' step 5 immediately before `pip install -e ".[feetech]"`. Scope lerobot-software (install) verified against the enum — `setup-motors` is setting IDs/baud with lerobot-setup-motors or the Feetech software, which this is not, despite the word 'feetech' in the command. ONE DEFECT: the `cause` hardened the wiki's 'gcc and other C build tools may not be installed by default' into 'are not installed by default'. Restored 'may not be'. Kept at confirmed / unverified:false on the same vendor-documentation reasoning as eval-output-dir-file-exists; escalated to needs_human.
- illegal-instruction-raspberry-pi: Fully verified. Every quote verbatim: comment 1 (pham-tuan-binh, NONE) datasets narrowing, comment 3 (same, NONE) the pyarrow diagnosis and 'The fix is to do `pip install pyarrow==20.0.0`', comment 4 'Confirmed this works...Thanks Pham-tuan-binh!', comment 5 'Confirmed solution also from my side.', comment 7 (jadechoghari, CONTRIBUTOR) 'Closing this issue as it has been resolved.' CRITICAL CHECK PASSED: comment 4's author ccha425 IS the top-level thread author, so the reporter-confirmation that carries confidence 'confirmed' is real — the fix proposer being NONE does not sink it, and review_notes states that honestly. Title 'Illegal instruction' verbatim; body supports every symptom including the import-time narrowing. ONE DEFECT: review_notes quoted comment 6 without its permalink. Added https://github.com/huggingface/lerobot/issues/1738#issuecomment-3368003220 (jamminc, NONE).
- keyboard-teleop-sync-write-crash: All four cited positions verify with correct authors, associations and permalinks, and every quote is verbatim — body ('Is it due to windows environment?' and the StopIteration traceback at motors_bus.py line 1232), comment 1 (masato-ka, COLLABORATOR) pointing at #3057, comment 2 (prrw, the reporter) 'yes and no...', comment 4 (devika-dudo, NONE) the DataProcessorPipeline reading. ADVERSARIAL CHECK: comment 3 was NOT cited and is by a COLLABORATOR, so it could have contained the maintainer diagnosis the entry says does not exist — read it, and it is only '@prrw I understood situation. thank you.', so the 'no maintainer diagnosis' claim and confidence open / unverified true both hold. Issue state open confirms it. Recorded that check in review_notes so the next reader does not have to redo it; no substantive field changed.
- lerobot-eval-camera-resolution-forced: All four quotes verified verbatim at the stated post numbers (hf/176555.json), John6666's trust_level 3 / staff=false verified, title verbatim in post 1, no Discord content cited. Fixed: the cause asserted the OpenCV FFMPEG-instead-of-V4L2 backend as the cause of layer B, where post 4 only calls it a "strong candidate" for a "probably ... separate real-camera backend/configuration issue" — cause now carries the responder's hedged wording. Source notes re-led with the comment-N stem.
- max-relative-target-typeerror: Cause quote verified verbatim in the body; the single comment by pkooij (MEMBER) says only "We are solving this in PR ... /1837". Fixed: the fix field read "upgrade to a lerobot version containing that PR", an instruction no comment gives (nothing says the PR merged or which release carries it) — it now states exactly what the record offers and that no builder-side workaround exists. Symptom 2 was a paraphrase of the reporter's docs complaint; replaced with his verbatim words.
- multi-gpu-tensor-device-mismatch: All three quotes verified verbatim at the stated comment ids, cause attributed correctly to a CONTRIBUTOR. Fixed: review_notes now records that NO comment states the workaround worked ("Here is a workaround", "quick fix", and comment 4's "My gpu stuck so I can't test"), so `confirmed` rests on reporter-authored + CONTRIBUTOR-endorsed rather than a report-back, and a reviewer can downgrade without re-reading the thread.
- orbbec-camera-timeout: FAQ text, the setup-section Serial Number instruction and the timeout tip all verified verbatim in the wiki snapshot (lines 1231, 1248, 2887-2902), and the cited URL matches SOURCES.md row 24 exactly. Fixed: the cause called the frame timeout "a transient camera-connection problem", which the wiki never states — it now says the wiki gives no cause for that variant, only a reconnect. review_notes adds the USBFS-cache context that precedes the tip (deliberately not promoted into `fix`) and flags the id-vs-title mismatch for needs_human.
- policy-repo-id-missing: Both maintainer quotes verified verbatim; tc-huang is COLLABORATOR and jadechoghari CONTRIBUTOR, so `confirmed` is within the ticket's definition. Fixed: the cause attributed prose to comment 2 that it does not contain (the comment gives only the flag plus a permalink to train.py L119-L122) — cause re-anchored on the error string's own wording plus that pointer. review_notes now records that the ValueError text exists only in the thread title, because the body's image failed to upload and comment 1 is a screenshot.
- raspberry-pi-4-camera-error: Title string verified verbatim in the body; both comments are github-actions[bot] stale-closures, correctly refused, so open/unverified:true with the prescribed fix sentence stands. Fixed: symptom 1 presented the reproduction command as `lerobot-teleoperate ...` while the body types `erobot-teleoperate ...` — the command is now quoted as the source has it, so nothing reads as a verbatim paste that is not.
- record-loop-slower-than-target-fps: Warning text verified verbatim in post 1 and the responder's refusal-to-guess quote verified in post 2; he is a forum member, not a maintainer, so open/unverified:true is right. De-instancing the title (dropping 5.6 Hz / 30.0 Hz) was considered and REJECTED: the current title is verbatim from a cited source while the de-instanced sentence appears verbatim nowhere (post 2 has placeholder parentheses), and alias 1 already carries the generic phrasing for search. Fixed: that decision is now recorded in review_notes so it is not re-litigated at promotion.
- record-no-policy-or-teleoperator-warning: Full three-sentence warning verified verbatim in the body; comment 4 by imstevenpmwork (COLLABORATOR) verified verbatim and supports both the "expected, not a fault" cause and the "nothing to fix" conclusion; comment 1's --teleop.* suggestion correctly kept parenthetical since comment 2 is only "Thank you for your suggestion". Fixed: symptom 2 ("the follower arm is not back at its rest position...") was the warning's own third sentence presented as an observation nobody reported — dropped, with the reason recorded.
- robot-client-unrecognized-robot-type: Argparse title string verified verbatim in the body and repeated in comment 2; all three comment quotes verified. Fixed: confidence downgraded reported → open and the fix replaced with the ticket's prescribed sentence, because the `fix` field held a narration of the reporter's "Fixed now, thanks!" rather than an action, and the thread's only actionable proposal (be on latest `main`) is explicitly refuted by comment 2. The resolution evidence is preserved as a symptom line and in needs_human.
- robot-disconnect-not-called-on-exception: All quotes, permalinks, comment positions and associations verified verbatim in corpus/lerobot/2621.json; maintainer confirmation (comment 4, COLLABORATOR) supports confidence confirmed. One change: slug `record-exit-leaves-motors-torqued` asserted the motors were torque-enabled, which no comment states (the body says only "remain in a fixed position"), so the slug is now `record-exit-motors-not-disconnected`. Nothing published yet, so the frozen-slug constraint is not breached.
- smolvla-stale-local-files-optim: Error string, traceback and the reporter's one-line resolution verified verbatim. One change: the fix said the issue was solved "once he did" update the files — the reporter never describes performing an update, so the sentence is rewritten as attribution of the resolution rather than a described action. Confidence stays confirmed (reporter confirmed resolution).
- unboundlocalerror-action-in-record: Traceback and both maintainer comments verified verbatim. Removed the unsourced trailing clause "so the current lerobot-record entry point replaces this code path" — the only sourced statement is "`control_robot.py` is deprecated". reported/unverified is correct: the collaborator asked for confirmation of PR #1073 and never received it.
- unrecognized-argument-policy-path: Two material corrections. (1) The cause asserted that draccus "accepts only --flag=value"; no comment states this (the reporter only guesses "Maybe it's about parse?"), and the issue body's own usage block for commit 42bf1e8b lists `--control.policy.checkpoint_path` but no `--control.policy.path` at all — corroborated by comment 3, where the reporter patches `pretrained_path` into policies.py — so a competing, equally public explanation (the flag did not exist in that version) is now stated alongside. (2) confidence downgraded confirmed→reported and unverified→true: the only reporter-confirmed remedy is for `train.py --policy.path` on a later commit, and comment 5 is an explicit non-confirmation of the collaborator's command on the failing version, so the titled error has no confirmed fix. Also flagged that the thread is SO-100, not SO-101.
- windows-temp-permission-denied: Symptoms and the absence of any answer verified; bot stale/close comments correctly excluded from sources. One change: the title carried the run-specific random token `tmp8845bk36`, which no other builder will ever type, so the title is now the generic shape, the run-specific string moved into symptoms, and the verbatim searchable stem "PermissionError: [Errno 13] Permission denied" added as the first alias. Reporter's Windows username stays redacted everywhere.

## Own checker

- rerun-has-no-attribute-scalar: confidence not confirmed but unverified=false — set unverified: true

## Skipped clusters

- readme-two-arm-servo-count-inconsistent: Not a troubleshooting item: it is a documentation-clarity report against the upstream README, with no builder symptom, no error string, zero comments, and no answer in the public record (opened and closed as completed 21 minutes later by the reporter, https://github.com/TheRobotStudio/SO-ARM100/issues/158, retrieved 2026-09-09). The reporter writes "From my understanding the leader arm requires six STS3215 servos but the bill of parts table entries with the servos sums to 12" — the table he read is headed 'Parts For Two Arms', so 12 is the two-arm total and the count is not inconsistent. Verified against our data before skipping: data/parts.yaml has servo-sts3215-c001-345 qty 7, servo-sts3215-c044-191 qty 2, servo-sts3215-c046-147 qty 3 = 12 servos for both arms (6 follower + 6 leader), and all three rows cite that very table ('Parts For Two Arms table: product codes C001/C044/C046, 7.4V', data/parts.yaml:351/372/397). So our BOM already matches upstream and there is no discrepancies.md row to write either. If Guillaume wants to serve this search anyway, it belongs in /faq or /kits ('how many servos do I need?'), not in troubleshooting, and would need a sourced answer that this thread does not contain.

## Dropped at clustering

- https://github.com/TheRobotStudio/SO-ARM100/issues/6: sourcing chatter: dead Alibaba STS3215 listing
- https://github.com/TheRobotStudio/SO-ARM100/issues/8: feature request: separated per-part STL files
- https://github.com/TheRobotStudio/SO-ARM100/issues/9: sourcing chatter: AUS/NZ source for Waveshare boards
- https://github.com/TheRobotStudio/SO-ARM100/issues/11: feature request: open CAD format (STEP)
- https://github.com/TheRobotStudio/SO-ARM100/issues/14: sourcing chatter: China-mainland parts links
- https://github.com/TheRobotStudio/SO-ARM100/issues/16: sourcing chatter: do Taobao listings match the parts list
- https://github.com/TheRobotStudio/SO-ARM100/issues/27: sourcing chatter: one-arm vs two-arm servo bundle pricing
- https://github.com/TheRobotStudio/SO-ARM100/issues/40: sourcing chatter: alternative camera for mainland China
- https://github.com/TheRobotStudio/SO-ARM100/issues/45: feature request: individual part files
- https://github.com/TheRobotStudio/SO-ARM100/issues/68: sourcing chatter: overhead camera mount camera unavailable in Europe
- https://github.com/TheRobotStudio/SO-ARM100/issues/76: sourcing chatter: USA servo source because of tariffs
- https://github.com/TheRobotStudio/SO-ARM100/issues/81: feature request: native SolidWorks files with mates
- https://github.com/TheRobotStudio/SO-ARM100/issues/99: feature request: document DH parameters, payload, workspace, repeatability
- https://github.com/TheRobotStudio/SO-ARM100/issues/102: sourcing chatter: controller board unavailable in Europe
- https://github.com/TheRobotStudio/SO-ARM100/issues/104: feature request: MuJoCo MJCF files for SO-100
- https://github.com/TheRobotStudio/SO-ARM100/issues/112: feature request: URDF with a camera mount
- https://github.com/TheRobotStudio/SO-ARM100/issues/119: feature request: reorient Mount Helper STL for printing
- https://github.com/TheRobotStudio/SO-ARM100/issues/126: feature request: link a new Feetech servo debug tool from the README
- https://github.com/TheRobotStudio/SO-ARM100/issues/130: sourcing chatter: European shopping links
- https://github.com/TheRobotStudio/SO-ARM100/issues/135: feature request: a shared SO-101 manipulation benchmark
- https://github.com/TheRobotStudio/SO-ARM100/issues/136: off-topic: general programming-competition advice
- https://github.com/TheRobotStudio/SO-ARM100/issues/138: off-topic: unrelated CPU design idea
- https://github.com/TheRobotStudio/SO-ARM100/issues/139: off-topic: nonsensical mathematical-notation post
- https://github.com/TheRobotStudio/SO-ARM100/issues/152: off-topic: promotional post for a different kit (HopeJr)
- https://github.com/TheRobotStudio/SO-ARM100/issues/153: feature request: add a screw list (NOTE: this repo already has data/fasteners.yaml, so the upstream gap is one we have filled -- worth a line in the report, not a troubleshooting entry)
- https://github.com/TheRobotStudio/SO-ARM100/issues/160: feature request: CAD source for optional STLs
- https://github.com/TheRobotStudio/SO-ARM100/issues/161: sourcing chatter: C044/C046 servo variants unavailable on Alibaba
- https://github.com/huggingface/lerobot/issues/408: feature request: Isaac Sim / Isaac Lab integration
- https://github.com/huggingface/lerobot/issues/462: feature request: bring-your-own control scheme
- https://github.com/huggingface/lerobot/issues/952: not SO-101: PI0 on the LIBERO benchmark
- https://github.com/huggingface/lerobot/issues/1365: not SO-101: MuJoCo OpenGL on macOS, no arm involved
- https://github.com/huggingface/lerobot/issues/1393: feature request: configure one motor ID at a time
- https://github.com/huggingface/lerobot/issues/1508: feature request: dual-arm triple-camera ACT eval config
- https://github.com/huggingface/lerobot/issues/1518: feature request: bimanual SO-101 support
- https://github.com/huggingface/lerobot/issues/1700: not SO-101: PI0 inference dimension mismatch
- https://github.com/huggingface/lerobot/issues/1946: feature request: bimanual SO101 support PR, auto-closed stale
- https://github.com/huggingface/lerobot/issues/1998: feature request: distributed v2.1 to v3.0 dataset conversion tooling
- https://github.com/huggingface/lerobot/issues/2035: not SO-101: svla_so100 dataset Parquet error (NOTE: its secondary KeyError is the same root cause as cluster camera-feature-key-mismatch; dropped on the inventory's relevance tag, flagged in that cluster's note)
- https://github.com/huggingface/lerobot/issues/2070: off-topic: LEGO SPIKE as an alternative platform
- https://github.com/huggingface/lerobot/issues/2259: not SO-101: fine-tuning on a Franka via RLBench
- https://github.com/huggingface/lerobot/issues/2351: not SO-101: adapting SmolVLA to an AgileX PIPER arm
- https://github.com/huggingface/lerobot/issues/2735: sourcing chatter: where to buy the kit and cameras
- https://github.com/huggingface/lerobot/issues/2831: feature request: wandb Registry support, closed as wrong repo
- https://github.com/huggingface/lerobot/issues/3058: off-topic: Korean docs translation coordination thread
- https://github.com/huggingface/lerobot/issues/3076: feature request: RL stack refactoring, call for contributions
- https://github.com/huggingface/lerobot/issues/3098: not SO-101: LIBERO sim VRAM contention, no SO-10x in the thread
- https://github.com/huggingface/lerobot/issues/3134: feature request: 0.6.0 release roadmap tracking thread
- https://github.com/huggingface/lerobot/issues/3143: feature request: reward models, call for contributions
- https://github.com/huggingface/lerobot/issues/3146: feature request: ONNX / TensorRT export CLI
- https://github.com/huggingface/lerobot/issues/3191: feature request: compute hardware guide / benchmark
- https://github.com/huggingface/lerobot/issues/3290: off-topic: Chinese docs translation coordination thread
- https://github.com/huggingface/lerobot/issues/3385: feature request: smooth human intervention, avoid follower jump
- https://github.com/huggingface/lerobot/issues/3549: feature request: --policy.revision flag for lerobot-rollout
- https://github.com/huggingface/lerobot/issues/3758: feature request: lerobot-check-calibration to detect leader/follower drift (NOTE: its motivating incident -- a ~17 degree offset silently biasing a trained policy -- is evidence for the calibration clusters; worth a report line)
- https://github.com/huggingface/lerobot/issues/3793: feature request: push intermediate training checkpoints to the Hub
- https://github.com/huggingface/lerobot/issues/3794: feature request: post-hoc EMA smoothing and velocity clipping for rollouts
- https://github.com/huggingface/lerobot/issues/3817: off-topic: LeLab (third-party tool) camera preview on Windows, not lerobot or the arm
- https://github.com/huggingface/lerobot/issues/4080: feature request: spring-back gripper trigger for the leader
- https://github.com/huggingface/lerobot/issues/4282: not SO-101: LeKiwi sync_read retry budget
- https://github.com/huggingface/lerobot/issues/4368: feature request: RFC on ROS 2 integration strategy
- https://github.com/huggingface/lerobot/issues/4400: feature request: add a TurboVLA policy plugin
- https://github.com/huggingface/lerobot/issues/4442: feature request: explicit motor-to-URDF joint mapping
- https://github.com/huggingface/lerobot/issues/4496: feature request: CLI to pin and restore camera mount poses
- https://discuss.huggingface.co/t/holy-tariffs-is-there-a-usa-source-for-lerobot-servo-motors/153846: sourcing chatter: USA source for servos because of tariffs (duplicate of SO-ARM100#76)
- https://github.com/TheRobotStudio/SO-ARM100/issues/107: not SO-101: the 'mini robot' is a separate display model, not the SO-101 arm (CLAUDE.md puts any model other than SO-101 out of scope for v1)
- https://github.com/TheRobotStudio/SO-ARM100/issues/2: pure usage question, no failure: how to run LeRobot after assembly
- https://github.com/TheRobotStudio/SO-ARM100/issues/13: pure usage question, no failure: licence file needed for a Mujoco Menagerie PR
- https://github.com/TheRobotStudio/SO-ARM100/issues/15: pure usage question, no failure: per-lot vs per-unit servo pricing
- https://github.com/TheRobotStudio/SO-ARM100/issues/20: pure usage question, no failure: is there a ROS package
- https://github.com/TheRobotStudio/SO-ARM100/issues/23: pure usage question, no failure: does LeRobot support the Waveshare-branded ST3215
- https://github.com/TheRobotStudio/SO-ARM100/issues/25: pure usage question, no failure: is the FE-URT-1 board required
- https://github.com/TheRobotStudio/SO-ARM100/issues/28: pure usage question, no failure: are URT-1 and motor-control boards both needed
- https://github.com/TheRobotStudio/SO-ARM100/issues/37: pure usage question, no failure: can Dynamixel servos replace the STS3215
- https://github.com/TheRobotStudio/SO-ARM100/issues/39: pure usage question, no failure: overall arm dimensions
- https://github.com/TheRobotStudio/SO-ARM100/issues/46: pure usage question, no failure: does inverse-kinematics code exist
- https://github.com/TheRobotStudio/SO-ARM100/issues/56: pure usage question, no failure: servo-strength to arm-size ratio for heavier payloads
- https://github.com/TheRobotStudio/SO-ARM100/issues/65: pure usage question, no failure: slicer settings for PETG instead of PLA
- https://github.com/TheRobotStudio/SO-ARM100/issues/79: pure usage question, no failure: which URDF variant (05d vs 8j) matches a strengthened-motor kit
- https://github.com/TheRobotStudio/SO-ARM100/issues/82: pure usage question, no failure: which 32x32 wrist-camera add-ons are SO-101 compatible
- https://github.com/TheRobotStudio/SO-ARM100/issues/84: pure usage question, no failure: how to cite the project
- https://github.com/TheRobotStudio/SO-ARM100/issues/131: pure usage question, no failure: is there a mounting plate for the Seeed XIAO board (NOTE: our data/parts.yaml already carries seeedstudio-mounting-plate-so101, so this is a vendors.yaml/report line)
- https://github.com/TheRobotStudio/SO-ARM100/issues/133: pure usage question, no failure: why 5-axis and not 6-axis
- https://github.com/TheRobotStudio/SO-ARM100/issues/144: pure usage question, no failure: what the Rotation Pitch teardrop holes are for (unanswered)
- https://github.com/TheRobotStudio/SO-ARM100/issues/147: pure usage question, no failure: enable copy permission on the Onshape document
- https://github.com/TheRobotStudio/SO-ARM100/issues/154: pure usage question, no failure: how to scale the follower arm 1.5-2x
- https://github.com/TheRobotStudio/SO-ARM100/issues/156: pure usage question, no failure: positional precision of the display-only miniarm
- https://github.com/TheRobotStudio/SO-ARM100/issues/179: pure usage question, no failure: licence of STEP/STS3215_03a.step -- this is OUR OWN upstream question filed 2026-09-09, tracked in memory entry servo-model-decision, not a builder symptom
- https://github.com/huggingface/lerobot/issues/692: pure usage question, no failure: how to get real-time observations for policy evaluation
- https://github.com/huggingface/lerobot/issues/1319: pure usage question, no failure: how to inspect Feetech servo health (tool pointers given, nothing broken)
- https://github.com/huggingface/lerobot/issues/1351: pure usage question, no failure: dataset format, camera count and sim-vs-real for SmolVLA
- https://github.com/huggingface/lerobot/issues/1439: pure usage question, no failure: which script runs inference-only on a real robot
- https://github.com/huggingface/lerobot/issues/1447: pure usage question, no failure: SmolVLA camera ordering and action dimensions
- https://github.com/huggingface/lerobot/issues/1462: pure usage question, no failure: is observation.state joint angles or raw servo angles
- https://github.com/huggingface/lerobot/issues/1607: pure usage question, no failure: how to run a trained ACT model between custom motion code
- https://github.com/huggingface/lerobot/issues/1622: pure usage question, no failure: why a policy appears to ignore extra camera streams
- https://github.com/huggingface/lerobot/issues/1712: pure usage question, no failure: is the dataset too small for pi0 to generalise placements
- https://github.com/huggingface/lerobot/issues/1763: pure usage question, no failure: best camera setup for SmolVLA fine-tuning
- https://github.com/huggingface/lerobot/issues/1839: pure usage question, no failure: how to run a model against a gamepad-recorded dataset
- https://github.com/huggingface/lerobot/issues/1969: pure usage question, no failure: how to record a multi-task dataset
- https://github.com/huggingface/lerobot/issues/1978: pure usage question, no failure: is there a best-fit policy per sim environment
- https://github.com/huggingface/lerobot/issues/2050: pure usage question, no failure: how to run RL in simulation on so101
- https://github.com/huggingface/lerobot/issues/2104: pure usage question, no failure: can a larger VLM backbone be used for SmolVLA
- https://github.com/huggingface/lerobot/issues/2303: pure usage question, no failure: does the follower have a scripting API
- https://github.com/huggingface/lerobot/issues/2459: pure usage question, no failure: remote SmolVLA client/server permission error on an old version, no detail given
- https://github.com/huggingface/lerobot/issues/2853: pure usage question, no failure: loss validation and VLA evaluation methodology
- https://wiki.seeedstudio.com/lerobot_so100m_new/: section #which-lerobot-repo of corpus/wiki/seeed-so100m-new.md -- pure usage question, no failure: which LeRobot repository to clone
- https://wiki.seeedstudio.com/lerobot_so100m_new/: section #handle-failed-episodes of corpus/wiki/seeed-so100m-new.md -- pure usage question, no failure: how to handle failed episodes during recording
- https://wiki.seeedstudio.com/lerobot_so100m_new/: section #dataset-collection-attention of corpus/wiki/seeed-so100m-new.md -- pure usage question, no failure: dataset-collection best practices
- https://wiki.seeedstudio.com/lerobot_so100m_new/: section #delete-modify-datasets of corpus/wiki/seeed-so100m-new.md -- pure usage question, no failure: how to delete or modify recorded datasets (links out, no inline content)
- https://wiki.seeedstudio.com/lerobot_so100m_new/: section #act-training-time of corpus/wiki/seeed-so100m-new.md -- pure usage question, no failure: how long ACT training takes
- https://discuss.huggingface.co/t/visualization-of-imitation-learning-by-smolvla-so101/176534: pure usage question, no failure: how to view the SmolVLA training curve locally without wandb
- https://discuss.huggingface.co/t/single-camera-smolvla-training/172719: pure usage question, no failure: guidance on single-camera SmolVLA training
- https://github.com/TheRobotStudio/SO-ARM100/pull/7: sourcing chatter: PR replacing the dead Alibaba servo link from SO-ARM100#6, which is itself dropped as sourcing chatter
- https://discuss.huggingface.co/t/so-101-robotic-arm-in-vacuum-chamber/172874: pure usage question, no failure: adapting the arm for a vacuum-chamber research project

## Batch notes

- batch 1 verify: ADVERSARIAL VERIFICATION of batch 1/9, run from the worktree /Users/macbook/Developer/so101-assembly-guide/.claude/worktrees/troubleshooting-candidates. Method: every cited thread file was re-opened programmatically and each quoted sentence checked as a normalised substring AT the claimed comment position, with the claimed permalink compared against comments[].url; all 12 entries' aliases (86 strings) were grepped against the whole corpus plus the Seeed wiki; every related_steps/related_parts id was checked against data-ids.json; every id and slug checked for kebab-case, <=60 chars, internal uniqueness and collision with the three existing ids and slugs (all pass, longest slug 44 chars). Result: 0 rejects, 1 keep, 11 fix, 1 merge kept as a merge (verdict id = its into_id, setup-motors-no-response). GIT QUERIES (non-negotiable 5), run in the worktree before touching data: tier-1 `git log --oneline -S"troubleshooting"` -> 12 commits (the data file lands in e19f06e and 86a2a8f; the rest are viewer/docs). Tier-2 `git log -- data/troubleshooting.yaml --grep='^Constraint:' --grep='^Directive:' --grep='^Rejected:'` -> one trailer, verbatim: "Constraint: slug is frozen at first publish; changing one is a URL break, not a rename" (86a2a8f, 'data(schema): step slug, troubleshooting.yaml, vendors.yaml per ADR-0002'). The same commit body states "Slug uniqueness is global across steps and troubleshooting entries, not per assembly, so a slug can never collide when pages are cross-linked" - checked, and the step slugs in data/assemblies are namespaced follower-_/leader-_, so no candidate slug can collide. No file under the repo was created or modified by this pass. MATERIAL DEFECTS FOUND AND FIXED (highest value first): (1) async-inference-produces-no-actions asserted 'No confirmed fix in the public record as of 2026-09-09.' while citing two threads that are fixed - #2120 (MEMBER: fixed in PR #2132) and #1500 (CONTRIBUTOR explanation, reporter: 'Thanks! It runs correctly now.'). That is a false statement on a published page; sources narrowed to the two genuinely open threads, frequency 5 -> 2, the dropped threads kept as cross-references. (2) second-camera-fails-when-both-configured took its title from #1743, the one cited thread whose only maintainer reply says the error is unrelated to the cameras - title moved to #1179's verbatim 'TimeoutError: Timed out waiting for async_read() to start.', which is both rule-3-compliant and the thread that confirms the fix. (3) camera-mount cited merged PR #62 with the note 'no thread content was available in the corpus' - an unread page cannot support anything, dropped, frequency 4 -> 3. (4) calibration-not-persisted cited servo-sts3215-c001-345 although its reporter runs 6.4V leader / 12V follower servos - related_parts dropped rather than swapped for a guess. (5) magnitude-exceeds-2047's review_notes attributed the winning procedure to 'the reporter'; comment 18 is williamito (NONE), a third builder, not FaboNo. (6) The power cluster's review note mis-stated its own frequency arithmetic (7 distinct = 6 GitHub threads + 1 wiki page cited in two rows, not '5 GitHub + 1 wiki'). (7) Two scope tags were internally inconsistent and are retagged: power-supply -> setup-motors (its own related_steps are F-002/L-002 and the error comes from lerobot-setup-motors), sync-read -> lerobot-software (its own reason for not merging is that it is a teleop/record dropout, which the enum puts there). (8) Five aliases were not verbatim anywhere and are replaced with the builders' actual words or the bare symbol (policy-barely-moves, power-supply, camera-mount, calibration-not-persisted, magnitude). (9) 'maintainer-written wiki' is corrected to 'Seeed Studio vendor wiki' throughout: under this run's own rule a maintainer is MEMBER/COLLABORATOR/OWNER/CONTRIBUTOR on one of the two repos, and the kit vendor is neither. Checked one by one, every 'confirmed' entry has independent repo-side confirmation and survives - with one exception flagged in its review_notes and in needs_human: the 'sudo chmod 666' half of could-not-connect-on-port is vendor-wiki-only and unconfirmed by any builder in the corpus (the entry keeps 'confirmed' on the maintainer-endorsed editable-install half). CHECKS THAT PASSED WITH NO CHANGE: no Discord, Slack or WeChat link is cited anywhere (the two Discord-only leads - lerobot#1620 comment 1, and HF forum post 2 routing to Discord - are recorded in review_notes and cited for nothing); every source row has retrieved '2026-09-09' and a note; every related_steps id (F-002, F-003, F-071, L-002, L-071) and related_parts id resolves in data-ids.json; no title is invented - each is either a verbatim error string or a verbatim issue title, with the two deliberate deviations ('[RxPacketError] Input voltage error!' and 'Could not connect on port "/dev/ttyACM0"') explained in their review_notes; the camera-key entry's #3645 source was checked to be an SO-101 thread (--robot.type=so101_follower) before accepting its title. One happy cross-check rather than a discrepancy: SO-ARM100#151's ThinkRobotics kit inventory (7x C001, 2x C044, 3x C046 across two arms) matches data/servos.yaml exactly (6 follower C001 + leader L2 C001 = 7; L1/L3 C044 = 2; L4-L6 C046 = 3). DISCREPANCIES: the two from extraction re-verified at their cited comment positions, plus one new - data/servos.yaml records no range or continuous-rotation property for wrist_roll while LeRobot hardcodes it as a full-turn joint and two builders with Seeed-printed parts report physical hard stops on it (lerobot#3193 comments 4 and 5); nothing in data/ is wrong today, but the Seeed variant must be noted the moment joint ranges are added. NEEDS_HUMAN: all 13 carried forward and updated for this pass. Five are decisions only Guillaume can make rather than more reading (whether to mirror upstream doc paths at all; whether optional camera mounts are in scope; whether WSL2 gets its own entry; whether to publish a known-broken async page; the unanswered Waveshare board-rating question). Eight are source conflicts. Three of my own calls want confirmation: the two scope retags, and the title move on the two-cameras entry. Durable copy of this payload: /private/tmp/claude-501/-Users-macbook-Developer-so101-assembly-guide/d833fc3b-d8e2-4870-870f-3609424bae0e/scratchpad/batch1-verified.json (builder script build_batch1_verified.py and the verification harness vq.py / checks.py / aliascheck.py alongside it).
- batch 2 verify: GIT QUERIES, run in /Users/macbook/Developer/so101-assembly-guide/.claude/worktrees/troubleshooting-candidates before reading any data. Tier 1: `git log --oneline -S"troubleshooting"` returns 12 commits, of which only 86a2a8f creates the troubleshooting data shape, plus e19f06e which adds the three existing entries; `git log --oneline -S"unverified"` returns 20 and confirms the unverified flag is a published site behaviour (c808ce9 "viewer+ci: indexable by default on kitsmith.dev; unverified parts leave the sitemap"), which is why this pass treats flipping unverified as a publishing decision, not a cosmetic one. Tier 2, trailer grep over data/troubleshooting.yaml and data/schema/troubleshooting.json, yields exactly one trailer, verbatim: "Constraint: slug is frozen at first publish; changing one is a URL break, not a rename" (86a2a8f), whose commit body also decides "Slug uniqueness is global across steps and troubleshooting entries, not per assembly, so a slug can never collide when pages are cross-linked." CONSEQUENCE, and a check the extractor did not run: I grepped every `slug:` from data/assemblies/follower.yaml and leader.yaml (52 step slugs, all of the form follower-_/leader-_) and compared them with the three published troubleshooting slugs and with all 16 candidate slugs below - no collision, and the slugs I changed (rerun-window-shows-no-camera, record-video-dts-out-of-order, find-joint-limits-missing-urdf-path) are changed now precisely because they can never be changed after publication. No file under the repo was written by this pass; data/troubleshooting.yaml, data/schema/ and data/assemblies/ were read only.

WHAT THIS PASS CHANGED, in order of weight. (1) merges was empty and is no longer: lerobot#2693 merges into the existing servo5-cable-torn as aliases plus one symptom, with no cause change, which is what the ticket's Acceptance line requires and what the extractor had declined to decide. (2) Three compound entries were narrowed so that a confidence label covers only the bug it was earned on: record-video-muxing-dts-timebase claimed confirmed across three error strings of which one is confirmed; hil-serl-so101-unsupported mixed an unfixed TypeError with a maintainer-closed urdf_path crash; end-effector-teleop-erratic-motion joined two independent mitigations. (3) One entry rejected outright (wrist-roll-motor-not-responding) and four new entries created from the splits: hil-serl-urdf-path-missing, rerun-has-no-attribute-scalar, wrist-roll-missing-from-calibration, wrist-roll-unstable-during-teleop. (4) Two overclaims removed from the one entry carrying confidence: confirmed on forum evidence (opencv-camera-backend-selection-fails): the WSL branch's "confirmed by the builder who tried it" became a hedged symptom, and the OpenCV 4.13/4.12 backend boundary is now attributed to the poster who explicitly declined to call it a confirmed regression. (5) Two titles changed for searchability and uniqueness (the EE entry, the RealSense entry), one symptom given the citation it was missing (gauge zero also fails, #63 comment 10), and one inferred part id dropped (servo-sts3215-c001-345 on wrist-roll-unstable-during-teleop: #1333 names only "Feetech STS3215 servos" on an SO-100, neither voltage nor gear ratio, so our 7.4V 1:345 id would be inferred rather than resolved).

INDEPENDENT CHECKS RUN, so a reviewer need not repeat them. Every related_steps and related_parts id resolves in data-ids.json (F-001, F-011, F-012, F-050 among steps; base-so101, base-motor-holder-so101, wrist-roll-pitch-so101, servo-sts3215-c001-345 among parts) and no candidate id or slug collides with setup-motors-no-response, servo5-cable-torn or wrong-leader-gear-ratio. All five PR rows that the extractor took from "the inventory snapshot" were re-checked line by line against corpus/so-arm100.index.jsonl: #117 closed/1, #106 closed/6, #167 closed/0, #95 open/0, #48 closed/2, plus #30 closed/5 - every state and count matches the entry text, and no entry asserts merged. Every quoted comment was located in its thread file by normalised substring match at the position claimed; the one mismatch found is recorded in the EE entry ("I managed to tune th e IK solver"). A programmatic check confirms frequency equals the distinct cited thread count on all 16 entries. Every source URL is github.com, discuss.huggingface.co or wiki.seeedstudio.com, and every source carries retrieved 2026-09-09, matching corpus/RETRIEVED (RETRIEVED=2026-09-09).

OFF-PLATFORM LEADS FOUND AND DELIBERATELY UNUSED: WeChat number in lerobot#1819 comment 5 ("加我微信18451316007"), WeChat handle in lerobot#2431 comment 6, Discord pointer in SO-ARM100#54 ("I saw some people having trouble importing it into their own sims on discord earlier"), and Discord suggestions in hf forum topic 178337 posts 2, 4 and 10. None is citable; none is cited.

FREQUENCY RULE, applied consistently and now equal to the cited distinct-thread count on every entry: 4 (EE), 3 (hil-serl leader, urdf, printed part, which-stls, encoder wrap, zero point, opencv, video), 2 (camera preview, realsense, duplicate part), 1 (urdf_path, rerun scalar, both wrist entries). Where a count includes fix-side PRs or alias-only threads, the effective independent-report count is stated in that entry's review_notes.

BATCH SHAPE FOR GUILLAUME: 16 entries after verdicts - 3 assembly (printed part, which-stls, duplicate part), 3 calibration (encoder wrap, zero point, wrist-roll missing), 10 lerobot-software, 0 setup-motors, which is the right outcome rather than a gap: no thread in this batch has lerobot-setup-motors or the Feetech software as the failing surface, so the tag has no members here (the extractor had tagged the wrist-roll cluster setup-motors; #2693 became a merge onto an existing entry and the other two threads fail in lerobot-calibrate and in teleop). By confidence: 2 confirmed, 12 reported, 2 open. 14 of 16 carry unverified: true. Durable copy of this payload: /private/tmp/claude-501/-Users-macbook-Developer-so101-assembly-guide/d833fc3b-d8e2-4870-870f-3609424bae0e/scratchpad/out/batch2-verified.json (generator: out/batch2-verified.py).

- batch 3 verify: ADVERSARIAL VERIFICATION of batch 3/9. All 13 clusters re-opened from the JSON files; every cited thread, comment position, author_association and quote checked against the file, not against the cluster metadata. Result: 3 keep, 9 fix, 0 reject. Nothing was rejected because every entry traces to a public comment and the weak ones are salvageable as unverified; the fixes are concentrated in four classes — unsupported detail, a frequency that counted one source twice, a cause/fix mismatch, and fields that contradicted their own confidence.

SHAPE NOTES FOR THE ASSEMBLER. (a) 12 verdicts are emitted, one per entry id; the merge has no verdict field, so its verdict is: KEEP as a merge onto wrong-leader-gear-ratio, with two corrections recorded in its review_notes (the \"always 7.4v\" line is a blockquote by a NONE commenter, not a maintainer statement; the #31 title alias normalises a double space). (b) discrepancies row 3 carries entry_id servo5-cable-torn, which is an EXISTING published id in data/troubleshooting.yaml, not a candidate in this batch — do not try to match it to an entry. (c) Three entries changed confidence downward (calibration-range-outside-expected-bounds and dagger-snap-back-on-resume from confirmed to reported with unverified true; find-port-and-leader-follower-port-mixup kept confirmed but frequency 2 to 1), and two changed frequency (find-port 2->1, dataset-version-conversion-fails 2->1), so flag counts and the frequency sort both shift. Five entries now carry unverified: true (leader-handle, calibration-range, drive-mode-reversed, async-and-local-inference, dagger-snap-back), up from three.

CONCRETE ERRORS FOUND AND FIXED. (1) async-server-gpu-client-cpu-device-mismatch claimed a Raspberry Pi client; grep -ci raspberry = 0 on both cited threads — that hardware is lerobot#3861's, in a different cluster. Removed. (2) find-port frequency 2 counted two SECTIONS of one wiki page as two threads; the distinct cited-source count is 1. Its symptoms also asserted two behaviours the page never states. (3) calibration-range-outside-expected-bounds stated a cause from two NONE commenters and then a fix from two different reporters' unrelated remedies; fix now matches cause, confidence downgraded, and the missing source for its 'hold each pose longer' clause (#607 comment 6) added. (4) drive-mode-reversed-after-calibration asserted 'different drive_mode between the arms' for both threads, but #3942's own dump shows drive_mode 0 on BOTH arms with the joint still inverted. (5) servo-rotated-before-assembly-breaks-range quoted the half of #170 comment 1 that the same comment's EDIT retracts. (6) dagger-snap-back-on-resume had confidence confirmed and unverified false on an entry whose titled defect is an open issue with an unmerged PR. (7) dataset-version-conversion-fails counted an unrelated converter thread toward frequency. (8) async-and-local-inference-differ had a symptom with no cited comment (#2531 comment 1, added). Two checks came back CLEAN where I expected a fabrication: the alias \"low_factor=np.float32(0.15283203), upp_factor=np.float32(0.27294922)\" is genuinely verbatim in SO-ARM100#60's issue body (different values from #607's), and the full RuntimeError title is genuinely in #2137's issue body.

EXTRACTOR CORRECTIONS CONFIRMED, not reverted. All four of the extraction's metadata corrections were re-checked in the files and are right: #169 comment 1 is dmosher42 (NONE) and neither #169 nor lerobot#4007 has any MEMBER/COLLABORATOR/OWNER/CONTRIBUTOR comment; #170 has no maintainer comment at all (comment 1 is dmosher42, comment 2 is the reporter), so that entry's maintainer support comes only from #75; lerobot#607's comment 12 (pkooij, MEMBER) only announces a procedure change and closes the thread; lerobot#3861 comment 2 IS reporter validation of PR #3886 by RahimMirani (CONTRIBUTOR, the thread author), so that cluster is confirmed despite the metadata's 'proposed-only'. A downstream pass must not 'restore' any of these.

GIT QUERIES (run from /Users/macbook/Developer/so101-assembly-guide/.claude/worktrees/troubleshooting-candidates). Tier-1: `git log --oneline -S\"troubleshooting\"` -> 12 commits, relevant ones 86a2a8f \"data(schema): step slug, troubleshooting.yaml, vendors.yaml per ADR-0002\" and c80b40d \"repo: issue forms for step corrections, troubleshooting entries and site bugs\"; `git log --oneline -S\"unverified\"` -> 20+ commits across data and viewer. Tier-2 trailer grep over data/troubleshooting.yaml, data/schema and data/assemblies (note: --grep must precede the pathspec or git errors) returned, quoted verbatim: \"Constraint: our own wording says motor; only verbatim quotes say servo\" and \"Directive: a leader step that overrides a field a plain sentence points at must re-read the sentence; the validator checks the numbers, the reviewer checks the words\" (1020f73); \"Constraint: slug is frozen at first publish; changing one is a URL break, not a rename\" (86a2a8f); \"Constraint: ids are stable and never reused\" and \"Directive: add a field to the schema before the viewer reads it, never the reverse\" (e973c5b); plus \"Constraint: a plain sentence is a rewording; every number in it must exist in a field or source its `from` names\" (41fad95). The first binds these candidates and was ENFORCED in verification: entries 3 and 5 said 'servos' in our own prose and now say 'motors'; 'servo' survives only inside quoted source text. The slug/id trailers are why the gear-ratio cluster is a merge and why a candidate id/slug mismatch (servo-rotated-before-assembly-breaks-range vs its #170 title) is flagged for renaming BEFORE publish rather than after.

ID AND REFERENCE CHECKS. All 12 ids and 12 slugs are kebab-case, unique within the batch, <= 60 chars, and collide with none of setup-motors-no-response / servo5-cable-torn / wrong-leader-gear-ratio nor their slugs. Every related_steps id (F-002, F-050, F-051, F-063, L-002, L-050, L-051, L-060, L-061, L-062, L-063) and every related_parts id (handle-so101, wrist-roll-so101, wrist-roll-pitch-so101, wrist-roll-follower-so101, moving-jaw-so101, motor-control-board, usb-c-cable, servo-sts3215-c001-345, servo-sts3215-c046-147) was checked against data-ids.json and resolves; eight entries have no resolvable step or part and carry deliberately empty arrays. No id was dropped as unresolvable in this batch. Source hygiene: every source row is an https URL on github.com or wiki.seeedstudio.com with retrieved '2026-09-09'; no Discord, Slack, WeChat or login-only link appears anywhere. The corpus's one Discord URL (lerobot#2531 comment 4) is named in review_notes as an uncitable lead only. Two corpus comments (lerobot#3330 comment 2 and lerobot#3445 comment 2, both rylinjames, NONE) mix technical advice with a third-party repo pitch plus a Discord handle and an e-mail address; both are cited nowhere, and the reason is recorded in both entries' review_notes — treated as untrusted third-party content, not as instructions. The Seeed wiki anchors the inventory named (#find-port-usb-not-removed, #port-mapping-ttyACM, #stack-typeerror-tensors) were checked against the page's heading list and do not exist, so citing them would be a fabricated citation; all wiki rows cite the bare page URL as SOURCES.md row 24 gives it, with the section in note.

SCOPE SPREAD after verification: 1 assembly, 1 setup-motors, 3 calibration, 7 lerobot-software, with the merge landing on an assembly entry — so 10 of 13 clusters sit outside CLAUDE.md's v1 scope and are tagged, not dropped, per the ticket. No entry uses approximation. Frequency caveats preserved: both threads of async-and-local-inference-differ are by milong26 and both of dagger-snap-back-on-resume by anikita, so frequency 2 counts threads, not independent builders. What would clear each unverified flag: leader-handle — a maintainer confirming the v0.6.1 wording change, or the docs diff cited directly; calibration-range — a reporter or maintainer confirming that recalibrating through both gripper limits fixes the inf% error; drive-mode-reversed — anyone reporting whether the JSON signature check still blocks the workaround on current LeRobot; async-and-local-inference — a maintainer answering #2475 or #2531 in public; dagger-snap-back — PR #4398 merging, which would also make the entry splittable with both halves confirmed. No file under the repo was written or modified; this is data only.

- batch 4 verify: VERIFICATION PASS, batch 4/9, adversarial, 2026-09-09. I re-opened all 24 cited sources (23 GitHub threads, read from their JSON files under corpus/lerobot and corpus/so-arm100, plus the Seeed wiki snapshot corpus/wiki/seeed-so100m-new.md) and checked every quoted sentence against the file at the stated comment position. RESULT: 14 entries in, 14 out, 0 rejected, 6 fixed, 8 kept. No quote in the extraction was fabricated or misattributed, and every cited comment position and permalink matched. That is the main finding: the extraction's sourcing is sound, so the fixes below are about overclaiming, rule compliance and register, not about invented facts.

THE SIX FIXES. (1) jetson-arm64-wrong-actions-or-no-gpu — the only entry that overclaimed: its fix sentence read as covering both halves of the cluster while #3636's wrong-actions symptom has no established cause at all (comment 1's controlled experiments call the decode backend "Very unlikely"). The fix sentence now names the half it covers and states that no fix is established for #3636. (2) follower-arm-lag — frequency 1 -> 2 per the batch rule that frequency equals distinct threads cited; the extractor's de-duplication by author was well argued but would have corrupted the frequency-descending sort of the staging file if eight other batches count threads. The one-builder fact is preserved in review_notes and raised in needs_human. (3) policy-choice-class-not-found — `cause` carried an inference about how the script was invoked that no comment states; trimmed to "Not established in the public record." (4) end-effector-teleop-stopiteration — the '.pos keys' clause of the cause rested on #1433 comment 1, which was quoted in review_notes but missing from `source`; permalink added. (5) ffmpeg-no-valid-stream — added #941 comment 10, an independent builder reporting that `conda install -c conda-forge 'ffmpeg>=7.0'` let her record a dataset, so `confirmed` no longer leans on the vendor wiki alone; also recorded the maintainer's caveat that ffmpeg 7 "would reintroduce conflicts with OpenCV". (6) daisy-chain-power-brownout — one word of our own prose, servo -> motor.

NO ENTRY WAS SPLIT, and no entry was created. Five entries bundle two distinguishable failures (jetson, record-keyboard, rerecorded-episode, training-memory, policy-choice) and the extractor routed each to needs_human; splitting is a page-inventory decision for review, not a verifier verdict, so all five stay whole with their needs_human rows sharpened. The one entry where a bundle could actively mislead was fixed in its fix sentence instead (jetson). If you act on only one split, take record-keyboard: #4483's reporter-confirmed Foxglove patch is currently the best-evidenced fix in this batch that no page states in its fix field.

NOTHING REJECTED, and why the two weakest entries survive: follower-arm-lag has no cause and no fix anywhere in either thread, and daisy-chain-power-brownout is a single zero-comment thread whose impact list is headed "Impact Observed / Likely"; both are salvageable as `unverified: true` / `confidence: open` with the fix written as "No confirmed fix in the public record as of 2026-09-09.", which is what the ticket asks for, and rejecting them would lose two real searchable symptoms. daisy-chain remains the entry I would hold back longest: it is the only assembly-scope candidate, i.e. the only one already inside v1, and it would put an unreviewed electrical claim against step F-071.

CHECKS RUN ON EVERY ENTRY. Ids and slugs: all 14 kebab-case, all slugs <= 60 chars and matching ^[a-z0-9]+(-[a-z0-9]+)*$, all unique, none colliding with setup-motors-no-response, servo5-cable-torn or wrong-leader-gear-ratio (verified against data/troubleshooting.yaml lines 2, 25, 46). Sources: every ref is an https URL with retrieved '2026-09-09'; no Discord, Slack, WeChat or login-only link appears in any entry or in any cited thread as the only evidence; the Seeed wiki is cited as https://wiki.seeedstudio.com/lerobot_so100m_new/, matching the inventory row and the existing house entry. Ids: every related_steps and related_parts id resolves in data-ids.json (only daisy-chain carries any: F-071, servo-cable-3pin, motor-control-board); the 13 software entries are correctly empty rather than padded. Scope tags: lerobot-software 13, assembly 1 — all correct per the enum; nothing in this batch is setup-motors, calibration, sourcing or firmware, and I checked the two temptations (the editable-install pair is install/runtime software, not firmware; daisy-chain is physical build and wiring, so assembly, not sourcing). Titles: verbatim error strings where one exists (StopIteration, RuntimeError: Could not load libtorchcodec, Couldn't find a choice class for 'smolvla', terminate called after throwing an instance of 'std::bad_alloc', No valid stream found in input file. Is -1 of the desired media type?, local modifications to configuration files are ignored during runtime, Slave Arm Lagged, and the two verbatim issue titles), and searchable phrases where none does (rerecorded-episode, record-keyboard, so101 end effector). cause and fix are one sentence each everywhere. confidence after this pass: confirmed 6, reported 3, open 5; unverified true on all 8 non-confirmed entries.

MERGES: none. I independently checked all three published entries against all 14 candidates: nothing here touches setting motor IDs with lerobot-setup-motors, the motor-5 wrist cable, or leader gear ratios. This batch is post-assembly software almost end to end.

GIT QUERIES (non-negotiable 5), run in /Users/macbook/Developer/so101-assembly-guide/.claude/worktrees/troubleshooting-candidates before reading data. Tier 1: `git log --oneline -S"troubleshooting"` -> 12 commits, the data-bearing ones 86a2a8f and c80b40d; `grep -n "^- id:" data/troubleshooting.yaml` confirms the three published ids are still setup-motors-no-response, servo5-cable-torn and wrong-leader-gear-ratio (lines 2, 25, 46), i.e. none has been renamed since extraction. Tier 2, `git log --grep='^Constraint:' --grep='^Directive:' --grep='^Rejected:' -- data/troubleshooting.yaml data/schema data/assemblies`, trailers quoted verbatim: "Constraint: mined entries never enter data/troubleshooting.yaml unreviewed" and "Directive: three open decisions (v1 scope, file layout, rerun cadence) live in the ticket; answer them before promoting entries" (2fbc52f); "Constraint: slug is frozen at first publish; changing one is a URL break, not a rename" (86a2a8f); "Constraint: ids are stable and never reused" and "Directive: add a field to the schema before the viewer reads it, never the reverse" (e973c5b); "Constraint: our own wording says motor; only verbatim quotes say servo" and "Directive: a leader step that overrides a field a plain sentence points at must re-read the sentence; the validator checks the numbers, the reviewer checks the words" (1020f73); "Constraint: a plain sentence is a rewording; every number in it must exist in a field or source its `from` names" and "Constraint: glossary kind: fact needs a source; kind: definition is general meaning only, never a kit fact" (41fad95). THE ONE THE EXTRACTOR MISSED AND IT BINDS PROMOTION: 1020f73's motor/servo constraint. I applied it to the single occurrence in our own prose (daisy-chain's cause) and deliberately did not sweep the rest, because every other "servo" in this batch is inside a quotation, an error string, or a frozen part id (servo-cable-3pin, servo-sts3215-*) where changing it would falsify a quote or break an id. At promotion, the same constraint applies to the plain-register rewrite of every entry that ships, and 41fad95's numbers rule will bite the daisy-chain figures (2.7 A, 2-3 A/pin, AWG 26-24), which have no datasheet link anywhere in the thread.

REGISTER NOTE FOR THE REVIEWER, beyond the trailer: follower-arm-lag's title "Slave Arm Lagged" and several quoted symptoms use slave/master. Those are the reporter's words and must stay verbatim inside quotes and in the searchable title, but the page's own prose has to read leader/follower.

DISCREPANCIES: both carried over and both independently verified against the worktree — data/parts.yaml:413 is `servo-sts3215-12v-345` with `qty: 0` and the name "Feetech STS3215 servo, 12V, 1:345 gear (optional follower variant)", and data/parts.yaml:525 is `power-supply-5v` with `qty: 2` and the name "Power supply, 5V (for 7.4V servos)" — both line numbers exact. So the gap the extractor reported is real: two independent builders in this batch (jtabor on lerobot#1433, rabhishek100 on SO-ARM100#132) are on the 12 V variant, and the BOM carries that motor at qty 0 with no 12 V supply part at all. I found no further contradiction between these candidates and data/ — in particular nothing here contradicts data/servos.yaml's joint-to-ratio table or the three published entries. I wrote no file under the repo; data/troubleshooting.yaml, data/schema/ and data/parts.yaml were read only. Durable copy of this output: /private/tmp/claude-501/-Users-macbook-Developer-so101-assembly-guide/d833fc3b-d8e2-4870-870f-3609424bae0e/scratchpad/batch4-verified.json

- batch 5 verify: BATCH 5/9 ADVERSARIAL VERIFICATION. 12 entries in, 12 out (0 rejected), 1 merge, 2 discrepancies, 4 needs_human. Verdicts: 8 fix, 4 keep. Every cited thread file was re-opened in full and every quoted string re-checked against it; no files were written anywhere.

WHAT THE EXTRACTION GOT WRONG (substantive, in order of how much it would have misled a reader):

1. MERGE, wiki misreading — the proposed new symptom for servo5-cable-torn inverted the wiki note. "If the servo is installed here before wiring, it will be difficult to install due to the limited space" means the WIRING is hard after the servo is in (exactly what the existing entry's cause already says), not that the servo will not fit. Symptom rewritten, the alias built on the inversion dropped.
2. loose-base-shoulder-joints-vibration — invented attribution: "the owner and reporter agree that much of it is normal". The OWNER never says it in the thread (comment 1 is a question, comment 4 is "Great!"); the claim exists only as the reporter's "you are correct that the looseness in my video is actually the normal thing", referring to something said outside the record. Re-attributed. Also added the 7.4V qualifier the extraction dropped, against the OWNER's unquoted "Some people have reported problems with the 12v version".
3. faulty-follower-arm-core-dump — inflated corroboration: comment 1 was presented as "a second voice / a second user". The account differs (AlexHe99 vs author alexhegit) but the pasted log runs on the same host and tree as the issue body (alex@SER9, /home/alex/lerobot), so independence cannot be assumed. Replaced with observables; frequency was already 1.
4. gripper-servo-very-stiff — confidence too high: the maintainer confirmed the CAUSE, nobody confirmed the FIX. "Now I'm happy to make progress more with a new one" is intent, not a retested bus. Downgraded confirmed -> reported, unverified true.
5. feetech-goal-velocity-required — wrong scope: setup-motors is IDs/baud via lerobot-setup-motors or the Feetech software; Goal_Velocity/Goal_Acceleration plus "I expected the example scripts for teleop to work" is lerobot-software. Corrected.
6. Four anchor drifts, all verified and corrected: leader.yaml 231/236 -> 230/235, parts.yaml 247 -> 246, compare.yaml cmp-wiring 27-28 -> block at 25-35, glossary.yaml 198-207 -> 197-207. printing.yaml:10-13 and parts.yaml:137-148 were exact. The pattern is why every remaining anchor was re-checked rather than trusted.
7. Two cause sentences over-claimed beyond their quote (floating-cantilever "left out of the slice"; print-infill "in the same repository") and two fields broke the one-sentence rule (faulty-follower fix, wire-groove cause). All four tightened.

WHAT THE EXTRACTION GOT RIGHT AND IS UPHELD: every verbatim quote in every review_notes exists in the file at the stated comment position (checked one by one, including both ConnectionError strings in lerobot#2819, which string-match the body exactly once tab padding is stripped); its three deliberate deviations from the cluster brief all survive (floating-cantilever's real cause is the slicer's deselected object, not the collaborator's reorientation suggestion; loose-base is setup-motors, not assembly; faulty-follower is lerobot-software and not confirmed); and its correction of the brief's claim that our data cites a YouTube leader video is right — grep of data/ for "youtu" returns nothing.

REJECTION CONSIDERED AND DECLINED: wrist-camera-folder-link-broken. It meets every mechanical acceptance criterion and is in the same posture as three other kept open entries, so removing it would be curation rather than verification, and a reviewer can drop a row far more cheaply than recover one. Its drop recommendation is preserved in review_notes and its evidence re-confirmed (grep of data/ and SOURCES.md for wrist cam / UVC / 32x32 returns nothing).

RECOMPUTED TALLIES (the extraction's were internally inconsistent — it listed five items under "confirmed 4"). Confidence: confirmed 4 (floating-cantilever-print-warning, loose-base-shoulder-joints-vibration, print-infill-13-or-30-percent, youtube-tutorial-differs-from-stl), reported 4 (faulty-follower-arm-core-dump, gripper-servo-very-stiff, wire-groove-optional-on-motor-brackets, feetech-goal-velocity-required), open 4 (leader-joints-lock-into-detents, sts3215-backlash-1-degree, wall-thickness-0-8mm, wrist-camera-folder-link-broken). unverified: true on 8 (all reported + all open). Scope: assembly 9, lerobot-software 2, setup-motors 1. frequency 1 on all twelve — this batch is singletons by construction, and every frequency equals the distinct thread count actually cited.

CRITERION 7, SLUG AND ID UNIQUENESS, CHECKED MECHANICALLY: all 12 slugs are kebab-case, 31-43 chars (limit 60), mutually distinct, and collide with nothing — compared against every slug in data/assemblies/follower.yaml, data/assemblies/leader.yaml and data/troubleshooting.yaml (the step slugs included, per commit 86a2a8f's body line "Slug uniqueness is global across steps and troubleshooting entries, not per assembly"), and against the three existing ids (setup-motors-no-response, servo5-cable-torn, wrong-leader-gear-ratio) and their slugs (setup-motors-error-check-cables, wrist-roll-servo-cable-torn, leader-servo-wrong-gear-ratio-at-joint). Ids are the cluster keys, so they cannot collide across batches either.

CRITERION 4, ID RESOLUTION, RE-RUN: every related_steps and related_parts id resolves in scratchpad/data-ids.json — steps F-002, F-012, F-013, F-014, F-022, F-041, F-061, F-062; parts rotation-pitch-so101, servo-sts3215-c001-345, servo-sts3215-12v-345, servo-horn-geared, servo-horn-plain, base-motor-holder-so101, motor-holder-so101-base, motor-holder-so101-wrist. Each mapping was additionally checked against the step's own parts list in data/assemblies/follower.yaml (e.g. F-041 really does list motor-holder-so101-wrist and carry the orientation_note "Wire groove on the harness side (Waveshare)."; F-061 really does list servo-sts3215-c001-345). Nothing was dropped as unresolvable, and the seven empty related_steps / six empty related_parts each carry their reason.

CRITERION 5, SOURCES: 24 source rows across entries and merge, all https, all retrieved '2026-09-09'. No Discord, Slack or WeChat link is used anywhere. Three public non-GitHub URLs appear inside threads and are deliberately NOT cited as sources because they were not retrieved in this corpus: robonine.com (SO-ARM100#134), youtu.be/sj4D2Y_e5aw (SO-ARM100#24) and the user-attachments screenshots (SO-ARM100#175, #64). The YouTube playlist in SO-ARM100#29 appears inside the fix sentence because the reporter's cited comment is the source for it — and see needs_human for why that link should not be published unchecked.

ALIAS PROVENANCE, added to every entry because the ticket asks for "verbatim where possible": roughly half the aliases in this batch are editorial search phrasings that appear in no thread (e.g. "arm shakes when teleoperating", "print service says wall thickness too thin", "my bracket has no slot for the cable"). They are kept — aliases are search keys, and the search box is the point of the ticket — but each entry's review_notes now says which aliases are verbatim source text and which are editorial, so a reviewer is never misled into thinking a builder typed them.

GIT QUERIES, run from /Users/macbook/Developer/so101-assembly-guide/.claude/worktrees/troubleshooting-candidates. Tier 1, `git log --oneline -S"troubleshooting"`: 12 commits, the only data-owning one 86a2a8f "data(schema): step slug, troubleshooting.yaml, vendors.yaml per ADR-0002". Tier 2, `git log --grep='^Constraint:' --grep='^Directive:' --grep='^Rejected:' -- data/troubleshooting.yaml data/schema/troubleshooting.json`: exactly one trailer, quoted verbatim — "Constraint: slug is frozen at first publish; changing one is a URL break, not a rename" (commit 86a2a8f). That is why the servo5 cluster stays a merge with no id or slug change.

NOT FOLDED IN, for whoever owns the neighbouring batches: lerobot#2819 comments 4 and 5 (positions 4 and 5, same reporter, duplicate posts) describe a second, unrelated incident — the robot left in a car overnight in winter — whose symptom is `lerobot-calibrate` failing on BOTH ports with "RuntimeError: FeetechMotorsBus motor check failed" and an empty found-motor list; a distinct candidate with no answer in the record. Also note for merge time: "[TxRxResult] Incorrect status packet!" is generic and likely titles a bus/wiring cluster in another batch — check for a title collision with gripper-servo-very-stiff's aliases before publishing.

NO FILES WRITTEN. data/troubleshooting.yaml, data/schema/ and everything the build reads are untouched; repo files were opened read-only (data/assemblies/follower.yaml, data/assemblies/leader.yaml, data/parts.yaml, data/printing.yaml, data/compare.yaml, data/glossary.yaml, SOURCES.md) solely to re-verify the extraction's file:line anchors, which is what caught the four drifts above.

- batch 6 verify: ADVERSARIAL VERIFICATION of batch 6/9. Every cited thread file was re-opened in full (corpus/lerobot/{3585,1010,1595,1694,3345,1779,2116,3244}.json, corpus/so-arm100/{66,158}.json, corpus/wiki/seeed-so100m-new.md) and every quote in every review_notes was checked against the file at the stated comment position. Outcome: 11 entries in, 11 entries out — 9 FIX, 2 KEEP, 0 REJECT — plus 1 merge corrected, 1 skip upheld, 5 needs_human (1 new), 1 discrepancy (new), 3 standing drop recommendations.

ZERO REJECTS IS THE CORRECT OUTCOME, not a soft pass. The reject trigger in this run is a fix that cannot be traced to a public comment AND an entry that is not salvageable as unverified. Every entry here either has a fix traceable to a quoted comment (1010, 1595+1694, 1694 comment 5, the two wiki entries) or already carries the canonical open sentence with unverified: true (3585, 1779, 3345, 2116) or an explicitly attributed unconfirmed proposal (SO-ARM100 #66, 3244). The corrections were therefore surgical rather than structural — but four of them removed claims that did not exist in the record, which is what this pass was for.

THE FOUR SUBSTANTIVE DEFECTS FOUND (each an unsupported claim, now removed):

1. incorrect-status-packet — the fix offered the Kotakku FT_SCServo_Debug_Qt build as a way to bring servos to the same firmware version on Linux and macOS. The record says the opposite: comment 6 (CarolinePascal, COLLABORATOR) offers that tool only "to debug Feetech servos" and its P.S. reads "Currently investigating a solution for firmware updates !", and comment 9 states "So far, the only _easy_ way to upgrade the firmware of Feetech servos is to use [Feetech's utility] on a Windows virtual machine." A builder following the extracted fix would have been sent to a tool that cannot do the job.
2. incorrect-status-packet — the fix said to power 7.4V motors "from the 7.4V/5V supply". "5V" appears nowhere in the thread; it is our own BOM's part name (data/parts.yaml power-supply-5v) leaking into a sourced sentence. The source says only "powering the motors at 7.4V". Also corrected: `sudo chmod 666 /dev/ttyACM*` is the Seeed wiki's glob form, not the form in comments 18 and 20.
3. calibration-works-on-old-version-not-new — the cause ended "...so no motor answered the handshake". No comment says that, and nothing in the thread explains why a single dead motor would silence all six IDs. Inferred mechanism removed.
4. act-state-delta-timesteps-inconsistent — the cause bridged two quoted facts with "so ACT's forward concatenates a 3-dimensional state with a 2-dimensional one and raises in torch.cat". Nobody states that mechanism; the two facts now stand side by side without the bridge. (The same discipline was applied to the merge's symptom, which had invented "so the next motor does not answer when Enter is pressed".)

OTHER CORRECTIONS: confidence downgrade on no-signal-from-servo5 (vendor wiki is not a reporter or maintainer under this run's definition — see needs_human); scope flips incorrect-status-packet setup-motors -> calibration (every cited report hits the error inside lerobot-calibrate; routing consequence flagged for Guillaume) and eye-to-hand-calibration-large-error calibration -> lerobot-software (hand-eye is OpenCV camera extrinsics; the enum reserves calibration for lerobot-calibrate/range/homing and puts cameras under lerobot-software); title shortened to near-verbatim on shoulder-lift-motor-overload; "both reporters confirmed" softened on calibrate-robot-type-vs-teleop-type to say exactly who confirmed what, with issue 1694's thread URL added as a source row so frequency: 2 is citable on its own; one verbatim-quote repair on goal-position-outside-calibrated-band ("~75°" not "~75 degrees"); an invented alias dropped from the merge. KEEPs with no change: find-joint-limits-config-fails, bi-so-follower-cameras-not-supported.

MECHANICAL CHECKS, all passing. Ids/slugs: all 11 ids and slugs match ^[a-z0-9]+(-[a-z0-9]+)*$, all slugs <= 60 chars (longest 40), all unique within the batch, none colliding with the published ids (setup-motors-no-response, servo5-cable-torn, wrong-leader-gear-ratio) or slugs (setup-motors-error-check-cables, wrist-roll-servo-cable-torn, leader-servo-wrong-gear-ratio-at-joint). References: every related_steps and related_parts id was checked against data-ids.json and resolves — steps F-002, L-002, F-021, F-050, L-050; parts motor-control-board, power-supply-5v, servo-sts3215-c001-345. Six entries carry empty arrays on purpose (our data has no calibration or software step), with the reason in each review_notes; where a step id IS cited on a software/calibration entry it is flagged as a cross-link target, not as where the symptom arises. Sourcing: 29 source rows across entries and the merge, every one an https thread URL, comment permalink or the Seeed wiki URL, every one retrieved '2026-09-09'; no Discord, Slack, WeChat or login-gated link appears in any ref, and none of these threads offered one. Three third-party URLs encountered in the record are deliberately NOT cited and live only inside review_notes: the Makerworld SO-100 mirror (licence unverified, CLAUDE.md rule 3), github.com/SergioMOrozco/lerobot_3d (a commenter's self-promoted project), and the reporter's fork branch on issue 3244. Titles: all verbatim from a source except two constructed search phrases where no error string exists (shoulder-lift-motor-overload, eye-to-hand-calibration-large-error), which the schema permits and which are now flagged as constructed in their review_notes. Cause and fix are one sentence each throughout; symptoms quote the builder where a builder exists. Frequencies equal the distinct threads actually cited (all 1 except calibrate-robot-type-vs-teleop-type at 2).

WIKI CONTEXT CLAIMS INDEPENDENTLY CONFIRMED: all three quoted wiki passages exist verbatim at corpus/wiki/seeed-so100m-new.md lines 736 (power-supply tip, inside the motor-ID setup walk-through right after "Connect the controller board to the 'wrist_roll' motor only and press enter."), 878 (servo-5 tip, right after the follower lerobot-calibrate command and before "Manual calibration of leader arm") and 323 (3D Printing Guide :::caution). The file has no anchors matching the inventory's #cabling-check-each-step / #servo5-signal-quirk / #3d-printing-caution — those are inventory labels, correctly not appended; the bare URL matches SOURCES.md row 24 exactly.

SKIP UPHELD (readme-two-arm-servo-count-inconsistent, TheRobotStudio/SO-ARM100#158) — re-verified independently, and it is a correct skip. The thread title is "Typo in README ("Parts For Two Arms")", there are zero comments, and the reporter opened and closed it as completed himself; the table he read is headed "Parts For Two Arms", so 12 is the two-arm total and nothing is inconsistent. Our data already agrees with upstream: data/parts.yaml gives servo-sts3215-c001-345 qty 7 (line 337), servo-sts3215-c044-191 qty 2 (366), servo-sts3215-c046-147 qty 3 (391) = 12 servos for both arms, matching data/servos.yaml's joint map (6 follower 1:345; leader 2x 1:191, 1x 1:345, 3x 1:147). No troubleshooting entry, no discrepancies.md row. If Guillaume wants to serve that search it belongs in /faq or /kits, not troubleshooting.

DISCREPANCIES — one row, and one deliberate non-row. The row is the servo-5 conflict with the PUBLISHED entry servo5-cable-torn in data/troubleshooting.yaml: vendor wiki says no signal from servo 5 is normal, our live page says motor 5 not answering is a torn wrist cable. The non-row is the voltage question on issue 1010: 7.4V motors fed 12V, and comment 7's "the motor description says to use 12V instead of 7.4V", are a field instance of data/discrepancies.md D-002 ("Follower power supply voltage"), which is already RESOLVED in favour of upstream (5V supply for 7.4V servos; 12V only for the 12V servo variant) — a source agreeing with our resolution's hazard is not a source disagreeing with our data, so filing it would invite re-opening a settled row. It is recorded instead as a cross-link: incorrect-status-packet's review_notes point power-supply-5v at D-002. Similarly checked and not filed: so100-stl-files-removed (nothing in data/ offers SO-100 source files as a download route; data/compare.yaml calls SO-100 deprecated citing the pinned upstream SO100.md, and 'deprecated' does not contradict 'to be deleted'); and the firmware-version claim in 1010 (no data/ file states any servo firmware version, so there is nothing to contradict). The gear-ratio and joint-mapping facts these threads touch (1:345 at follower joints, motor 5 = wrist roll, bus id 1 = shoulder_pan) match data/servos.yaml and data/parts.yaml.

GIT QUERIES RUN, from /Users/macbook/Developer/so101-assembly-guide/.claude/worktrees/troubleshooting-candidates and before reading any data file. Tier 1: `git log --oneline -S"troubleshooting"` (12 commits; relevant: 86a2a8f "data(schema): step slug, troubleshooting.yaml, vendors.yaml per ADR-0002" and e19f06e "data: three assembly troubleshooting entries, ten kit vendors") and `git log --oneline -S"related_steps"` (6 commits, same two plus 41fad95). Tier 2: `git log --grep='^Constraint:' --grep='^Directive:' --grep='^Rejected:' -- data/troubleshooting.yaml` surfaced exactly one trailer, quoted verbatim: "Constraint: slug is frozen at first publish; changing one is a URL break, not a rename" (86a2a8f). Same commit body, verbatim: "Slug uniqueness is global across steps and troubleshooting entries, not per assembly, so a slug can never collide when pages are cross-linked" and "troubleshooting.related_steps/related_parts and vendors.part_map are cross-checked in validate-lib like every other reference." `git log --grep=... --oneline -- data/assemblies/` surfaced no trailers. Consequences applied: the uniqueness and <=60-char checks above were run mechanically against the published schema's own pattern (data/schema/troubleshooting.json: slug pattern ^[a-z0-9]+(-[a-z0-9]+)*$, maxLength 60; required id, slug, title, symptoms, cause, fix, source; additionalProperties false — so the four review fields must be stripped at promotion); and because slugs freeze at first publish, every slug here is a PROPOSAL, cheap to change now and a URL break later — which is why the id rename on calibration-works-on-old-version-not-new must happen before promotion, not after. NO FILE UNDER THE REPO WAS WRITTEN OR MODIFIED in this run: data/troubleshooting.yaml, data/schema/troubleshooting.json, data/parts.yaml, data/compare.yaml, data/discrepancies.md and SOURCES.md were read only, and this agent returns data rather than writing to data/staging/.

FOR PHASE 5 ASSEMBLY. Counts by scope after the verifier's two flips: lerobot-software 5 (goal-position-outside-calibrated-band, shoulder-lift-motor-overload, eye-to-hand-calibration-large-error, act-state-delta-timesteps-inconsistent, bi-so-follower-cameras-not-supported), calibration 5 (incorrect-status-packet, calibrate-robot-type-vs-teleop-type, calibration-works-on-old-version-not-new, find-joint-limits-config-fails, no-signal-from-servo5-during-calibration), sourcing 1 (so100-stl-files-removed), setup-motors 0, assembly 0, firmware 0. By confidence: confirmed 3 (incorrect-status-packet, calibrate-robot-type-vs-teleop-type, calibration-works-on-old-version-not-new), reported 4 (shoulder-lift-motor-overload, no-signal-from-servo5-during-calibration, so100-stl-files-removed, bi-so-follower-cameras-not-supported), open 4 (goal-position-outside-calibrated-band, find-joint-limits-config-fails, eye-to-hand-calibration-large-error, act-state-delta-timesteps-inconsistent); unverified: true on 8 (one more than the extraction, from the servo-5 downgrade). ZERO entries are scoped `assembly`, so on a strict v1 reading this whole batch is post-v1 material — which is itself evidence for the ticket's open decision 1. CROSS-BATCH WARNING: issue 1694 is cited twice by design (its own motor-check entry, and comments 3-4 inside calibrate-robot-type-vs-teleop-type); de-duplicate at assembly if another batch mined it. WHAT WOULD CLEAR THE 8 unverified FLAGS: 3585 and 1779 need any maintainer reply (3585 also deserves a safety warning while open, since it reports torque re-enabling after an explicit disable_torque); SO-ARM100#66 needs the reporter to say whether the fault followed the motor; 3345 needs a cause from anyone; 2116 needs a merged fix (the behaviour is already maintainer-confirmed on 0.5.1); 3244 needs the reopen decision; no-signal-from-servo5 needs an upstream lerobot commit or issue naming the change that silences servo 5, or a maintainer confirming the behaviour; so100-stl-files-removed needs the Makerworld licence checked and the deletion confirmed against TheRobotStudio. STANDING DROP RECOMMENDATIONS beyond the five needs_human items: eye-to-hand-calibration-large-error (camera extrinsics, outside even the named post-v1 calibration phase), act-state-delta-timesteps-inconsistent (policy training, nothing a builder of the arm hits), bi-so-follower-cameras-not-supported (bimanual recording). All three were kept rather than rejected because the ticket says to extract and tag calibration/software clusters and let Guillaume decide scope. shoulder-lift-motor-overload is an SO-100 report and must say so if published.

- batch 7 verify: BATCH 7/9 ADVERSARIAL VERIFICATION — 13 entries in, 13 out. Verdicts: 8 keep, 5 fix, 0 reject. 0 merges. No files written anywhere in the repo; data/troubleshooting.yaml, data/schema/ and data/assemblies/ were read only.

GIT QUERIES (run from /Users/macbook/Developer/so101-assembly-guide/.claude/worktrees/troubleshooting-candidates before touching any data file). Tier 1: `git log --oneline -S"troubleshooting"` -> 12 commits, relevant one 86a2a8f "data(schema): step slug, troubleshooting.yaml, vendors.yaml per ADR-0002". Tier 2: `git log --grep='^Constraint:' --grep='^Directive:' --grep='^Rejected:' -- data/troubleshooting.yaml data/schema/troubleshooting.json` -> one trailer, quoted verbatim: "Constraint: slug is frozen at first publish; changing one is a URL break, not a rename". Its body also states verbatim: "Slug uniqueness is global across steps and troubleshooting entries, not per assembly, so a slug can never collide when pages are cross-linked" and "troubleshooting.related_steps/related_parts and vendors.part_map are cross-checked in validate-lib like every other reference". Note for whoever re-runs this: `git log data/troubleshooting.yaml --grep=...` fails with "fatal: option '--grep=^Constraint:' must come before non-option arguments" — the pathspec must follow `--`.

MECHANICAL SWEEP, all 13 entries, all PASS:

- Every "comment N by <author> (<association>)" claim matches comments[N-1] in the cited file. Every cited comment permalink equals that comment's .url. No mis-attribution found anywhere.
- Every "the reporter" attribution equals the thread's top-level author. The two load-bearing ones verified individually: ccha425 (1738 comment 4) IS the thread author, so illegal-instruction-raspberry-pi's `confirmed` really does rest on an OP confirmation even though the fix proposer is NONE; nomorewzx (so-arm100/26 comment 1) IS the thread author, so "self-answering" is correct. talregev (2246 comment 15) and prrw (3234 comment 2) likewise.
- 2748.json has comment_count 0 and comments [], so leisaac-assets-access-denied's `open` verdict is correct rather than a missed reply.
- Every source[].retrieved == '2026-09-09'. No Discord/Slack/WeChat or login-only URL in any source field on any entry. One Discord lead exists in the record (the sole reply on the GR00T forum topic sends the reporter there) and is confined to review_notes and needs_human, never used as a fix.
- Slugs: 13 candidate slugs checked against all 52 step slugs in data/assemblies/{follower,leader}.yaml (no collision), against each other (no duplicates), and against the 3 existing troubleshooting ids and slugs (no collision). All kebab-case, all <= 60 chars. This discharges the frozen-slug Constraint above.
- Ids: all 13 kebab-case, unique, distinct from setup-motors-no-response / servo5-cable-torn / wrong-leader-gear-ratio, and each equals its cluster key as the batch brief required.
- Wiki citations re-grepped rather than trusted: corpus/wiki/seeed-so100m-new.md:543 (evdev tip), :548-549 (apt commands), :554 ("After installation, rerun the LeRobot installation command."), :1810 (`File exists` note 3), :2905-2910 (FAQ restatement). Text verbatim at every claimed line. This was the highest confabulation risk in the batch — wiki prose has no comment structure to anchor it — and nothing was invented.
- Titles: every entry's title string re-found verbatim in its source, or confirmed to be the exact phrase a builder would type where no error string exists (groot-torch-version-conflict, evdev-build-fails-fresh-ubuntu, gripper-present-load-very-high, keyboard-teleop-sync-write-crash). The extractor's three deliberate title overrides of cluster names are all correct per rule 3: lerobot#1271 and #2155 do contain the verbatim ValueError/AttributeError in their bodies, and #2748's traceback line beats the generic "Access Denied".
- related_steps and related_parts are empty on all 13, so no id needed resolving. Spot-checked the two drops the extractor reported: F-002/L-002 and F-061/F-062/F-063 and servo-sts3215-c001-345 all DO exist in data-ids.json, so these were genuine considered-and-dropped decisions, not invented-then-removed ids. Both drops are right — nothing in either thread ties the software cause to a physical step.
- frequency is 1 on all 13 and each entry cites exactly one thread or one wiki page, so frequency equals the distinct thread count actually cited.

THE FIVE CORRECTIONS (all applied inline in `entries`):

1. corrupt-jpeg-data — the `fix` field stated non-confirmation ("Not confirmed — a contributor suggests...") rather than the proposed fix. Non-confirmation is already carried by confidence `reported` and unverified:true, so the field now states the proposal with its caveat. confidence/unverified unchanged.
2. eval-policy-on-real-robot-fails — the `cause` asserted "because its policy/teleoperator check evaluated False==False" as established fact; the reporter only wrote "Upon verification, it seemed to be False==False here." Reattributed to the reporter, line range added, still one sentence, confidence unchanged.
3. evdev-build-fails-fresh-ubuntu — the `cause` hardened the wiki's "gcc and other C build tools MAY not be installed by default" into "are not installed by default". "may not be" restored.
4. eval-feature-autodetect-regression — review_notes now records that comment 2 names lerobot#2247 as another report of the same problem and that #2247 was not in this batch and has not been read. Strongest find of the pass; also folded into needs_human so a reviewer scanning that list sees a cited duplicate thread went unread.
5. illegal-instruction-raspberry-pi — review_notes quoted comment 6 without a permalink; added https://github.com/huggingface/lerobot/issues/1738#issuecomment-3368003220 (jamminc, NONE).

WHY NOTHING WAS REJECTED. The default-to-reject rule bites when a fix cannot be traced to a public comment AND the entry is not salvageable as unverified. Every fix in this batch traces to a cited public comment or a cited wiki line, and the six weak ones were already salvaged as unverified:true (2 reported, 4 open). Three entries carry the extractor's "recommend dropping at review" note for low v1 relevance — leisaac-assets-access-denied, groot-torch-version-conflict, gym-manipulator-sim-imitation-error — and I deliberately did NOT reject them: the ticket says "Scope tags, not scope decisions... whether they enter v1 is a decision Guillaume makes at review. Do not skip them." The notes are preserved so he can drop them in one pass.

THE ONE JUDGMENT CALL I MADE, stated plainly so it can be overruled. The two Seeed-wiki entries (eval-output-dir-file-exists, evdev-build-fails-fresh-ubuntu) are KEPT at confidence `confirmed` / unverified:false. The strict reading of the brief's rule 3 would downgrade both, because the brief defines "maintainer" as MEMBER/COLLABORATOR/OWNER/CONTRIBUTOR on huggingface/lerobot or TheRobotStudio/SO-ARM100, and a vendor wiki author holds no such association. I judged that definition to be a rule for reading GitHub author-association metadata, not an exhaustive test that a documentation page can fail — a published vendor page has no reporter/maintainer axis at all — and data/troubleshooting.yaml already cites this same wiki URL at unverified:false for wrong-leader-gear-ratio. Downgrading "run `sudo apt install build-essential` when gcc is missing" to unverified:true would noindex and flag a correct, well-sourced step. Both entries are in needs_human so the source-class policy gets decided once and applied to both identically rather than being settled silently here.

Scope check against the enum, done independently of the extractor: 12 lerobot-software, 1 firmware. evdev-build-fails-fresh-ubuntu stays lerobot-software (install) despite the word "feetech" in the failing command — `setup-motors` is setting IDs or baud with lerobot-setup-motors or the Feetech software, which this is not. circular-import-teleoperator stays lerobot-software rather than calibration — it is an import error from a broken PyPI release that happens to surface during calibration, not lerobot-calibrate behaviour. gripper-present-load-very-high stays firmware (Feetech servo register semantics); it is the best of the six enum values but fits none cleanly, and that is in needs_human. Zero assembly / setup-motors / calibration / sourcing entries in this batch — it is the software tail of the corpus, which matters for the v1-scope decision the ticket puts to Guillaume: if calibration and lerobot-software stay out of v1, twelve of these thirteen do not ship.

Confidence after verification: confirmed 7 (circular-import-teleoperator, eval-feature-autodetect-regression, eval-output-dir-file-exists, eval-policy-on-real-robot-fails, evdev-build-fails-fresh-ubuntu, find-cameras-read-thread-race, illegal-instruction-raspberry-pi), reported 2 (corrupt-jpeg-data, gripper-present-load-very-high), open 4 (groot-torch-version-conflict, gym-manipulator-sim-imitation-error, keyboard-teleop-sync-write-crash, leisaac-assets-access-denied). unverified:true on 6 (the 2 reported + the 4 open). The extractor's own notes miscounted confirmed as "6" then corrected to "7 in fact" in the same sentence; 7 is right.

DISCREPANCIES: empty, and that is a verdict rather than an omission. I checked every data-adjacent statement in the batch against data/. The only one is the maintainer's "smolvla_base model was trained for a 6-dimensional action space (SO-101 arm)" in lerobot#2246 comment 12, which agrees with data/servos.yaml (six joints, bus ids 1-6 per arm). The Seeed wiki uses `--robot.type=so100_follower` in some commands and `so101_follower` in others, but that is one page covering both arms, not a contradiction of anything in data/. Nothing in the thirteen threads contradicts a part id, step id, gear ratio or bus id in the repo.

VERSION-BOUND ENTRIES a reviewer must keep pinned, or they read as permanent faults: circular-import-teleoperator (lerobot 0.4.3 PyPI, fixed by PR #2795), eval-feature-autodetect-regression (regression from PR #1771, addressed by #2265/#2278), find-cameras-read-thread-race (0.5.2, fixed by #3812), eval-policy-on-real-robot-fails (June 2025, pre-src/ layout, fixed by #1284), illegal-instruction-raspberry-pi (August 2025; the `pyarrow==20.0.0` pin may itself be stale against current wheels — consider softening to "a pyarrow build matching the Pi's architecture, e.g. pyarrow==20.0.0"). Four of the five are already fixed upstream, which is worth weighing before publishing pages for them.

Input corpus as retrieved 2026-09-09, all re-opened in this pass: /private/tmp/claude-501/-Users-macbook-Developer-so101-assembly-guide/d833fc3b-d8e2-4870-870f-3609424bae0e/scratchpad/corpus/lerobot/{2848,1487,2246,1271,3569,2155,1738,3234,2748}.json, corpus/so-arm100/26.json, corpus/wiki/seeed-so100m-new.md, and scratchpad/hf/176500.json. A durable copy of this verification summary is at /private/tmp/claude-501/-Users-macbook-Developer-so101-assembly-guide/d833fc3b-d8e2-4870-870f-3609424bae0e/scratchpad/batch7-verification.md. The extracted JSON's `skipped` key is not part of the output schema and was empty; nothing was skipped.

- batch 8 verify: VERIFICATION PASS, batch 8/9, adversarial. All 13 cited threads re-opened in full from the pre-fetched corpus and every quote in every review_notes checked against the file at the stated position: 13 GitHub/forum/wiki sources, 0 missing quotes, 0 fabricated comment positions, 0 broken permalinks (each cited `#issuecomment-<id>` matches the `comments[].url` of the comment whose text is quoted). Verdicts: 4 keep, 9 fix, 0 reject. No merges (nothing in this batch touches the three existing assembly entries). No entry was rejected: every cause/fix traces to a cited public comment or is correctly marked open/unverified, which is the ticket's bar — the batch is extracted-and-tagged software corpus, not v1 content.

SUBSTANTIVE CORRECTIONS MADE (not cosmetic): (1) robot-client-unrecognized-robot-type — confidence reported → open; its `fix` was a narration of "Fixed now, thanks!", and the thread's only actionable proposal ("be on latest `main`") is explicitly refuted by the reporter in comment 2. (2) max-relative-target-typeerror — `fix` said "upgrade to a lerobot version containing that PR"; no comment says PR #1837 merged or names a release, so the fix now states only what pkooij (MEMBER) wrote and that no builder-side workaround exists. (3) policy-repo-id-missing — cause attributed prose to comment 2 that the comment does not contain (it gives the flag plus a permalink only); re-anchored, and review_notes now records that the ValueError string exists ONLY in the thread title because the issue body's image failed to upload (`<!-- Failed to upload ... .png -->`) and comment 1 is a screenshot. (4) orbbec-camera-timeout — cause called the frame timeout "a transient camera-connection problem", which the wiki never states. (5) lerobot-eval-camera-resolution-forced — cause asserted the FFMPEG/V4L2 backend where post 4 only hedges. (6) record-no-policy-or-teleoperator-warning — dropped an invented symptom (the warning's own third sentence presented as something the builder observed). (7) raspberry-pi-4-camera-error — the reproduction command was silently corrected from the body's `erobot-teleoperate` to `lerobot-teleoperate`; now quoted as the source types it. (8) multi-gpu-tensor-device-mismatch — kept `confirmed` but review_notes now states plainly that no comment says the workaround worked ("Here is a workaround" / "quick fix" / "My gpu stuck so I can't test"), so Guillaume can downgrade to `reported` without re-reading.

ONE PLANNED CHANGE DELIBERATELY NOT MADE: record-loop-slower-than-target-fps keeps its instance-specific title ("...slower (5.6 Hz) than the target FPS (30.0 Hz)..."). De-instancing it would have produced a title that appears verbatim in no cited source (post 2 quotes the template only as "Record loop is running slower (...) than the target FPS (...)"), breaking the batch's own rule 1; alias 1 already carries the generic phrasing, which is what the site's search box needs. Recorded in that entry's review_notes so it is not re-litigated.

MECHANICAL CHECKS, all 13 entries: ids and slugs are kebab-case, unique among themselves (13/13), and collide with none of setup-motors-no-response / servo5-cable-torn / wrong-leader-gear-ratio nor their slugs; longest id 40 chars, longest slug 48 (schema cap 60, pattern `^[a-z0-9]+(-[a-z0-9]+)*$` — verified by script). Every source is https and carries retrieved '2026-09-09'; hosts are github.com, discuss.huggingface.co and wiki.seeedstudio.com only — no Discord, Slack, WeChat or login-gated link anywhere (the one Discord pointer, post 2 of HF 176555 recommending the LeRobot Discord, is recorded in review_notes only). Every `cause` and every `fix` is one sentence. Every entry has >= 1 symptom. Scope: all 13 are genuinely lerobot-software under the enum — install (libtiff/cv2), teleop/record/train/eval CLI, cameras, python errors; none is assembly, setup-motors, calibration, sourcing or firmware, so no scope tag was changed. frequency = 1 on all 13 and each cites exactly one distinct thread/page, so frequency equals the distinct thread count cited. related_steps and related_parts are empty on all 13 (nothing invented): data-ids.json holds 52 steps (F-/L-xxx) and 26 parts, and no step or part resolves to a host-software failure — notably there is no camera, PC or non-3-pin-cable part in the BOM, so the Orbbec and Pi entries have nothing to link.

EVIDENCE HYGIENE. github-actions[bot] carries author_association CONTRIBUTOR but is never cited as confirmation; in lerobotdataset-dataclass-typeerror and raspberry-pi-4-camera-error `state: closed` means closed-as-stale by that bot, not fixed. On the Hugging Face forum, John6666 is trust_level 3 with staff=false, admin=false, moderator=false (verified in both thread JSONs) and is NOT a lerobot maintainer; both HF-sourced entries say so inline in the source notes. Maintainer-status calls used: imstevenpmwork COLLABORATOR, tc-huang COLLABORATOR, pkooij MEMBER, NikodemBartnik MEMBER, danaaubakirova CONTRIBUTOR, jadechoghari CONTRIBUTOR; milong26, hachimada, Yyl-Sylmi, wzhaofeng, xk21520, ylyhlh, asyk454, raul-machine-learning, Kev-Park, jfmateos, 321915514, qinglin-ai all NONE.

GIT QUERIES (run in /Users/macbook/Developer/so101-assembly-guide/.claude/worktrees/troubleshooting-candidates). Tier-1: `git log --oneline -S"troubleshooting"` returns 12 commits of 103 on the branch. Tier-2 trailer greps, quoted verbatim — for data/troubleshooting.yaml: "Constraint: slug is frozen at first publish; changing one is a URL break, not a rename". For data/schema: "Constraint: ids are stable and never reused"; "Constraint: slug is frozen at first publish; changing one is a URL break, not a rename"; "Constraint: a page dataset row without a source fails validation like any other row"; "Constraint: a plain sentence is a rewording; every number in it must exist in a field or source its `from` names"; "Constraint: glossary kind: fact needs a source; kind: definition is general meaning only, never a kit fact"; "Constraint: cable paths live in data (cables.yaml); renderers read them, never derive them"; "Constraint: derived cable entries are overwritten on regeneration; hand-authored ones (derived: false) win"; "Constraint: provenance is never rendered inside the instruction panel"; "Constraint: title_short must name the same parts and action as title; it is a rewording, never a new fact"; "Directive: add a field to the schema before the viewer reads it, never the reverse"; "Directive: when adding a `from` pointer kind, extend STEP_FIELDS or the record map in scripts/src/plain.ts and the schema pattern in the same commit". Consequences honoured: ids/slugs verified unique and non-colliding before they freeze (and the one entry whose id and title disagree is escalated in needs_human rather than silently renamed), every entry carries at least one source, and no schema field was invented — the four review fields are the ticket's, stripped at promotion.

TWO CORRECTIONS TO THE EXTRACTOR'S OWN GIT CLAIMS. (a) It said e19f06e "created the data file"; 86a2a8f added data/troubleshooting.yaml (verified with `git log --diff-filter=A`) and e19f06e "data: three assembly troubleshooting entries, ten kit vendors" added the three entries. Both SHAs exist and e19f06e is an ancestor of HEAD (`git merge-base --is-ancestor`). (b) Pickaxe hygiene worth passing on: `git log --oneline -S"troubleshooting"` does NOT surface e19f06e at all, because the added entry lines do not contain the literal string — so that pickaxe is an incomplete census of this file's history, and `git log --oneline --diff-filter=A -- data/troubleshooting.yaml` plus a path-scoped log is what actually finds the authoring commits. e973c5b and c6f3f02, the two other SHAs the extractor quoted trailers from, both exist and carry those trailers.

NO FILES WRITTEN. Nothing under the repo was created or modified by this verification agent — not data/troubleshooting.yaml, not data/schema/, not data/staging/; the only repo reads were data/troubleshooting.yaml, data/schema/troubleshooting.json and SOURCES.md, plus git history. Scratch helpers live in the session scratchpad (dumpthread.py, dumphf.py, chk.py).

CARRY-FORWARD FOR PHASE 5. All 13 are scope lerobot-software and frequency 1, so none is in v1 scope as CLAUDE.md stands — they all ride on the ticket's open decision 1. Final confidence split after verdicts: confirmed / unverified:false (5) — lerobot-teleoperate-module-moved, multi-gpu-tensor-device-mismatch, policy-repo-id-missing, record-no-policy-or-teleoperator-warning, orbbec-camera-timeout, record-resume-num-episodes-semantics (6 entries; note multi-gpu's confirmed is flagged as downgradable). reported / unverified:true (2) — lerobot-eval-camera-resolution-forced, max-relative-target-typeerror. open / unverified:true (5) — lerobotdataset-dataclass-typeerror, libtiff-undefined-symbol-jpeg12, raspberry-pi-4-camera-error, record-loop-slower-than-target-fps, robot-client-unrecognized-robot-type. What would clear each flag: HF 176555 — a maintainer reply naming which command forces the resolution, or the reporter confirming the extras/V4L2 fix. lerobot#1527 — anyone confirming that a release containing PR #1837 accepts an int `max_relative_target`. lerobot#3078 — the commit or version that resolved it. lerobot#1678 — any human diagnosis of the `datasets` feature-metadata TypeError. SO-ARM100#34 — any reply, or a conda channel/ordering confirmed to avoid the libtiff/libjpeg symbol mismatch. lerobot#1699 — a reply, or a confirmed way to run `--display_data=true` on a Pi 4. HF 178517 — the full warning source path plus which of the three named causes applied on that machine. discrepancies is empty on purpose: nothing in 13 software threads touches a servo gear ratio, joint mapping, step id or part id in data/, so there is no data_location to point at; the one real source conflict is internal to the Seeed wiki and is raised in needs_human instead.

- batch 9 verify: Adversarial verification of batch 9/9 (nine singleton clusters, all lerobot-software). Every one of the nine thread files was re-opened in full and every quoted string, comment position, author, author_association and permalink id in the extracted review_notes was checked against the file. Result: 4 keep, 5 fix, 0 reject. Nothing was rejected because every entry either traces to a public comment or is honestly gradable as reported/open, which the ticket explicitly allows.

FIVE DEFECTS FOUND AND CORRECTED

1. unrecognized-argument-policy-path (issue 1111) — the worst of the batch, and the only confidence downgrade. Its cause asserted that draccus "accepts only --flag=value"; no comment says that (the reporter guesses "Maybe it's about parse?"). Verified by grep over the thread: the usage block pasted for HIS commit (42bf1e8b) lists `[--control.policy.checkpoint_path str]` and `[--policy str]` but NO `--control.policy.path`, and comment 3 shows him adding `pretrained_path: str = None` to `lerobot/configs/policies.py` to get a path through at all — so the titled error plausibly names a flag that did not exist in that version, which rewriting with `=` would not fix. Combined with comment 5 ("Maybe I used the wrong version. I'll check it") being an explicit non-confirmation, the fix is confirmed only for a DIFFERENT failure (`train.py --policy.path`, commit 2b71789e). confirmed → reported, unverified false → true; both readings now stated in the cause. Comment 5 added as a source (it was missing).
2. unboundlocalerror-action-in-record (1097) — fix ended "so the current lerobot-record entry point replaces this code path". Unsourced; the only sourced statement is "Closing this as it `control_robot.py` is deprecated". Removed.
3. smolvla-stale-local-files-optim (1742) — fix said the issue was solved "once he did" update the files. The reporter never describes performing an update ("Argh issue solved as I did not update the smolvla files"). Rewritten as attribution, not action.
4. windows-temp-permission-denied (1590) — title contained `tmp8845bk36`, a per-run random token no other builder will ever type or see, so it failed the "exact string as the user sees it" rule on its own terms. Title is now the generic shape, the run-specific string moved to symptoms, and the verbatim stem "PermissionError: [Errno 13] Permission denied" added as the first alias. The reporter's Windows username stays redacted in every field.
5. robot-disconnect-not-called-on-exception (2621) — slug `record-exit-leaves-motors-torqued` encoded an inference ("torqued") that no comment states; now `record-exit-motors-not-disconnected`. Nothing is published, so the frozen-slug constraint is not breached; flagged so the assembler does not read it as a URL break.

FINAL SPLIT AFTER VERDICTS: confirmed 4 (robot-disconnect-not-called-on-exception, rtc-inference-delay-kwarg-on-act, smolvla-stale-local-files-optim, wrist-camera-glitchy-in-record), reported 3 (smolvla-lora-alpha-defaults-to-8, unboundlocalerror-action-in-record, unrecognized-argument-policy-path), open 2 (vla-jepa-hardcoded-cond-tap, windows-temp-permission-denied). unverified: true on all five non-confirmed entries. The extracted notes' own count was self-contradictory ("confirmed 4" while naming five) and is superseded by this line.

MERGES: empty, and verified rather than assumed. All nine threads are software defects (record/rollout/train entry points, PEFT config, camera frame rate); the three existing ids are setup-motors-no-response (motor ID setting over the serial bus), servo5-cable-torn (physical wrist assembly) and wrong-leader-gear-ratio (servo selection). No symptom, alias or error string in these nine overlaps any of the three, so there is nothing to fold in as an alias or symptom.

DISCREPANCIES: empty by verification, not by omission. The field means conflicts with what is already in `data/`; I checked every candidate against data-ids.json (52 steps F-001..L-071, 26 parts, 12 servo rows, 3 existing entries) and none of these entries asserts anything about a step, part, servo, gear ratio or fastener, so there is nothing in `data/` to contradict. The conflicts I did find are source-internal (1111's two competing causes, 1606's two builders' opposite remedies) or repo-rule conflicts (1111 being an SO-100 thread, the whole batch being lerobot-software); those are routed to needs_human and the per-entry review_notes, which is where the ticket puts them.

IDS, SLUGS, SOURCES, SCOPE, FREQUENCY — all checked mechanically. No candidate id collides with setup-motors-no-response / servo5-cable-torn / wrong-leader-gear-ratio or with another candidate. All ten slugs (nine final plus the one replaced) were tested against the 55 `slug:` values in data/assemblies/follower.yaml, data/assemblies/leader.yaml and data/troubleshooting.yaml — no collision; longest is 37 chars, under the schema's 60. Every source is an https github.com issue or comment permalink with retrieved '2026-09-09'; every comment permalink id was matched to a comments[].url in the file. No Discord, Slack, WeChat or login-gated link appears anywhere in these nine threads, so none could leak into a source. github-actions[bot] carries author_association CONTRIBUTOR on 1590 but its stale/close boilerplate is not a maintainer statement: correctly excluded from sources and it does not lift that entry off "open". All nine scope tags are lerobot-software and all are right per the enum (install, teleop, record, cameras, python errors) — including wrist-camera-glitchy-in-record, whose confirmed cause is camera hardware but whose enum home is "cameras". Every frequency is 1 and equals the distinct threads cited (1606's second builder is in the same thread, not a second one).

related_steps / related_parts are empty on all nine, and that is the correct outcome rather than an unfinished field: data/schema/troubleshooting.json requires only [id, slug, title, symptoms, cause, fix, source], and nothing in the 52 step ids or 26 part ids covers a software defect (no camera part, no generic "motor", no software-setup step). Inventing one is forbidden by the ticket. If the reviewer wants these linked, `data/` needs new anchors first.

REQUIRED GIT QUERIES, re-run from /Users/macbook/Developer/so101-assembly-guide/.claude/worktrees/troubleshooting-candidates before any data file was read. Tier 1, `git log --oneline -S"troubleshooting"` — 12 commits; the two owning the data file and its schema are `86a2a8f data(schema): step slug, troubleshooting.yaml, vendors.yaml per ADR-0002` and `c80b40d repo: issue forms for step corrections, troubleshooting entries and site bugs; footer reports link`. Tier 2, `git log --grep='^Constraint:' --grep='^Directive:' --grep='^Rejected:' -- data/troubleshooting.yaml data/schema/troubleshooting.json` — one trailer, quoted verbatim from 86a2a8f: "Constraint: slug is frozen at first publish; changing one is a URL break, not a rename". That commit body also records verbatim: "Slug uniqueness is global across steps and troubleshooting entries, not per assembly, so a slug can never collide when pages are cross-linked." That is what drove the slug-collision check against data/assemblies/*.yaml above, and why the one slug edit is called out explicitly. (The prompt's tier-2 form `git log <path> --grep=...` fails with "fatal: option '--grep=^Constraint:' must come before non-option arguments"; the pathspec must follow `--`.)

NO FILES WRITTEN. Nothing under the repo was modified; data/troubleshooting.yaml, data/schema/ and data/assemblies/*.yaml were read only. The four review fields (scope, frequency, confidence, review_notes) must be stripped at promotion — data/schema/troubleshooting.json sets additionalProperties: false.

STANDING SCOPE CAVEAT FOR GUILLAUME: all nine entries are lerobot-software, which CLAUDE.md puts out of scope for v1, so the whole batch rides on the ticket's open decision 1. If only a subset is promoted, two stand apart: robot-disconnect-not-called-on-exception has a physical-damage consequence (the reporter burnt a motor because the follower stayed connected after a crash), and wrist-camera-glitchy-in-record is a hardware-capability problem wearing a software error. Three are version archaeology on entry points that no longer exist (unboundlocalerror-action-in-record and unrecognized-argument-policy-path on the deprecated `control_robot.py`; smolvla-stale-local-files-optim on lerobot 0.1.0's `lerobot/common/...` layout) and each carries that caveat in its review_notes — without it they mislead on current releases. Two (2621, 3997) rest on a maintainer saying only "Fixed in PR #N", so a reviewer may want to pin a minimum version before publishing.
